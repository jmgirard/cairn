# A fixture document, not documentation

This file is INPUT to `test_the_extractor_matches_its_committed_contract`. Its
expected extraction is committed beside it as `extractor-contract.expected.txt`
and is **never regenerated by the guard** — that is the whole point. The §8
ledger cannot pin the extractor's behaviour, because §9's remedy is to
regenerate the ledger and the comparison is invariant under co-regeneration.
This pair is not: change how the extractor behaves and the guard reds.

## 1. Zebra sorts last but comes first

Zebra first. Apple second. Middle sentence spans
a hard wrap and must normalize to one unit.

**A bold rule ends here.** The sentence after it is its own unit.

A sentence ending in a quotation mark said "like this." Another ending in a
parenthetical (like this.) And one ending in a code span with a period in it,
`tracking-rules.md`. The zero-unresolved bar keeps its hyphen.

## 2. This heading bounds section 1

Text under the second heading must not appear in section 1's extraction.
