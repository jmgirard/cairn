# Roadmap

_The only authority on milestone status. Grouped by status, not ID._
_Last hygiene check: 2026-07-20 (M102 done — verify-suite speedup; "Verify-suite subprocess cost" candidate graduated, 1 candidate added (cost-store test I/O, M102 Out), 1 lesson captured, none retired, M95 row pruned to retention). Release timing is user-declared — D-050._

Note: this repo dogfoods the tracking file formats by hand; it is a plugin,
not an R package, so R-specific gates don't apply.

## Milestones

_Released 1.0.0 2026-07-16 (tag v1.0.0) · 1.1.0 2026-07-19 (tag v1.1.0) · 1.1.1 2026-07-19 (tag v1.1.1)._

| ID | Title | Status | Depends on | Priority | File/Archive |
|---|---|---|---|---|---|
| M103 | Exploratory sources — supply-push ingestion: a corpus of uncited sources is triaged into candidates, not dismissed as irrelevant | review | — | normal | milestones/M103-exploratory-sources.md |
| M102 | Verify-suite speedup — the dogfood suite runs on its tests, not on process spawns, and greens in any repo state | done | — | normal | milestones/archive/M102-verify-suite-speedup.md |
| M96 | Growth ratchet — the rulebook is governed by attention, not by a permitted size | dropped | — | normal | milestones/archive/M96-rulebook-growth-ratchet.md |
| M100 | Review-finding enforcement — findings travel verbatim, outcomes meet projections | done | — | high | milestones/archive/M100-review-finding-enforcement.md |
| M101 | Decommissioning — machinery measured not to work is removed or repointed | done | — | high | milestones/archive/M101-decommission-measured-noops.md |
| M99 | Budget-first drafting — a capped artifact's size is visible while it is written | done | — | normal | milestones/archive/M99-budget-first-drafting.md |

## Candidates

_Ordered higher-priority-first (advisory only — candidates carry no Priority field). Triage: D-027._

- Dedicated `/explore-sources` skill for supply-push exploration: M103 lands the doctrine + `/milestone-plan` routing but no dedicated trigger; a standalone skill would make exploration discoverable to an agent not already in a planning session, at the cost of new surface near M56's rejected query-op / graph-tooling machinery. Swept: no row or D-entry covers an exploration skill; M56 rejected a formal query op and graph tooling, which this must not become. Promote if the rulebook + plan-step routing proves insufficiently discoverable in practice — added 2026-07-20 — M103 Out
- Cost-store test I/O, not a spawn: `test_attribution_mode_refuses_the_milestone_filter` (`scripts/tests/test_cairn_cost.py:299`) and its paired `cost.main(["--attribution"])` each scan the real ~25k-record telemetry store in-process (~1.6s for the one test), so the cost is store I/O, outside M102's subprocess scope. Fix direction: point the attribution tests at a small fixture store, keeping one test against the real store for the live-shape guarantee. Swept: no row or D-entry covers cost-store test I/O; M102 scopes it Out. Promote if the store grows enough to make this noticeable, or fold into any milestone touching cairn_cost — added 2026-07-20 — M102 Out
- Supersede D-056's yield clause and repair its placement test (RR04 rec 9, `apply` — parked by D-057): D-056's Consequences states "The test predicts no yield" and tells a reader to treat M95's −9 as evidence about the file's composition. The committed ledger at `cairn/references/rulebook-classification-ledger.md` contradicts it by ~56 lines net — though it CONFIRMS D-056's headline claim that the mass is mostly class 1/2, so the supersession is narrow: parts 1 (rulebook is current knowledge) and 3 (guard-pinning as deletion screen) stand. RR04 Q6 also finds the three-step test structurally defective — it misroutes intra-file restatement to class 1, since a restated rule IS a rule — and would add a step 0 (single home) and split deletion/placement from inversion/guard-verification. Precondition (commit the ledger) is MET. Deliberately not urgent: D-057 closed the stock-side program, so no pass depends on the clause; it is parked rather than dropped because a false statement stands in permanent history and IP4 makes supersession the only remedy. Swept: no row or D-entry covers this; D-057 references it and explicitly parks it. Promote if a measured `cairn_cost` regression reopens pass work, or fold into any milestone that next touches the placement test — added 2026-07-20 — RR04 rec 9
- Records-hygiene lesson family (M98-D1, deferred): M98's re-derivation found 8 `LESSONS.md` items that pass the maturation bar's principle test but fire at a hygiene or plan gate rather than at guard-authoring — candidate-row graduation timing (M35), archive-summary budgeting (M51), AC amendment at the implement gate (M69), sub-threshold finding judgment (M73), the archive-decisions collision sweep (M77), running a records rule over your own artifacts (M78), rule-home placement (M78), and compress-what-your-phase-owns (M87). They need their own read-trigger, not a seat in `guard-doctrine.md`, whose trigger is authoring a guard. Two (M69, M77) may instead be D-051 *ownership* retirements against `/milestone-implement` step 6 and `/milestone-plan`'s collision sweep, checkable when a milestone next ships those surfaces. Swept: no row or D-entry covers a second doctrine module; D-055 is the criterion, not a home. Promote when LESSONS.md next approaches either cap, or when a milestone touches those skill surfaces — added 2026-07-20 — M98-D1
- Anchor-choice doctrine for the mutation harness (RR03 rec 8, `consider`): harness anchors are exemplar blocks chosen "partly for matchability, not because each anchored sentence is doctrine", so an editorial pass pays a re-anchoring tax growing with the guard count. Home is `guard-doctrine.md` §1; one paragraph — anchor on rules, never scaffolding. **Trigger evaluated NEGATIVE 2026-07-20 (M95):** twelve removals cost one guard assert and one harness entry, and D-056(3) removes the freeze mechanism RR03 feared by making guard-pinning a deletion screen rather than a keep. Kept parked, not pruned: one pass is one data point, and RR04 notes 111 of 278 mutation blocks anchor in the rulebook, so any future relocation hits the trigger. Swept: no row or D-entry owns anchor choice. Promote if a LATER pass finds re-anchoring dominating its cost — added 2026-07-19 — RR03 rec 8
- Always-read audit frame (RR03 rec 7, `consider`): the `/milestone` audit — or M96's advisory output — names, per always-read file, its **inflow test** (what belongs here), **outflow or read-bound** (how content leaves, or how the read stops growing), and **attention signal** (what reports growth), so the next ungoverned file is visible before it costs 30 milestones. RR03 §5 derived the frame by applying it: DECISIONS has inflow-test + outflow (supersession) + no signal needed once read-bounded; LESSONS has all three after M98's graduation outflow; `tracking-rules.md` had **none of the three** — no inflow test (anything a milestone writes), no outflow (nothing until M95), no signal (nothing until M94/M96) — and that, not per-file governance, was the defect. One paragraph of doctrine, no new mechanism; RR03 rec 10 rejects unifying the three files' lifecycles, so this is the shared frame, never a shared machine. Swept: no row, archive entry, or D-entry covers an audit surface for always-read governance — D-045 supplies the two file classes and D-053 the per-class mechanism, but neither reports which files lack one. Promote once M95 and M96 give the rulebook all three elements, so the frame is derived from a worked case rather than a projected one — added 2026-07-20 — RR03 rec 7
- Archival-with-tombstone for superseded D-entries (RR02 rec 6, `consider`): move superseded entries verbatim to an archive file leaving a tombstone line, bounding the heading scan itself. RR02 judges it arguably IP4-clean (nothing fabricated, rewritten, or renumbered) but IP4-adjacent enough to need its own D-entry, and says it buys almost nothing once M97 bounds the read: the whole file stays greppable on disk and the heading scan grows ~100 chars per decision — decades of headroom at current velocity. Swept: no row or D-entry covers D-entry archival; M56/D-051's divergence-vector rejection binds any second record. Promote only if the heading scan itself becomes the cost — added 2026-07-19 — M97 Out
- Soft offset norm for rulebook growth (RR02 rec 5, `consider`): one rulebook sentence — *a milestone adding rulebook lines names the lines it removes, or logs why none* — as standing outflow pressure against the measured +7.6 lines/milestone inflow. RR02 rejected the same idea as a HARD gate (some milestones legitimately add with nothing to offset — M92's retirement rule had no predecessor) but recommends the soft form at ~3 lines' cost. Deliberately not planned now: RR02 says drop it if M96's ratchet alone proves sufficient, so it waits on evidence from a few M96 cycles. Swept: no row or D-entry covers an outflow norm for the rulebook; D-051 is the LESSONS.md analogue and is the model. Promote if the ratchet fires repeatedly without changing authoring behaviour — added 2026-07-19 — M96 Out
- Work-log staleness signal is corrupted by amendment entries: `/milestone` §2 flags an `in-progress` milestone with "no work-log entry in 14+ days", but a work log accumulates entries that record *queueing* rather than work — `Depends-on` amendments, mirror catch-ups, status transitions — and each refreshes the clock, so a genuinely stalled milestone reads as active. Found live at M88 T3: intraclass M48 had sat `planned` and unworked for a week while every one of its entries was a `Depends-on` amendment, which is what forced M88-D1 to abandon idleness for the `planned` case entirely. M88 solved it only for release-shaped milestones; the general signal is still wrong for every other milestone the audit judges. Fix direction: classify entries (work vs. bookkeeping) or measure from the first entry at a status rather than the last. Swept: no row, archive entry, or D-entry covers work-log signal quality — M83 is references-extraction staleness, a different parser — added 2026-07-19 — M88 T3
- Repair resumability for a declined scaffold migration: M82's §3 adds the successor `.gitignore` entry as its first action, but that is exactly what silences `check_gitignore_deprecations` (`cairn_validate.py:662` fires only when the old entry is present AND the new one absent) — so a declined move, or a "keep both and skip" on the both-present case, leaves the old directory on disk with no later repair run or `/milestone` audit ever surfacing it again, and `SKILL.md:218-220` tells the agent that silence is expected; the fix drives the step from the superseded-entry map plus a filesystem check, which contradicts M82's AC3 and Scope ("driven by the advisory's own output") and so needs a plan-time amendment, not a repair patch; bounded today — `DEPRECATED_GITIGNORE` holds one pair and this repo is already migrated; swept: no existing row covers repair resumability — promote when a second repo adopts the new shelf, or fold into the next `/cairn-init` repair milestone — added 2026-07-18 — M82 review F1 (85)
- Provenance blocks are concatenated without a separator, so an extraction status could swallow body prose: `_provenance_block` collects EVERY `**Provenance.**`-headed run (M81 F2/F3's decoy defence) but joins them with no blank line (`cairn_validate.py:294`) while `_extraction_status` collects to the next blank line — so a second heading match opens a block the first block's status runs straight into. **Corrected 2026-07-18 (hotfix #83):** the cited evidence is gone — every second match in this repo was a *false* heading (prose wrapping onto a line starting with "provenance"), and tightening `_PROV_HEAD` to a label test dropped both `oracle-*-notes.md` pages from 652/1213 chars to 383/418, each now a single genuine block with an unabsorbed status. The join mechanism survives but has no live trigger: it needs ≥2 real headings, i.e. the decoy `## Provenance` shape F2/F3 deliberately supports. Swept: no row covers block concatenation; M83 scoped both readers Out — promote if a page is ever misclassified — added 2026-07-18 — M83 T7
- Citekey resolution + dependent discovery: `citekey (p. N)` is mandated (`validation-doctrine.md:79`) but never parsed in either direction — a citekey naming no page and a page nothing cites both pass — and `cairn_impact.py:45` excludes `references/`, so the one record type designated "current knowledge, corrected in place" (`tracking-rules.md:137-139`) is the one with no impact tooling; blocked on a standing rejection — M56 rejected "a formal query op" and "graph tooling" (`archive/M56-llm-wiki-investigation.md:17-19`), which a superseding D-entry must narrow before this is planned; promote if restatement drift produces a live wrong value (intraclass's Koo-bands and unbacked-Cicchetti chains are the candidates) — added 2026-07-18 — M79 Out
- Concurrent-cairn-operator hardening: if two people ever run cairn in the same repo at once the tracking files race — milestone IDs and D-numbers are model-picked with no allocator, duplicate D-numbers auto-merge and validate GREEN (`cairn_validate.py:500-506` uses a `set`), `/milestone-plan` commits to the default branch with no fetch/pull at any step, and `check_single_in_progress` (`cairn_validate.py:61-64`) is a hard FAIL two operators trip by construction; cheapest standalone piece is a `check_d_uniqueness`; swept: no existing row or D-entry covers it — promote if a second cairn operator actually appears — added 2026-07-18 — M72 Out (D-043)
- Content-gated memory guard: make M19's memory-boundary hook inspect the write and fire only on durable-state signals (decisions, conventions, project facts), staying silent on pure per-user prefs; promote only if the unconditional soft nudge proves too noisy — added 2026-07-11 — M19 Out
- Contributor-facing scaffold: cairn ships nothing a repo can hand an outsider — no CONTRIBUTING, PR template, or issue template (`git ls-files` matches zero; there is no `.github/` in this repo), so a contributor learns the branch/commit/tracking conventions only by being told; would be a `/cairn-init` opt-in, not a core scaffold addition — promote once M72's README subsection is in use and proves insufficient — added 2026-07-18 — M72 Out
- Branch-protection compatibility: cairn structurally requires direct pushes to the default branch (`tracking-rules.md:248-252` docs-only commits; `/milestone-review`'s post-merge hygiene pass), which a repo protecting its default branch behind required PRs cannot do — needs a decision on whether hygiene commits get their own PR or the rule gains a protected-branch mode — promote if an adopting repo turns on branch protection — added 2026-07-18 — M72 Out
- Scaffold-spec version stamp / content-drift detection (Direction 2, deferred from M24): M24 detects *missing* §1 pieces but not a piece whose template *body* changed while the file still exists; stamp a scaffold-spec version into the adopted CLAUDE.md and compare against the plugin's current spec to catch content drift — needs a maintained spec version + changelog + a definition of "what counts as a bump"; promote only if content drift (as opposed to missing files) actually bites — added 2026-07-12 — M24 Out
