# Roadmap

_The only authority on milestone status. Grouped by status, not ID._
_Last hygiene check: 2026-07-19 (M97 done — bounded DECISIONS read; collision-sweep row graduated; M90 pruned under terminal-row retention). LESSONS 49/50 lines, 21,085 chars — over its 20,500 threshold: M97 retired nothing and compression is the treadmill RR03 diagnosed, so lesson graduation discharges it. Release timing is user-declared — D-050._

Note: this repo dogfoods the tracking file formats by hand; it is a plugin,
not an R package, so R-specific gates don't apply.

## Milestones

_Released 1.0.0 2026-07-16 (tag v1.0.0) · 1.1.0 2026-07-19 (tag v1.1.0) · 1.1.1 2026-07-19 (tag v1.1.1)._

| ID | Title | Status | Depends on | Priority | File/Archive |
|---|---|---|---|---|---|
| M94 | Cost instrumentation — measure what a milestone spends before governing it | done | — | high | milestones/archive/M94-cost-instrumentation.md |
| M95 | Rulebook editorial slimming — the rulebook states rules, not their legislative history | planned | — | high | milestones/M95-rulebook-editorial-slimming.md |
| M96 | Growth ratchet — the rulebook is governed by attention, not by a permitted size | planned | M94, M95 | normal | milestones/M96-rulebook-growth-ratchet.md |
| M97 | Bounded DECISIONS read — the sweep scans headings and reads what it hits | done | — | high | milestones/archive/M97-bounded-decisions-read.md |
| M98 | Lesson graduation to doctrine — a matured lesson family leaves LESSONS.md whole | planned | — | high | milestones/M98-lesson-graduation-to-doctrine.md |
| M93 | Hygiene-line accretion — the ROADMAP stamp is replaced, not appended | done | — | high | milestones/archive/M93-hygiene-line-replace-not-append.md |
| M91 | Reference re-verification — the three partial extractions get read against their sources | done | — | normal | milestones/archive/M91-reference-reverification.md |
| M92 | Lesson retirement — a lesson leaves LESSONS.md when a guard enforces it or another file owns it | done | — | high | milestones/archive/M92-lesson-retirement.md |

## Candidates

_Ordered higher-priority-first (advisory only — candidates carry no Priority field). Triage: D-027._

- Rulebook read-cost reduction: `skills/shared/tracking-rules.md` is read WHOLE at every skill fire (3–4× per milestone) and grew 33,746 → 52,797 chars across M63–M93 (+56%, ~630 chars/milestone sustained), governed by no cap on either weight axis. D-031 is both the precedent and the fence: it extracted the Validation doctrine for exactly this cost ("~68 always-read rulebook lines … ~11% of every non-numeric session's core read") and set the norm "new domain doctrine gets a module, not a rulebook section" — but ruled extraction legal only for "a self-contained, conditionally-relevant domain block", with "the cross-skill contract (file map, caps, status, git model, gates, output discipline, profiles mechanism) stays monolithic per RR01 rec 15". **Corrected 2026-07-19 (RR02):** extraction is NOT the remedy — RR02 found no new domain doctrine entered after M58, yet the file still grew 44%, because the growth mode is the thickening of existing cross-skill sections and the dominant inflow is *rationale, not rules* (the rulebook restating D-entry context it does not own, against `tracking-rules.md:11-13`). The remedy is an editorial pass — state the rule, cite the D-entry, delete the defense — targeting ~550-600 lines with zero rules removed (RR02 rec 1). RR01 rec 15 upheld. Swept: no row, archive entry, or D-entry covers rulebook mass. Promote: user-approved at the RR02 ingest gate — added 2026-07-19 — M94 Out
- Lesson graduation to doctrine (RR03 rec 4, `apply`, high): `cairn/LESSONS.md` sits at 20,484/20,500 chars and 48/50 lines — 16 chars and 1 line of headroom — and has grown NET since M92 gave it a retirement outflow, because consolidation conserves mass while relaxing the item axis (RR03 Q3; D-049 names the mechanism itself at "consolidating items raises the mean"). RR03 classifies 18 of 31 items (12,232 of 19,560 item-chars, 63% of item mass) as matured guard-authoring craft that fails BOTH D-051 criteria forever by construction — no test fails on the mistake, because they teach the judgment guards cannot make ("The harness catches neither"), and no tracking-file slot owns guard-authoring craft. Fix direction: distil the family into `skills/shared/guard-doctrine.md` (D-031's module shape, conditionally read at guard-authoring time rather than surfaced at plan time), retire the covered lessons whole under D-051's ownership criterion, and annotate D-051 naming maturation as a third outflow beside enforcement and ownership. The milestone's T1 must RE-DERIVE the family boundary, never inherit RR03's 63% classification; RR03 flags `LESSONS.md:19` (M51) and `:44` (M87) as a possible second tier (records-hygiene craft, not guard craft). Swept: no row, archive entry, or D-entry covers graduation — M92/D-051 is retirement by enforcement/ownership, a different exit; RR02's rulebook-outflow row is a different file. Promote: next, as M98 — added 2026-07-19 — RR03 rec 4
- Anchor-choice doctrine for the mutation harness (RR03 rec 8, `consider`): harness anchors are chosen as exemplar blocks per file, "partly for matchability, not because each anchored sentence is doctrine" — 14 of 21 of M95's ledger blocks are guard-pinned, so every editorial pass pays a re-anchoring tax growing with the guard count, and RR03 warns it can harden into a freeze on rulebook editability. Fix direction: state an anchor-choice discipline — anchor on rules, not on scaffolding — in the harness's own doctrine, whose home M98 creates (`skills/shared/guard-doctrine.md`). Deliberately not folded into M98: RR03 gates it on evidence ("only if M95′'s re-anchoring cost proves material"), which M95 has not yet produced. Swept: no row, archive entry, or D-entry covers anchor choice — D-051's guard/lesson boundary and M95's AC2 re-anchor machinery are adjacent but neither owns it. Promote if a rulebook editorial pass finds re-anchoring dominating its cost — added 2026-07-19 — RR03 rec 8
- `decision heading quality` classifier redesign (M97 review F1-F4/F6, all scored sub-80 and logged, not actioned): the advisory's claim matcher is a four-stem regex (`supersed|annotat|narrow|refin`) with a `[^.\n]*` window, and it misses the noun form ("this supersession of D-031" — the repo's own idiom, used in D-054's body), claims split across a line wrap (the window dies at the newline and cairn hard-wraps at ~66 chars), reversed order ("D-031 is superseded here"), and synonyms outside the rule's stated vocabulary ("replaces", "overrides", "retires"); it also excludes neither code fences nor block quotes, so a fenced example reads as a real claim (a FALSE POSITIVE, the direction D-023 says is worse) and a fenced `### D-` line would truncate the enclosing entry's body. **The findings interact and cannot be patched independently** — closing the noun-form miss is safe today ONLY because the wrap limit suppresses the false positive it would otherwise raise on D-054's own descriptive "headings hide a supersession" line, so fixing either alone leaves the advisory's green contingent on where one paragraph happens to wrap. Fix direction: a claim classifier scoped to the sentence rather than the line, fence/quote-aware, distinguishing a CLAIM ("supersedes D-031") from a DESCRIPTION ("headings hide a supersession"), verified against a fixture set varying phrasing, wrap point, and voice independently (M57/M81: vary every axis the prose is free in, where the value under test lives). Recall does not depend on this matcher — the read protocol's back-reference step covers the legacy and missed cases — so this is advisory quality, not a recall hole. Swept: no row or D-entry covers the classifier; M97 Out covers mass advisories, a different thing. Promote if a real supersession is ever missed in practice, or before another repo relies on the advisory — added 2026-07-19 — M97 review F1-F4/F6
- Verify-suite subprocess cost: the three suites run 722 tests in ~37s wall, but the cost is process spawns, not test count — `skills/tests` runs 441 tests in 0.50s while `hooks/tests` runs 72 in 16.5s (~0.23s each, essentially all shelling out) and `scripts/tests` 209 in 21.6s, of which two M81 combinatorial-fixture tests alone cost 5.7s (`test_contradiction_is_caught_wherever_it_sits_and_however_wrapped` 3.34s; `test_decoration_layout_and_phrasing_vary_independently` 2.41s). At ~6 verify runs per milestone that is ~3.7 min of a ~39 min milestone (~10%). Fix direction: invoke hook entry points in-process instead of `subprocess.run` per test, keeping a few end-to-end spawns for the real CLI contract, and collapse the two fixture products. Independent of M94's signal — promotable immediately. Swept: no row or D-entry covers suite runtime — added 2026-07-19 — M94 Out
- Archival-with-tombstone for superseded D-entries (RR02 rec 6, `consider`): move superseded entries verbatim to an archive file leaving a tombstone line, bounding the heading scan itself. RR02 judges it arguably IP4-clean (nothing fabricated, rewritten, or renumbered) but IP4-adjacent enough to need its own D-entry, and says it buys almost nothing once M97 bounds the read: the whole file stays greppable on disk and the heading scan grows ~100 chars per decision — decades of headroom at current velocity. Swept: no row or D-entry covers D-entry archival; M56/D-051's divergence-vector rejection binds any second record. Promote only if the heading scan itself becomes the cost — added 2026-07-19 — M97 Out
- Soft offset norm for rulebook growth (RR02 rec 5, `consider`): one rulebook sentence — *a milestone adding rulebook lines names the lines it removes, or logs why none* — as standing outflow pressure against the measured +7.6 lines/milestone inflow. RR02 rejected the same idea as a HARD gate (some milestones legitimately add with nothing to offset — M92's retirement rule had no predecessor) but recommends the soft form at ~3 lines' cost. Deliberately not planned now: RR02 says drop it if M96's ratchet alone proves sufficient, so it waits on evidence from a few M96 cycles. Swept: no row or D-entry covers an outflow norm for the rulebook; D-051 is the LESSONS.md analogue and is the model. Promote if the ratchet fires repeatedly without changing authoring behaviour — added 2026-07-19 — M96 Out
- Work-log staleness signal is corrupted by amendment entries: `/milestone` §2 flags an `in-progress` milestone with "no work-log entry in 14+ days", but a work log accumulates entries that record *queueing* rather than work — `Depends-on` amendments, mirror catch-ups, status transitions — and each refreshes the clock, so a genuinely stalled milestone reads as active. Found live at M88 T3: intraclass M48 had sat `planned` and unworked for a week while every one of its entries was a `Depends-on` amendment, which is what forced M88-D1 to abandon idleness for the `planned` case entirely. M88 solved it only for release-shaped milestones; the general signal is still wrong for every other milestone the audit judges. Fix direction: classify entries (work vs. bookkeeping) or measure from the first entry at a status rather than the last. Swept: no row, archive entry, or D-entry covers work-log signal quality — M83 is references-extraction staleness, a different parser — added 2026-07-19 — M88 T3
- Prior-PR-comments review lens is a standing no-op in a solo-maintained repo: `/milestone-review`'s lens reads GitHub PR review threads, but ackwards M68, M67, and M61 each logged zero findings from it — M67 recorded its touched files as "changed by direct-to-master commits" with no PR comments, M61 logged "prior-PR: no evidence (no-op)" — because the substantive review record lives in archived milestone `## Review` sections while the PR threads hold only Codecov bot noise. It spends `gh api` calls and a reasoning pass at every review looking where the content provably is not, and three consecutive clean results are indistinguishable from a lens that cannot fire. Fix direction: point it at `milestones/archive/` `## Review` sections, which M77's lesson already names as the home of milestone-local decisions; needs a decision on whether the PR-thread read is replaced or kept as a second surface for repos that do review on GitHub. Swept: no candidate row, archive entry, or D-entry covers the lens's input surface — added 2026-07-19 — M89 plan (field evidence, ackwards); M91 quantified it in this repo — the lens enumerated all 11 merged PRs touching `cairn/references/` and found zero inline comments and zero review bodies, so the no-op is now measured rather than inferred
- Repair resumability for a declined scaffold migration: M82's §3 adds the successor `.gitignore` entry as its first action, but that is exactly what silences `check_gitignore_deprecations` (`cairn_validate.py:662` fires only when the old entry is present AND the new one absent) — so a declined move, or a "keep both and skip" on the both-present case, leaves the old directory on disk with no later repair run or `/milestone` audit ever surfacing it again, and `SKILL.md:218-220` tells the agent that silence is expected; the fix drives the step from the superseded-entry map plus a filesystem check, which contradicts M82's AC3 and Scope ("driven by the advisory's own output") and so needs a plan-time amendment, not a repair patch; bounded today — `DEPRECATED_GITIGNORE` holds one pair and this repo is already migrated; swept: no existing row covers repair resumability — promote when a second repo adopts the new shelf, or fold into the next `/cairn-init` repair milestone — added 2026-07-18 — M82 review F1 (85)
- Provenance blocks are concatenated without a separator, so an extraction status could swallow body prose: `_provenance_block` collects EVERY `**Provenance.**`-headed run (M81 F2/F3's decoy defence) but joins them with no blank line (`cairn_validate.py:294`) while `_extraction_status` collects to the next blank line — so a second heading match opens a block the first block's status runs straight into. **Corrected 2026-07-18 (hotfix #83):** the cited evidence is gone — every second match in this repo was a *false* heading (prose wrapping onto a line starting with "provenance"), and tightening `_PROV_HEAD` to a label test dropped both `oracle-*-notes.md` pages from 652/1213 chars to 383/418, each now a single genuine block with an unabsorbed status. The join mechanism survives but has no live trigger: it needs ≥2 real headings, i.e. the decoy `## Provenance` shape F2/F3 deliberately supports. Swept: no row covers block concatenation; M83 scoped both readers Out — promote if a page is ever misclassified — added 2026-07-18 — M83 T7
- Citekey resolution + dependent discovery: `citekey (p. N)` is mandated (`validation-doctrine.md:79`) but never parsed in either direction — a citekey naming no page and a page nothing cites both pass — and `cairn_impact.py:45` excludes `references/`, so the one record type designated "current knowledge, corrected in place" (`tracking-rules.md:137-139`) is the one with no impact tooling; blocked on a standing rejection — M56 rejected "a formal query op" and "graph tooling" (`archive/M56-llm-wiki-investigation.md:17-19`), which a superseding D-entry must narrow before this is planned; promote if restatement drift produces a live wrong value (intraclass's Koo-bands and unbacked-Cicchetti chains are the candidates) — added 2026-07-18 — M79 Out
- Concurrent-cairn-operator hardening: if two people ever run cairn in the same repo at once the tracking files race — milestone IDs and D-numbers are model-picked with no allocator, duplicate D-numbers auto-merge and validate GREEN (`cairn_validate.py:500-506` uses a `set`), `/milestone-plan` commits to the default branch with no fetch/pull at any step, and `check_single_in_progress` (`cairn_validate.py:61-64`) is a hard FAIL two operators trip by construction; cheapest standalone piece is a `check_d_uniqueness`; swept: no existing row or D-entry covers it — promote if a second cairn operator actually appears — added 2026-07-18 — M72 Out (D-043)
- Budget-first drafting (cap prevention): up-front per-artifact budgets in the templates/rulebook so first drafts land under their cap by construction, rather than compressing after. **Widened 2026-07-19 from the 150-line milestone body to EVERY capped artifact** — the ≤25-line archive summary and the 400-char `NON_ITEM_LINE_CAP` both produced the same thrash at M97's hygiene pass. **Promotion trigger has now FIRED:** the row said "reassess once M69's diagnostic + single-pass discipline is in use and we can see whether the cure suffices" — it is in use, and it did not suffice. Evidence (M97 review hygiene, 2026-07-19): the archive summary ran 32→29→27→27→26→25 lines and the hygiene stamp 414→350 chars, ~7 round trips, two of the edits swapping three lines for three — verbatim the trap `LESSONS.md:19` (M51) names ("a shorter sentence that still wraps to N lines saves nothing"). Diagnosis: the lesson exists and is well written, so the defect is APPLICATION, not knowledge — which is why the remedy is budget-by-construction and NOT another lesson line (LESSONS is already over its density threshold). Two parts: (a) per-artifact line/char budgets stated in the templates where the drafting happens; (b) a drafting-time counter — a small `cairn_scripts` entry point taking a path and reporting it against whichever cap applies, so the count is visible while writing instead of at `cairn_validate` time, the caps currently living in three separate places. Distinct from M84's density advisory (checked 2026-07-18): this is wrapped prose where line count tracks weight correctly and the defect is drafting overshoot, whereas M84 covers item-list files where line count cannot see weight at all. Swept: no row, archive entry, or D-entry covers drafting-time cap feedback — added 2026-07-17, widened 2026-07-19 — M69 Out; M97 review hygiene
- Content-gated memory guard: make M19's memory-boundary hook inspect the write and fire only on durable-state signals (decisions, conventions, project facts), staying silent on pure per-user prefs; promote only if the unconditional soft nudge proves too noisy — added 2026-07-11 — M19 Out
- Contributor-facing scaffold: cairn ships nothing a repo can hand an outsider — no CONTRIBUTING, PR template, or issue template (`git ls-files` matches zero; there is no `.github/` in this repo), so a contributor learns the branch/commit/tracking conventions only by being told; would be a `/cairn-init` opt-in, not a core scaffold addition — promote once M72's README subsection is in use and proves insufficient — added 2026-07-18 — M72 Out
- Branch-protection compatibility: cairn structurally requires direct pushes to the default branch (`tracking-rules.md:248-252` docs-only commits; `/milestone-review`'s post-merge hygiene pass), which a repo protecting its default branch behind required PRs cannot do — needs a decision on whether hygiene commits get their own PR or the rule gains a protected-branch mode — promote if an adopting repo turns on branch protection — added 2026-07-18 — M72 Out
- Scaffold-spec version stamp / content-drift detection (Direction 2, deferred from M24): M24 detects *missing* §1 pieces but not a piece whose template *body* changed while the file still exists; stamp a scaffold-spec version into the adopted CLAUDE.md and compare against the plugin's current spec to catch content drift — needs a maintained spec version + changelog + a definition of "what counts as a bump"; promote only if content drift (as opposed to missing files) actually bites — added 2026-07-12 — M24 Out
