# Roadmap

_The only authority on milestone status. Grouped by status, not ID._
_Last hygiene check: 2026-07-27 (post-merge after M120 — M120 archived, M115's row pruned to hold terminal-row retention at 5. LESSONS 49 of 50: no lesson added and none retired; M117's harness-blind-spot line was EXTENDED in place with M120's finding that a green §8 certification does not prove inversion, which keeps the count flat. 20 candidates.)_

Note: this repo dogfoods the tracking file formats by hand; it is a plugin,
not an R package, so R-specific gates don't apply.

## Milestones

_Released 1.0.0 2026-07-16 (tag v1.0.0) · 1.1.0 2026-07-19 (tag v1.1.0) · 1.1.1 2026-07-19 (tag v1.1.1) · 1.2.0 2026-07-23 (tag v1.2.0)._

| ID | Title | Status | Depends on | Priority | File/Archive |
|---|---|---|---|---|---|
| M119 | RR08's follow-ons — the decisions-format advisory (D-075), the rulebook history enumeration, and a two-sided exempt-set guard | done | M118 | normal | milestones/archive/M119-decisions-advisory-and-consistency-guards.md |
| M120 | Ingest the Opus 5 prompting guide, and adopt the three conduct rules cairn has no home for | done | — | normal | milestones/archive/M120-opus5-guide-adoptions.md |
| M121 | Verification triage — classify every self-verification instruction, and re-decide D-067's two fresh-context readers | planned | M120 | normal | milestones/M121-verification-triage.md |
| M118 | The milestone-local `## Decisions` section is history and joins the cap-exempt set (D-074, supersedes D-046's choice 3) | done | — | normal | milestones/archive/M118-decisions-section-cap-exempt.md |
| M117 | Detector coverage on the site axis, and a plan gate that records the alternative it rejected (intraclass M93 passes 6–9) | done | — | normal | milestones/archive/M117-detector-site-axis-and-recorded-alternative.md |
| M116 | Repair the three-step placement test — retention takes the deletion arm, inversion moves to guard verification, and a step-0 single-home check (RR04 rec 9) | done | — | normal | milestones/archive/M116-placement-test-repair.md |

## Candidates

_Ordered higher-priority-first (advisory only — candidates carry no Priority field). Triage: D-027._

- The hook's heading normalization is unguarded against case drift: `hooks/session_context.py`'s `heading_name` lowercases so the hook and the cap counters agree on what a cap-exempt section is — the exact agreement M113's LESSONS line says must be matched by hand because the two layers cannot share code — but nothing reds when it stops. Measured at M119's post-merge hygiene 2026-07-27: removing `.lower()` from `heading_name` leaves all 98 hooks tests green, and `## Work Log` then misses the exempt set, which is M113's original bug verbatim. M119's `TestExemptSetMirror` does not cover this: it compares the two `CAP_EXEMPT_SECTIONS` constants, and both sides stay equal while the normalization applied to headings diverges — which is why M113's lesson was checked for retirement at this hygiene pass and deliberately KEPT (the retirement bar is a test that FAILS on the mistake, never a guard in the same area). Swept 2026-07-27: no row or D-entry covers hook-side normalization; M119's row is the constant mirror, a different axis. Promote when a third caller needs the same normalization, or on any live recurrence of a heading missing the exempt set — never on a count of milestones — added 2026-07-27 — M119 post-merge hygiene
- Rulebook says a decision entry "is never one line" while D-077 records the counterexample: `tracking-rules.md:126` explains why the `decisions format` advisory does not police entry length — "because a decision entry carries its rationale and is never one line (D-075)" — but D-077 (2026-07-27, shipped by M119 itself) measures the corpus at 23 of 24 entries wrapping and names the exception verbatim, M84's `- 2026-07-18: review — 3 lenses (…)`. The operative reasoning survives the exception (D-077 calls it "an entry of the work log's own genre sitting in a decisions section"), so this is an overclaiming absolute, not a wrong rule — the M114 lesson's shape, where a characterization beside a correct measurement is the falsifiable surface. Found at M119's review as F4 (scored 80) and deliberately not fixed there: the sentence is mutation-pinned, and re-anchoring a reflowed rulebook line is the M104 trap that fed M119's nine-round §8 loop, which the review-entry override existed to stop. Swept 2026-07-27: no row, no archive entry, and no D-entry beyond D-075/D-077 covers the one-line grammar. Promote when `tracking-rules.md` is next edited near that bullet for another reason — the re-anchoring cost is then already being paid — never as a standalone pass — added 2026-07-27 — M119 review F4 (80)
- No trip counter on the guard-doctrine §8 description-layer loop: the thrash rule counts REVIEW returns (`/milestone-review` step, D-064) with triggers at the third return and at one criterion failing twice by the same shape, but §8 rounds run inside `/milestone-implement` before review exists, so M119 ran **nine** of them with the counter at zero and no tripwire reachable. The repeated shape was textbook trigger (b): rounds 5-9 each found a false claim in the comment the previous round's fix had written, and round 9's two findings were in round 8's text. Measured cause (round 9): the returns scaled with the volume of prose the milestone wrote, not the complexity of the code it shipped — 14 lines of signature table under 67 lines of comment, a 723-line test class with 186 lines of pure comment. §8 states its own falsifier ("if guard-authoring milestones still average multiple description-layer returns after adoption, the step didn't work — retire it (D-059), don't tune it") and M119 is evidence it fired; but round 7 found a live shipped false positive, so retirement is not free. Two candidate dispositions: retire §8 and fold description accuracy into the review fan-out, or extend the thrash counter to implement-phase loops. Swept: no row or D-entry covers a counter outside the review loop; D-064/D-065 are the review-loop rule itself and M114's own guard. Promote when the next guard-authoring milestone runs §8 and its round count is recorded — the disposition is the maintainer's and needs a D-entry either way. **Third-party corroboration 2026-07-27 (M120):** Anthropic's Opus 5 prompting guide reports the same cost independently of cairn — files the model writes to disk "(reports, Markdown documents, summaries) are often longer than on prior models", remedied by a length calibration, "Match the length of written documents to what the task needs: cover the substance, but do not pad with filler sections, redundant summaries, or boilerplate" (`references/prompting-opus-5.md`, § Written deliverable length). This adds a **third disposition** beside the row's two: budget the description layer, rather than retiring §8 or extending the thrash counter — which fits M119's own measured cause, prose volume rather than code complexity. The promotion condition is unchanged: a guide's advice about the model is not a measurement of this repo, and only the next guard-authoring milestone's recorded §8 round count can be. — added 2026-07-27 — M119 §8 round 9
- Reasoning-effort dial per spawned agent: Anthropic's Opus 5 guide names effort (low/medium/high/xhigh) as the primary control for token cost and latency, and reports that review accuracy holds at lower settings — but cairn's spawn vocabulary is model tier alone (`tracking-rules.md` "Model and agent strategy"; verified 2026-07-27: the string `effort` occurs twice in the corpus, both meaning "best-effort"). Blocked mechanically, not by preference: the Agent tool exposes `model` and no effort parameter, effort comes from an agent definition's frontmatter, and cairn ships no agent definitions at all (no `agents/` anywhere in the repo, verified 2026-07-27) — so adopting this means cairn starts shipping agent definitions, new surface adjacent to what D-026 dropped as off-model ("tiered tool exposure presumes per-tier tool gating"; D-026 is about tool gating, not effort, so it is adjacent rather than a standing rejection). Swept: no row or D-entry covers effort or agent definitions. Promote when cairn ships an agent definition for any other reason, or when the Agent tool exposes effort per spawn — never on a count of expensive sessions — added 2026-07-27 — M120 Out
- Numeric cap on subagent spawning: the corpus has no cap, count limit, or budget on spawning anywhere (verified 2026-07-27); three of eleven spawn sites are open-ended, and a lifecycle's floor is 5 subagents with no ceiling. The Opus 5 guide recommends deterministic caps for cost-sensitive workloads. Blocked by a standing rejection that must be superseded first, not ignored: `milestone/SKILL.md:79-84` reports the spawn count and then forbids acting on it — "never treat a large figure as a finding to act on, and never propose a cap from it. No governing mechanism over these numbers exists or is owed: D-057 closed the stock-side size-governance program" — which is guard-pinned by `test_cost_audit_line.py:45-55`. M120 ships delegation *guidance*, which that prose does not touch; a cap needs the superseding entry. Swept: no row or D-entry covers a spawn cap; D-057 is the stock-side closure it rests on. Promote when `cairn_cost` shows a measured regression attributable to spawn volume — D-057's own re-open trigger, and never a felt-cost one — added 2026-07-27 — M120 Out
- Redistribute the milestone template's drafting budgets now that `## Decisions` has stopped counting: the preamble **used to reserve ≥21 lines** for that section on the stated ground that it was "still counted (D-030/D-046)" — a ground D-074 removed and M118 struck from the template, so the freed lines now sit as undirected headroom rather than as budget for the five plan-owned sections the block does size (Goal 7 · Scope 26 · AC 28 · Coverage 11 · Tasks 25, each a measured p75 over 99 files). The block's stale "p90 21 / max 35" figure for the section went with it, unreplaced (corrected here 2026-07-27, M118: the row described the pre-M118 template). Deliberately deferred at M118's gate — the block has drifted twice under full attention (M99's fixed-point lesson) and a re-issue is a measurement pass of its own, so M118 corrects the stale ground and spends none of the freed space. Swept: no row or D-entry covers the drafting budgets; D-049/D-058 govern density thresholds, a different axis, and M99 authored the block. Promote when a plan draft is compressed against a per-section budget while the file sits under the cap with the freed headroom unspent — never on a count of milestones — added 2026-07-27 — M118 Out
- Disposition rule for sub-threshold findings about self-shipped doctrine (RR06 rec 6, `consider`, banked outside M114 per its BC8): a finding scoring under 80 whose subject is doctrine the milestone itself ships is resolved at the open gate or declined with a recorded reason, never merely logged. Two sentences in `/milestone-review`'s scoring step. Motivated by four under-scored predictive findings in M114 that later fired — F4 (60) predicted a trigger collision that occurred at pass 3; J5 (35) predicted a miscount that occurred at pass 5; RR06 adds F3 and G6. RR06 REJECTED changing the rubric or threshold (n=4 with selection bias, and every >=80 finding was real), so this is a disposition change only. Swept: no row or D-entry covers finding disposition; the scoring step is D-064-adjacent but untouched by it. Promote if a further under-scored finding about self-shipped doctrine fires after being logged — never on a count of passes. Lineage: RR05 B3 → RR06 Q5 → rec 6 → BC8 — added 2026-07-26 — M114 Out, transcribed to `main` 2026-07-26 at M115's plan gate because M114's copy exists only on its parked branch (M115 Out)
- Partial-pin asserts in `skills/tests/test_lesson_graduation.py`: three `assertIn` calls each pin only a fragment of a longer shipped sentence, so the rest of that sentence deletes green — `:45` "Read this whenever authoring or editing a test that locks prose" (the sentence runs on through the fixture, matcher and `cairn_validate` cases), `:51` "a module of `tracking-rules.md`" (runs on through D-031's reason and the conditional-read clause), and `:125` "An exclusion list may name only history files" (runs on into the operative never-a-live-directory remedy). Verified against the shipped bytes 2026-07-26 rather than inferred, and the check corrected what a first pass asserted: `:45` and `:125` are strict prefixes of their sentences, while `:51`'s literal starts six characters in (`It is ` precedes it) and is a mid-sentence fragment — the class is a partial pin, not specifically a lead-in. All three arrived with M98 (`9b8d7ff`, present on `main`); M114's own fourth instance of the class was widened at pass 8 instead of banked here, so this row is M98's residue only. RR07 §BC3 closed one instance of this class by hand; §BC2 is not a second, since it made `test_sweep_section_states_the_silent_cell_rule` gain a first assert over prose pinned by nothing, which is a different defect. One hand-closure plus M114's own at pass 8 is why the row is about the class and not the instances. Swept: no row, archive entry or D-entry covers assert truncation; the parked anchor-choice row (RR03 rec 8) shares `guard-doctrine.md` as a home but is about which block to anchor on, not how much of it an assert pins. Promote when a systematic remedy is identified — a harness check that a registered block is a whole sentence in its target, or a probe that deletes each pinned block's continuation — never after N further hand-widenings. **Remedy 1 evaluated NEGATIVE 2026-07-27 (M116 plan):** classifying all 412 `REGISTRY` blocks against their targets found 22 whole sentences vs 148 prefix-only, 31 suffix-only and 211 mid-fragment — a whole-sentence harness check reds 390/412, most of them fragments by design (`## Coverage`; D-042's per-clause entries), so it is not a systematic remedy. Remedy 2 and the drop condition stay untested: the pass classified blocks, it did not delete continuations. Drop if a mutation pass over the whole registry finds no registered block whose continuation deletes green. — added 2026-07-26 — M114 pass 8
- One-surface pin for a doctrine rule — a guard asserting a rule's phrase occurs in exactly one file, so a restatement elsewhere reds instead of silently forking it (M112's motivation). **Re-cut out of M114 at its third return (2026-07-26)**, having consumed all three returns across six findings of one shape: the detector, or the controls exercising it, did not cover a rendering the phrase can take — a line wrap, a blockquote continuation marker, nested blockquotes in both `>>` and `> >` spellings, emphasis applied to part of the phrase, case, word boundaries, and finally corpora with no non-vacuity assert, so the whole detector deleted green. The standing problem: phrase-search requires the searcher to enumerate every rendering, and enumeration by the detector's own author is exactly what guard-doctrine §3 says cannot be trusted. Swept: no row, archive entry or D-entry covers fork detection; the parked anchor-choice row (RR03 rec 8) shares `guard-doctrine.md` as a home but is about anchor choice, not detection. Promote when a rendering-independent approach is identified — a markdown/AST parse, or a content hash over normalized doctrine blocks — never after N further attempts at a wider matcher, per the falsifying-promotion-condition rule M114 ships. **Producer-sweep approach evaluated NEGATIVE 2026-07-27 (M117 plan):** intraclass M93's pass-9 fix — sweep the producer's own outputs and assert an invariant over them, enumerating nothing — is rendering-independent for a *function's* outputs, but this row's target is doctrine prose authored by hand across files, with no producer to sweep; the two approaches the row names stay the open ones. The shape lands in `guard-doctrine.md` §3 via M117, which is where it does fit. Lineage: M114 F1 → G1/G2/G3 → H1/H2/H3 → re-cut — added 2026-07-26 — M114 Out
- Dedicated `/explore-sources` skill for supply-push exploration: M103 lands the doctrine + `/milestone-plan` routing but no dedicated trigger; a standalone skill would make exploration discoverable to an agent not already in a planning session, at the cost of new surface near M56's rejected query-op / graph-tooling machinery. Swept: no row or D-entry covers an exploration skill; M56 rejected a formal query op and graph tooling, which this must not become. Promote if the rulebook + plan-step routing proves insufficiently discoverable in practice — added 2026-07-20 — M103 Out
- Anchor-choice doctrine for the mutation harness (RR03 rec 8, `consider`): harness anchors are exemplar blocks chosen "partly for matchability, not because each anchored sentence is doctrine", so an editorial pass pays a re-anchoring tax growing with the guard count. Home is `guard-doctrine.md` §1; one paragraph — anchor on rules, never scaffolding. **Trigger evaluated NEGATIVE 2026-07-20 (M95):** twelve removals cost one guard assert and one harness entry, and D-056(3) removes the freeze mechanism RR03 feared by making guard-pinning a deletion screen rather than a keep. Kept parked, not pruned: one pass is one data point, and RR04 notes 111 of 278 mutation blocks anchor in the rulebook, so any future relocation hits the trigger. Swept: no row or D-entry owns anchor choice. Promote if a LATER pass finds re-anchoring dominating its cost — added 2026-07-19 — RR03 rec 8
- Soft offset norm for rulebook growth (RR02 rec 5, `consider`): one rulebook sentence — *a milestone adding rulebook lines names the lines it removes, or logs why none* — as standing outflow pressure against the measured +7.6 lines/milestone inflow. RR02 rejected the same idea as a HARD gate (some milestones legitimately add with nothing to offset — M92's retirement rule had no predecessor) but recommends the soft form at ~3 lines' cost. Deliberately not planned now: RR02 says drop it if M96's ratchet alone proves sufficient, so it waits on evidence from a few M96 cycles. Swept: no row or D-entry covers an outflow norm for the rulebook; D-051 is the LESSONS.md analogue and is the model. Promote if the ratchet fires repeatedly without changing authoring behaviour — added 2026-07-19 — M96 Out
- Repair resumability for a declined scaffold migration: M82's §3 adds the successor `.gitignore` entry as its first action, but that is exactly what silences `check_gitignore_deprecations` (`cairn_validate.py:662` fires only when the old entry is present AND the new one absent) — so a declined move, or a "keep both and skip" on the both-present case, leaves the old directory on disk with no later repair run or `/milestone` audit ever surfacing it again, and `SKILL.md:218-220` tells the agent that silence is expected; the fix drives the step from the superseded-entry map plus a filesystem check, which contradicts M82's AC3 and Scope ("driven by the advisory's own output") and so needs a plan-time amendment, not a repair patch; bounded today — `DEPRECATED_GITIGNORE` holds one pair and this repo is already migrated; swept: no existing row covers repair resumability — promote when a second repo adopts the new shelf, or fold into the next `/cairn-init` repair milestone — added 2026-07-18 — M82 review F1 (85)
- Citekey resolution + dependent discovery: `citekey (p. N)` is mandated (`validation-doctrine.md:79`) but never parsed in either direction — a citekey naming no page and a page nothing cites both pass — and `cairn_impact.py:45` excludes `references/`, so the one record type designated "current knowledge, corrected in place" (`tracking-rules.md:137-139`) is the one with no impact tooling; blocked on a standing rejection — M56 rejected "a formal query op" and "graph tooling" (`archive/M56-llm-wiki-investigation.md:17-19`), which a superseding D-entry must narrow before this is planned; promote if restatement drift produces a live wrong value (intraclass's Koo-bands and unbacked-Cicchetti chains are the candidates) — added 2026-07-18 — M79 Out
- Concurrent-cairn-operator hardening: if two people ever run cairn in the same repo at once the tracking files race — milestone IDs and D-numbers are model-picked with no allocator, duplicate D-numbers auto-merge and validate GREEN (`cairn_validate.py:500-506` uses a `set`), `/milestone-plan` commits to the default branch with no fetch/pull at any step, and `check_single_in_progress` (`cairn_validate.py:61-64`) is a hard FAIL two operators trip by construction; cheapest standalone piece is a `check_d_uniqueness`; swept: no existing row or D-entry covers it — promote if a second cairn operator actually appears — added 2026-07-18 — M72 Out (D-043)
- BC-aware coverage message: a binding criterion ingested as `- [ ] AC-N (BCn):` with no Coverage line reports as a plain "ACn not referenced". M107 prescribes the form + a pinning test, so the check already enforces the mapping — this is message clarity only. Swept: no row/D-entry; extends M34's coverage check. Promote if a numbered-BC failure confuses an operator in practice — added 2026-07-21 — M107 Out (Q1)
- Content-gated memory guard: make M19's memory-boundary hook inspect the write and fire only on durable-state signals (decisions, conventions, project facts), staying silent on pure per-user prefs; promote only if the unconditional soft nudge proves too noisy — added 2026-07-11 — M19 Out
- Contributor-facing scaffold: cairn ships nothing a repo can hand an outsider — no CONTRIBUTING, PR template, or issue template (`git ls-files` matches zero; there is no `.github/` in this repo), so a contributor learns the branch/commit/tracking conventions only by being told; would be a `/cairn-init` opt-in, not a core scaffold addition — promote once M72's README subsection is in use and proves insufficient — added 2026-07-18 — M72 Out
- Branch-protection compatibility: cairn structurally requires direct pushes to the default branch (`tracking-rules.md:248-252` docs-only commits; `/milestone-review`'s post-merge hygiene pass), which a repo protecting its default branch behind required PRs cannot do — needs a decision on whether hygiene commits get their own PR or the rule gains a protected-branch mode — promote if an adopting repo turns on branch protection — added 2026-07-18 — M72 Out
- Scaffold-spec version stamp / content-drift detection (Direction 2, deferred from M24): M24 detects *missing* §1 pieces but not a piece whose template *body* changed while the file still exists; stamp a scaffold-spec version into the adopted CLAUDE.md and compare against the plugin's current spec to catch content drift — needs a maintained spec version + changelog + a definition of "what counts as a bump"; promote only if content drift (as opposed to missing files) actually bites — added 2026-07-12 — M24 Out
