# Roadmap

_The only authority on milestone status. Grouped by status, not ID._
_Last hygiene check: 2026-08-14 (M139 post-merge — archive written, M134 terminal row pruned, child-of-M139 candidate row added, RB12/RR12 archived; validate all-green.)_

Note: this repo dogfoods the tracking file formats by hand; it is a plugin,
not an R package, so R-specific gates don't apply.

## Milestones

_Released 1.0.0 2026-07-16 (tag v1.0.0) · 1.1.0 2026-07-19 (tag v1.1.0) · 1.1.1 2026-07-19 (tag v1.1.1) · 1.2.0 2026-07-23 (tag v1.2.0) · 1.3.0 2026-07-31 (tag v1.3.0) · 1.4.0 2026-08-05 (tag v1.4.0)._

| ID | Title | Status | Depends on | Priority | File/Archive |
|---|---|---|---|---|---|
| M140 | Whole-slice equality guards for the M139 rules | review | — | high | milestones/M140-equality-guards.md |
| M139 | The narrowing repair for a defeated promise is reachable at a review return | done | — | high | milestones/archive/M139-narrowing-at-the-return.md |
| M138 | The criteria audit reaches amended wording and one-exemplar verification clauses | done | — | high | milestones/archive/M138-criteria-audit-reach.md |
| M137 | Derived figures are pinned or procedural, never free-standing | done | — | high | milestones/archive/M137-derived-figures.md |
| M136 | An observed failure backs a claim only as the failure it is verified to be | done | — | high | milestones/archive/M136-failure-identity.md |
| M135 | Legacy-id tolerance for check_dangling_ids | done | — | normal | milestones/archive/M135-legacy-id-tolerance.md |

## Candidates

_Ordered higher-priority-first (advisory only — candidates carry no Priority field). Triage: D-027._

- README cleanup: clarify and streamline `README.md`, currently riddled with em dashes and other AI writing tropes — a plain-language, docs-only style pass. Swept 2026-08-14: no row, archive entry or D-entry covers README prose quality; the contributor-facing scaffold row (M72 Out) references the README's existence, not its style. Promote at will as a small docs pass — added 2026-08-14 — conversational (maintainer)
- The amendment-time audit surface records nothing: M138's step-6 re-audit mandates no work-log line — unlike plan step 3 and brief ingestion, whose record clause D-079 clause 2 owns — so whether the fresh reader ran is unrecoverable on resume and the per-criterion one-re-entry bound is uncountable (M138 review F1/74); the ingest-clearance exemption keys on a work-log line with no per-criterion shape, so "already covers" is decided by recollection (F8/68). Cheapest form: one step-6 sentence recording the re-audit on plan step 3's terms, plus a fixed clearance shape at the brief surface. Swept 2026-08-09: no row, archive entry or D-entry covers the third surface's record; D-079 clause 2 owns the two existing surfaces only. Promote when a resume cannot determine whether a re-audit ran, or an exemption is applied to wording no line names — the falsifier of "the session always knows its own history" — never on a count of amendments — added 2026-08-09 — M138 review F1/F8 (sub-threshold, logged)
- Re-measure the record-defect share after the derived-claims/failure-identity/derived-figures family (M134/M136/M137): re-run the 2026-08-08 effort audit's classification — re-read the actioned ≥80 findings and defect/amendment returns in `milestones/archive/` `## Review` sections for post-M137 milestones, per the method in `references/effort-experiment-notes.md` — and compare against the M113–M136 baseline the absorbed derived-figures candidate row recorded at `08bbb07` (roughly half record defects; M135's two forced a gated amendment). Three outcomes it discriminates, with their exits already on record: helping (stale-figure share drops — the family stands), dead weight (baseline rate persists — D-099 is the entry to supersede, per its own Consequences), self-thrash (actioned findings shift to pin-compliance ceremony rather than real defects — M137's F19/F27 are the shape to watch; the declined validator stays behind D-090's trigger either way). Attribution is to the family jointly, never M137 alone — the three rules shipped within days. Swept 2026-08-08: no row, archive entry or D-entry covers post-rule re-measurement; the standing-instrument row governs how gating instruments are introduced, not measurement of conduct rules; the effort page owns the method, not the task. Promote at the next audit-style re-measurement, or earlier if a defect return or gated amendment is again caused by a record defect — the falsifier of "the family is working" — never on felt cost — added 2026-08-08 — M137 close-out (user request)
- Standing-instrument adoption discipline — shadow pilot at adoption, and a fired falsifier executes rather than re-arms: two conduct clauses at the plan gate, no mechanism. (a) A milestone proposing a standing instrument (especially one that gates a status transition) states at its plan gate a pre-registered yield classification (what counts as a real finding, by kind), a shadow period (advisory-only for its first 2–3 milestones), and an autopsy date joining `cairn_cost` to classified yield before the instrument may gate anything. (b) When an adopted instrument's own falsifier fires, at most one narrowing attempt is permitted before the prescribed exit executes — replacing the falsifier is presumptive tuning (D-059). Motivated by §8's arc: D-067 adopted a gating instrument on n=1 evidence straight into a gating position; its exit fired at M119 (4.5-round average) and was re-armed five times (D-079→D-080→D-083→D-085→D-091) before M127 retired the step at user mandate, while the yield-by-kind classification that settled it was derivable from on-disk records by ~M117 (M127 post-mortem, 2026-07-31). Sits beside D-090's door, not behind it: D-090 governs what apparatus may be *proposed*; this governs how an approved one is *introduced*, and it is three plan-gate questions, not machinery. Swept 2026-07-31: no row, archive entry or D-entry covers adoption piloting or falsifier-refire; D-059/D-067 own retire-don't-tune, which clause (b) strengthens. Promote when the next milestone proposes a standing instrument — the rule binds at adoption time — never on a count of milestones — added 2026-07-31 — M127 post-mortem (user question)
- Frame membership rule vs the frame's actual contents: `tracking-rules.md`'s "Always-read governance" defines an always-read file as "one this repo re-reads at the start of most sessions", but the table's own third row is `tracking-rules.md`, read only when a cairn skill fires — not at session start — while `PROFILE.md`'s slots are read that same conditional way and carry no row (both recorded by M126's AC1 sweep). D-060 enumerated "GP1's four always-read files" without ever defining membership, so the set is a list rather than a rule, and the audit's "any newly-added always-read surface not covered" check has no test to apply. Swept 2026-07-31: no row, archive entry or D-entry covers frame membership; D-060 founded the frame and D-063 added a surface, neither defining the term. Promote when a surface arrives that the sweep cannot classify — the falsifier of "the list is good enough" — never on a count of surfaces — added 2026-07-31 — M126 Out
- Batch history-record corrections into one superseding entry per milestone: 6 of the 11 D-entries D-083–D-093 are per-claim corrections of other entries' measurement claims (D-084→D-086, D-091→D-092→D-093), each produced by a certification round auditing the record a previous round wrote (M127's plan sweep, 2026-07-31). M127 removes the generator — multi-round certification — so no rule is added now (M127 plan gate Q2); this row is the fallback if the cascade survives the rebuild. A reachable form must handle the review-return case: a defect found in the batching entry after it lands can only take a further superseding entry under IP4, so "one per milestone" would bind at authoring time, not absolutely. Swept 2026-07-31: no row, archive entry or D-entry covers correction batching; D-045/IP4 own the supersede discipline this would refine. Promote if two or more correcting D-entries land in a single milestone after M127 ships — the falsifier of "the generator is dead" — never on a count of entries overall — added 2026-07-31 — M127 plan gate (Q2)
- Roll the section-consistency ledger out beyond its retired first subject: M124 shipped the helper section-general (a file path and a heading) with one committed ledger, for guard-doctrine §8 alone; M127 retired §8 and deleted the machinery whole (D-095), so the mechanism's home is now git history — last present on main at `ba1e6bd` (`git show ba1e6bd:skills/tests/section_ledger.py`), restorable if a live subject appears. Remaining surfaces in descending exposure: guard-doctrine §§1–7 and 9, `tracking-rules.md`'s always-read sections, and the `records-hygiene.md` / `validation-doctrine.md` modules. Swept 2026-07-30: no row, archive entry or D-entry covers a rollout; D-065's parked one-surface-pin row is the cross-file class and stays separate. Promote when a consistency defeat — a contradicting sentence, or a rename reusing no word of the term — is found in a live doctrine section, never on a count of sections — added 2026-07-30, restated by M127 with git as the mechanism's home — M124 Out
- Rulebook says a decision entry "is never one line" while D-077 records the counterexample: `tracking-rules.md:126` explains why the `decisions format` advisory does not police entry length — "because a decision entry carries its rationale and is never one line (D-075)" — but D-077 (2026-07-27, shipped by M119 itself) measures the corpus at 23 of 24 entries wrapping and names the exception verbatim, M84's `- 2026-07-18: review — 3 lenses (…)`. The operative reasoning survives the exception (D-077 calls it "an entry of the work log's own genre sitting in a decisions section"), so this is an overclaiming absolute, not a wrong rule — the M114 lesson's shape, where a characterization beside a correct measurement is the falsifiable surface. Found at M119's review as F4 (scored 80) and deliberately not fixed there: the sentence is mutation-pinned, and re-anchoring a reflowed rulebook line is the M104 trap that fed M119's nine-round §8 loop, which the review-entry override existed to stop. Swept 2026-07-27: no row, no archive entry, and no D-entry beyond D-075/D-077 covers the one-line grammar. Promote when `tracking-rules.md` is next edited near that bullet for another reason — the re-anchoring cost is then already being paid — never as a standalone pass — added 2026-07-27 — M119 review F4 (80)
- Reasoning-effort dial per spawned agent: Anthropic's Opus 5 guide names effort (low/medium/high/xhigh) as the primary control for token cost and latency, and reports that review accuracy holds at lower settings — but cairn's spawn vocabulary is model tier alone (`tracking-rules.md` "Model and agent strategy"; verified 2026-07-27: the string `effort` occurs twice in the corpus, both meaning "best-effort"). Blocked mechanically, not by preference: the Agent tool exposes `model` and no effort parameter, effort comes from an agent definition's frontmatter, and cairn ships no agent definitions at all (no `agents/` anywhere in the repo, verified 2026-07-27) — so adopting this means cairn starts shipping agent definitions, new surface adjacent to what D-026 dropped as off-model ("tiered tool exposure presumes per-tier tool gating"; D-026 is about tool gating, not effort, so it is adjacent rather than a standing rejection). Swept: no row or D-entry covers effort or agent definitions. Promote when cairn ships an agent definition for any other reason, or when the Agent tool exposes effort per spawn — never on a count of expensive sessions — added 2026-07-27 — M120 Out
- Numeric cap on subagent spawning: the corpus has no cap, count limit, or budget on spawning anywhere (verified 2026-07-27); three of eleven spawn sites are open-ended, and a lifecycle's floor is 5 subagents with no ceiling. The Opus 5 guide recommends deterministic caps for cost-sensitive workloads. Blocked by a standing rejection that must be superseded first, not ignored: `milestone/SKILL.md:79-84` reports the spawn count and then forbids acting on it — "never treat a large figure as a finding to act on, and never propose a cap from it. No governing mechanism over these numbers exists or is owed: D-057 closed the stock-side size-governance program" — which is guard-pinned by `test_cost_audit_line.py:45-55`. M120 ships delegation *guidance*, which that prose does not touch; a cap needs the superseding entry. Swept: no row or D-entry covers a spawn cap; D-057 is the stock-side closure it rests on. Promote when `cairn_cost` shows a measured regression attributable to spawn volume — D-057's own re-open trigger, and never a felt-cost one — added 2026-07-27 — M120 Out
- Redistribute the milestone template's drafting budgets now that `## Decisions` has stopped counting: the preamble **used to reserve ≥21 lines** for that section on the stated ground that it was "still counted (D-030/D-046)" — a ground D-074 removed and M118 struck from the template, so the freed lines now sit as undirected headroom rather than as budget for the five plan-owned sections the block does size (Goal 7 · Scope 26 · AC 28 · Coverage 11 · Tasks 25, each a measured p75 over 99 files). The block's stale "p90 21 / max 35" figure for the section went with it, unreplaced (corrected here 2026-07-27, M118: the row described the pre-M118 template). Deliberately deferred at M118's gate — the block has drifted twice under full attention (M99's fixed-point lesson) and a re-issue is a measurement pass of its own, so M118 corrects the stale ground and spends none of the freed space. Swept: no row or D-entry covers the drafting budgets; D-049/D-058 govern density thresholds, a different axis, and M99 authored the block. Promote when a plan draft is compressed against a per-section budget while the file sits under the cap with the freed headroom unspent — never on a count of milestones — added 2026-07-27 — M118 Out
- Disposition rule for sub-threshold findings about self-shipped doctrine (RR06 rec 6, `consider`, banked outside M114 per its BC8): a finding scoring under 80 whose subject is doctrine the milestone itself ships is resolved at the open gate or declined with a recorded reason, never merely logged. Two sentences in `/milestone-review`'s scoring step. Motivated by four under-scored predictive findings in M114 that later fired — F4 (60) predicted a trigger collision that occurred at pass 3; J5 (35) predicted a miscount that occurred at pass 5; RR06 adds F3 and G6. RR06 REJECTED changing the rubric or threshold (n=4 with selection bias, and every >=80 finding was real), so this is a disposition change only. Swept: no row or D-entry covers finding disposition; the scoring step is D-064-adjacent but untouched by it. Promote if a further under-scored finding about self-shipped doctrine fires after being logged — never on a count of passes. Lineage: RR05 B3 → RR06 Q5 → rec 6 → BC8 — added 2026-07-26 — M114 Out, transcribed to `main` 2026-07-26 at M115's plan gate because M114's copy exists only on its parked branch (M115 Out)
- Partial-pin asserts in `skills/tests/test_lesson_graduation.py`: three `assertIn` calls each pin only a fragment of a longer shipped sentence, so the rest of that sentence deletes green — `:45` "Read this whenever authoring or editing a test that locks prose" (the sentence runs on through the fixture, matcher and `cairn_validate` cases), `:51` "a module of `tracking-rules.md`" (runs on through D-031's reason and the conditional-read clause), and `:125` "An exclusion list may name only history files" (runs on into the operative never-a-live-directory remedy). Verified against the shipped bytes 2026-07-26 rather than inferred, and the check corrected what a first pass asserted: `:45` and `:125` are strict prefixes of their sentences, while `:51`'s literal starts six characters in (`It is ` precedes it) and is a mid-sentence fragment — the class is a partial pin, not specifically a lead-in. All three arrived with M98 (`9b8d7ff`, present on `main`); M114's own fourth instance of the class was widened at pass 8 instead of banked here, so this row is M98's residue only. RR07 §BC3 closed one instance of this class by hand; §BC2 is not a second, since it made `test_sweep_section_states_the_silent_cell_rule` gain a first assert over prose pinned by nothing, which is a different defect. One hand-closure plus M114's own at pass 8 is why the row is about the class and not the instances. Swept: no row, archive entry or D-entry covers assert truncation; the parked anchor-choice row (RR03 rec 8) shares `guard-doctrine.md` as a home but is about which block to anchor on, not how much of it an assert pins. Promote when a systematic remedy is identified — a harness check that a registered block is a whole sentence in its target, or a probe that deletes each pinned block's continuation — never after N further hand-widenings. **Remedy 1 evaluated NEGATIVE 2026-07-27 (M116 plan):** classifying all 412 `REGISTRY` blocks against their targets found 22 whole sentences vs 148 prefix-only, 31 suffix-only and 211 mid-fragment — a whole-sentence harness check reds 390/412, most of them fragments by design (`## Coverage`; D-042's per-clause entries), so it is not a systematic remedy. Remedy 2 and the drop condition stay untested: the pass classified blocks, it did not delete continuations. Drop if a mutation pass over the whole registry finds no registered block whose continuation deletes green. — added 2026-07-26 — M114 pass 8
- One-surface pin for a doctrine rule — a guard asserting a rule's phrase occurs in exactly one file, so a restatement elsewhere reds instead of silently forking it (M112's motivation). **Re-cut out of M114 at its third return (2026-07-26)**, having consumed all three returns across six findings of one shape: the detector, or the controls exercising it, did not cover a rendering the phrase can take — a line wrap, a blockquote continuation marker, nested blockquotes in both `>>` and `> >` spellings, emphasis applied to part of the phrase, case, word boundaries, and finally corpora with no non-vacuity assert, so the whole detector deleted green. The standing problem: phrase-search requires the searcher to enumerate every rendering, and enumeration by the detector's own author is exactly what guard-doctrine §3 says cannot be trusted. Swept: no row, archive entry or D-entry covers fork detection; the parked anchor-choice row (RR03 rec 8) shares `guard-doctrine.md` as a home but is about anchor choice, not detection. Promote when a rendering-independent approach is identified — a markdown/AST parse, or a content hash over normalized doctrine blocks — never after N further attempts at a wider matcher, per the falsifying-promotion-condition rule M114 ships. **Noted 2026-08-14 (RR12 B2):** the normalized whole-slice equality fixture adopted for M139's child (D-103) is in-home-file evidence the row's content-over-normalized-blocks approach works; no promotion — this row's target is cross-file fork detection, a different application, and its promotion additionally sits behind D-090's trigger. **Producer-sweep approach evaluated NEGATIVE 2026-07-27 (M117 plan):** intraclass M93's pass-9 fix — sweep the producer's own outputs and assert an invariant over them, enumerating nothing — is rendering-independent for a *function's* outputs, but this row's target is doctrine prose authored by hand across files, with no producer to sweep; the two approaches the row names stay the open ones. The shape lands in `guard-doctrine.md` §3 via M117, which is where it does fit. Lineage: M114 F1 → G1/G2/G3 → H1/H2/H3 → re-cut — added 2026-07-26 — M114 Out
- Dedicated `/explore-sources` skill for supply-push exploration: M103 lands the doctrine + `/milestone-plan` routing but no dedicated trigger; a standalone skill would make exploration discoverable to an agent not already in a planning session, at the cost of new surface near M56's rejected query-op / graph-tooling machinery. Swept: no row or D-entry covers an exploration skill; M56 rejected a formal query op and graph tooling, which this must not become. Promote if the rulebook + plan-step routing proves insufficiently discoverable in practice — added 2026-07-20 — M103 Out
- Anchor-choice doctrine for the mutation harness (RR03 rec 8, `consider`): harness anchors are exemplar blocks chosen "partly for matchability, not because each anchored sentence is doctrine", so an editorial pass pays a re-anchoring tax growing with the guard count. Home is `guard-doctrine.md` §1; one paragraph — anchor on rules, never scaffolding. **Trigger evaluated NEGATIVE 2026-07-20 (M95):** twelve removals cost one guard assert and one harness entry, and D-056(3) removes the freeze mechanism RR03 feared by making guard-pinning a deletion screen rather than a keep. Kept parked, not pruned: one pass is one data point, and RR04 notes 111 of 278 mutation blocks anchor in the rulebook, so any future relocation hits the trigger. Swept: no row or D-entry owns anchor choice. Promote if a LATER pass finds re-anchoring dominating its cost — added 2026-07-19 — RR03 rec 8
- Soft offset norm for rulebook growth (RR02 rec 5, `consider`): one rulebook sentence — *a milestone adding rulebook lines names the lines it removes, or logs why none* — as standing outflow pressure against the measured +7.6 lines/milestone inflow. RR02 rejected the same idea as a HARD gate (some milestones legitimately add with nothing to offset — M92's retirement rule had no predecessor) but recommends the soft form at ~3 lines' cost. Deliberately not planned now: RR02 says drop it if M96's ratchet alone proves sufficient, so it waits on evidence from a few M96 cycles. Swept: no row or D-entry covers an outflow norm for the rulebook; D-051 is the LESSONS.md analogue and is the model. Promote if the ratchet fires repeatedly without changing authoring behaviour — added 2026-07-19 — M96 Out
- Citekey resolution + dependent discovery: `citekey (p. N)` is mandated (`validation-doctrine.md:79`) but never parsed in either direction — a citekey naming no page and a page nothing cites both pass — and `cairn_impact.py:45` excludes `references/`, so the one record type designated "current knowledge, corrected in place" (`tracking-rules.md:137-139`) is the one with no impact tooling; blocked on a standing rejection — M56 rejected "a formal query op" and "graph tooling" (`archive/M56-llm-wiki-investigation.md:17-19`), which a superseding D-entry must narrow before this is planned; promote if restatement drift produces a live wrong value (intraclass's Koo-bands and unbacked-Cicchetti chains are the candidates) — added 2026-07-18 — M79 Out
- Concurrent-cairn-operator hardening: if two people ever run cairn in the same repo at once the tracking files race — milestone IDs and D-numbers are model-picked with no allocator, duplicate D-numbers auto-merge and validate GREEN (`cairn_validate.py:500-506` uses a `set`), `/milestone-plan` commits to the default branch with no fetch/pull at any step, and `check_single_in_progress` (`cairn_validate.py:61-64`) is a hard FAIL two operators trip by construction; cheapest standalone piece is a `check_d_uniqueness`; swept: no existing row or D-entry covers it — promote if a second cairn operator actually appears — added 2026-07-18 — M72 Out (D-043)
- BC-aware coverage message: a binding criterion ingested as `- [ ] AC-N (BCn):` with no Coverage line reports as a plain "ACn not referenced". M107 prescribes the form + a pinning test, so the check already enforces the mapping — this is message clarity only. Swept: no row/D-entry; extends M34's coverage check. Promote if a numbered-BC failure confuses an operator in practice — added 2026-07-21 — M107 Out (Q1)
- Content-gated memory guard: make M19's memory-boundary hook inspect the write and fire only on durable-state signals (decisions, conventions, project facts), staying silent on pure per-user prefs; promote only if the unconditional soft nudge proves too noisy — added 2026-07-11 — M19 Out
- Contributor-facing scaffold: cairn ships nothing a repo can hand an outsider — no CONTRIBUTING, PR template, or issue template (`git ls-files` matches zero; there is no `.github/` in this repo), so a contributor learns the branch/commit/tracking conventions only by being told; would be a `/cairn-init` opt-in, not a core scaffold addition — promote once M72's README subsection is in use and proves insufficient — added 2026-07-18 — M72 Out
- Branch-protection compatibility: cairn structurally requires direct pushes to the default branch (`tracking-rules.md:248-252` docs-only commits; `/milestone-review`'s post-merge hygiene pass), which a repo protecting its default branch behind required PRs cannot do — needs a decision on whether hygiene commits get their own PR or the rule gains a protected-branch mode — promote if an adopting repo turns on branch protection — added 2026-07-18 — M72 Out
- Scaffold-spec version stamp / content-drift detection (Direction 2, deferred from M24): M24 detects *missing* §1 pieces but not a piece whose template *body* changed while the file still exists; stamp a scaffold-spec version into the adopted CLAUDE.md and compare against the plugin's current spec to catch content drift — needs a maintained spec version + changelog + a definition of "what counts as a bump"; promote only if content drift (as opposed to missing files) actually bites; M133 (references/impeccable.md) adds the stamp's second job: distinguishing a deliberately short record from one written before a section existed, so no interview is re-proposed — added 2026-07-12 — M24 Out
- Phase-gated loading of implement-time doctrine: impeccable loads its quality floor "immediately before editing UI" and never for planning-only work (its SKILL.md step 3), while cairn's skills read the whole always-read rulebook at fire time regardless of phase; the conditional modules (D-031) already read guard/records/validation doctrine at their moments, so the delta is whether any always-read rulebook section is genuinely implement-only and could move behind a phase moment. Swept 2026-08-04: no row, archive entry or D-entry covers phase-scoped rulebook reads — D-031 governs module packaging, the soft-offset-norm row governs growth, D-057/D-090 close the size and apparatus programs, so a move needs an ordinary-work trigger. Measured 2026-08-04 (plan-gate check of this trigger): the rulebook's large sections are all cross-phase; the single-phase-only mass is ≈2K tokens of the ~17K-token rulebook, ~1.5% of ~139K/turn cache-read context — judged not fired. Promote when a measured `cairn_cost` reading attributes a material share of session tokens to rulebook sections the session's phase never uses — never on a count of lines — added 2026-08-04 — M133 (references/impeccable.md)
- Action-graded finding vocabulary: impeccable's doctor grades each drift finding by the action it requires (auto = apply without asking, mention = inform, route = name the owning command — "the severity says what should happen, not how bad it is") and keeps tool-version, schema, and truth drift apart, where cairn's CHECK-FAIL / advisory-WARN vocabulary grades severity and leaves the required action to the reader. Swept 2026-08-04: no row, archive entry or D-entry covers finding-disposition vocabulary — D-046/D-075 set warn-never-fail tiers and D-059 retired one advisory, none name actions; adjacent to the standing-instrument row (adoption conduct), not covered by it. Promote when an operator acts wrongly on a validate or audit finding because its tier did not say what to do — a shipped-behavior defect, D-090's required trigger — never on a count of advisories — added 2026-08-04 — M133 (references/impeccable.md)
- Streamlining pass over existing code, possibly on Fable: a skill that audits already-shipped code — not the working diff — for length, directness, and efficiency, on the maintainer's hypothesis (stated as a hypothesis, 2026-08-05) that Fable can compress what Opus wrote. Two things must be settled before it is a skill. (1) Prior art: Claude Code ships a built-in `/simplify` that reviews the *changed* code for reuse, simplification, efficiency and altitude and applies the fixes (quality only, no bug hunt) — cairn ships no `simplify` of its own (`grep` over `skills/`, 2026-08-05), so the only unserved surface is whole-corpus audit of code no current branch touches, and the row must not restate the built-in. (2) The Fable half already has a door: D-004/D-062 route Fable *only* through the RB/RR brief protocol behind a per-instance approval gate, so a streamlining pass can be run today as a Review Brief via `/milestone-brief` with no new skill — that is the do-nothing alternative, and it is also the cheap way to test the hypothesis before building anything. Swept 2026-08-05: no row, archive entry or D-entry covers code simplification, refactoring, or a streamlining pass (`grep` over ROADMAP, `milestones/archive/`, `DECISIONS.md`, LESSONS). Adjacent, not covering: the `/explore-sources` row is the precedent for a new-skill candidate near rejected machinery; D-090 closes the door on *verification* apparatus, which this is not (it changes shipped code, not the checking of it), so D-090's trigger does not bind here; D-062 governs the model-tier question. Promote when an RB-run streamlining pass over existing cairn code returns changes the maintainer accepts that neither the review lenses nor the built-in `/simplify` would have surfaced — evidence the current path misses this class — never on a count of files that look long — added 2026-08-05 — conversational (maintainer). **Variant absorbed 2026-08-05 (maintainer): a mandated `/simplify` run at the end of `/milestone-implement`** — the complement of this row (working diff, built-in, at a flow gate rather than existing code, Fable, on demand). Not planned, on two grounds checked that day: (a) cairn references no harness built-in anywhere in `skills/` (grep, zero hits), so a mandated step would be its first hard dependency on a surface it neither ships nor versions, and an adopter whose harness lacks `/simplify` gets a step that silently no-ops; (b) D-090 closes the door on "a new certification or audit step" absent a shipped-behavior defect as trigger, and a quality pass at the completion checkpoint is one. Same promotion condition as this row's.
- Deferred second tier for hook nudges: impeccable's detector interrupts an edit only for mechanical, unambiguous findings and defers the taste tier to one deduplicated Stop-event pass that stays silent when clean, where cairn's memory_guard/idea_guard nudges fire unconditionally per event; the content-gated memory guard row (M19 Out) filters what fires, this defers when — two remedies for the same noise, cross-referenced not merged. Promote when the unconditional nudge is observed drowning a session — the same trigger the content-gating row states — never on a count of firings. Swept 2026-08-04: no row, archive entry or D-entry covers hook-finding timing; cairn's only Stop-event registration is stop_guard.py (stop points are commit points — corrected M133 review, the row first claimed the slot was empty), so a deferred detector tier would share or extend that hook rather than claim a free slot — added 2026-08-04 — M133 (references/impeccable.md)
