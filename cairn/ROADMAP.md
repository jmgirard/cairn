# Roadmap

_The only authority on milestone status. Grouped by status, not ID._
_Last hygiene check: 2026-07-11 (M02 done)_

Note: this repo dogfoods the tracking file formats by hand; it is a plugin,
not an R package, so R-specific gates don't apply.

## Milestones

| ID | Title | Status | Depends on | Priority | File/Archive |
|---|---|---|---|---|---|
| M03 | Pilot: migrate tidymedia | in-progress | M01 | high | milestones/M03-pilot-migrate-tidymedia.md |
| M02 | Pilot: fresh adoption in one package repo | done | M01 | high | milestones/archive/M02-pilot-fresh.md |
| M01 | Build plugin v0.1 from DRAFT_2 spec | done | — | high | milestones/archive/M01-build-v0-1.md |

## Candidates

- Stress-test migration on a Lineage B repo (ackwards or circumplex) — added 2026-07-11 — DRAFT_2 §11
- Plugin hooks for immediate guardrail feedback (e.g., README.Rmd edit reminder) — added 2026-07-11 — DRAFT_2 §9.2
- Marketplace one-command install: marketplace.json shipped 2026-07-11 (validates); remaining: document install paths in README (incl. Desktop Customize → Plugins) + contrast dev install (skills-dir symlink: live with checkout, no update step, branch-checkout footgun) vs marketplace snapshot (frozen copy, manual re-fetch per release) — dual-install ambiguity bit the pilot 2026-07-11 — added 2026-07-11 — DRAFT_2 §2.3
- Public release prep: LICENSE (MIT), README worked example, remove DRAFT files, tag v1.0 — added 2026-07-11 — DRAFT_2 §11
- Design-interview skill: one interview, two phases (facts → principles) with banked proto-principles and a chip-gated seam; proposed gold standard — encode question-quality + reconciliation findings so interviews improve on Opus; complement: elevate phase 2 to Fable behind a gate — added 2026-07-11 — references/design-interview-notes.md
- DESIGN principle ordering: specify IP block first then GP, numeric within type, numbers never reused/renumbered (retire via D-entry) — in cairn-init skeleton + rulebook — added 2026-07-11 — M02 pilot (openac DESIGN interview)
- Skill-less routing guardrails: rulebook only loads when a skill fires, so plain conversation can bypass tiers/git model; make claude-md-section an imperative classify-first router ("not trivial → invoke the skill; unsure → /milestone; never implement on main"), pair with guardrail hooks (existing candidate); test empirically in openac first; also the sole delivery path for rulebook conduct (incl. contextual-chip principle) when milestone talk starts as plain conversation — persistent memory can't serve other cairn users — added 2026-07-11 — M02 pilot
- Toolchain profiles (generalize beyond R): core is ~80% language-agnostic (this repo runs it sans R); concentrate R-ness into a profile with 6 slots (verify commands, consistency gate, test doctrine, release walk, init detection, greenfield opener questions), chosen at init, recorded in CLAUDE.md section; extraction method: diff what this repo waives vs what openac uses; keep domain doctrine (oracles) orthogonal to language profile; target v0.3 after pilots harden the R slots — added 2026-07-11 — M02 pilot
- Greenfield init flow (builds on toolchain profiles): when cairn-init runs in a new/empty repo, open with a project-type chip → selects the toolchain profile → profile supplies fixed opener questions (R profile: CRAN intent, compiled code Rcpp/RcppArmadillo, statistical calcs needing oracle verification; conventions like {cli} folded into one defaults question or deferred to first milestone-plan); each question carries a marked recommended option, each option states its consequence, every answer lands in a durable home (profile slots / DESIGN Conventions / test doctrine), "undecided" defaults to the reversible choice and is banked as a candidate row; cairn-init stays tracking-only — package skeleton is the obvious first milestone — added 2026-07-11 — toolchain-profiles candidate, references/design-interview-notes.md
- v0.2 polish from M01 independent review (nits): RR-ingestion protocol into rulebook or cited by path; DECISIONS + reviews/ check in /cairn-release and /hotfix session start; closing routing chip for /cairn-release; status-grouping note in cairn-init ROADMAP skeleton — added 2026-07-11 — M01 review
- Subagent title model prefix: skills that spawn subagents prefix the Agent description with [S]/[O]/[F] for Sonnet/Opus/Fable, since the background-tasks pane shows only the title ("Agent"), not the model; spawning skills always know the tier (rulebook model-tiering section), so encode it at spawn time — added 2026-07-11 — background-tasks pane UX
- Contextual chip construction as rulebook principle: openac pilot validated /milestone's state-conditional chip (offered to fix the specific issue the audit found — better than a-priori fixed options); /milestone's "single most sensible next action, e.g." phrasing licenses this, other skills' hardcoded `Routing chip:` menus (cairn-init, milestone-brief) don't; elevate to tracking-rules chip section — options composed from session findings, fixed menus become examples; invariants stay fixed (recommended-first, ≤4 options, Stop present, never auto-proceed) — added 2026-07-11 — M02 pilot (openac health audit)
- Output discipline & stage orientation: stage-banner line on all skill output ([cairn · implement · M01 · task 3/7]); between-gate chat reports deltas not dumps; chip weight caps (evidence in chat above the chip, option text lean, ≤4 options); chip descriptions in plain language — each option states what's being chosen and why it matters, technical support lives in chat above the chip, not in the option text (2026-07-11 pilot friction: overly technical descriptions obscured the actual choice, openac + tidymedia); chapter markers at phase transitions where harness supports (Desktop floating TOC); phase-completion recaps lead with substance — what the milestone did/changed/accomplished in plain words — before hygiene mechanics (caps, hashes, archive paths), which follow or compress to one line (2026-07-11: M02's own done-recap was all mechanics, no accomplishment — flagged by Jeff on cairn itself) — added 2026-07-11 — M02 pilot (chip minimize exists in Desktop — discovered 2026-07-11 — so layout concern softened; side-by-side still app feedback, not cairn)
- RB self-solicitation: openac M03 shipped a plausibly RB-worthy design question without the skills ever offering escalation — the existing hook (implement step 7 "needs Fable-level judgment") is confidence-triggered and the session most needing review is least likely to flag itself; fix structurally: rulebook category tripwires (stats/scoring sans oracle, irreversible exported API, IP-touching) + plan-time tagging ("RB-worthy question: X") inherited by implement + escalation chip option at gates on tripwire hits only — never a standing option (billed-nag noise would dilute D-004 deliberateness) — added 2026-07-11 — M02 pilot (Jeff's deliberate no-mention experiment)
- Cross-repo milestone ID ambiguity: IDs are repo-local by design, so two cairn-tracked repos in one conversation makes bare M<NN> ambiguous (bit the pilot 2026-07-11: cairn M03 = tidymedia migration vs openac M03 = whisper reader); add a rulebook convention — qualify IDs with the repo name (openac M03) whenever more than one tracked repo is in scope — added 2026-07-11 — M02 pilot
- ROADMAP done-row retention gap: done rows accumulate forever but the <60-line cap's remedies only name pruning candidates; define a sanctioned remedy (e.g., prune oldest done rows past N — archive files + git stay authoritative for history) in tracking-rules + cairn-init skeleton — added 2026-07-11 — M02 pilot (M01 done-row question)
