# Lessons

Durable repo lessons — build quirks, testing tricks, gotchas worth
remembering next time — captured at milestone end and surfaced at plan time.
Not status, not decisions: a lesson is a reusable "how this repo actually
behaves" note. Cross-cutting *choices* still go to `DECISIONS.md`.

One line per lesson: `- YYYY-MM-DD (M<NN>): <lesson>`. Capped at 50 lines
(tracking-rules weight-caps) — when full, prune the stalest; git keeps the
record. Current knowledge: a lesson proven false is corrected in place (D-045).

<!-- lessons appended below by /milestone-review post-merge hygiene -->

- 2026-07-11 (M16): this repo has no CI — `gh pr checks --watch` returns "no checks reported" + exit 0; treat as no-CI and merge directly, never wait for green.
- 2026-07-12 (M24): adding a new check to `cairn_validate.CHECKS` that requires files also means extending the shared `Tree.build()` fixture in `scripts/tests/test_scripts.py` — every existing validate test reuses it, so a stricter check silently fails the whole suite until the fixture represents a valid repo for the new invariant.
- 2026-07-12 (M32): this repo has no `pytest` — the script suite is unittest; run `python3 -m unittest discover -s scripts/tests` (or `-k` a name via `python3 -m unittest scripts.tests.test_scripts -k <sub>`). Don't write `pytest …` into acceptance criteria; it fails "No module named pytest".
- 2026-07-12 (M35): when a milestone's scope was drawn from existing ROADMAP candidate rows, those rows are NOT pruned at plan time (candidates graduate at completion, not at plan) — graduate them in the post-merge hygiene pass or the ROADMAP advertises shipped work as pending; both review lenses flagged M35's two fulfilled rows.
- 2026-07-13 (M48): a "sweep for all mentions of X" task must `git grep` the whole repo, not just the file being edited — M48's T3 swept only tracking-rules and missed `cairn/DESIGN.md`'s two "Two profiles ship" mentions, and the work-log falsely claimed "only the one mention" (F1/93, F2/82). And when you ship a new instance of a registered kind (a third reference profile), extend the guard that iterates that kind: `test_shipped_reference_profiles_are_valid` (the only test running the *real* validator on shipped profiles) hardcoded `(r-package, generic)`, so the new profile went unexercised by the very schema its AC cited (F3/80). Both classes caught by the diff-bug + blame lenses, not the author.
- 2026-07-12 (M51): the archive summary has its own tight ≤25-line cap (separate from the 150-line milestone cap) — a decisions-heavy milestone won't fit a full Goal/Outcome/Decisions/Review skeleton, so budget ~2 wrapped lines per section. Trimming prose only reduces the count when it removes a whole *wrapped* physical line: a shorter sentence that still wraps to N lines saves nothing, so `wc -l` after each edit rather than eyeballing word count.
- 2026-07-13 (M53): the false-coverage guard trap (M39/M40) is mechanized — prose-guards register in the mutation harness (`skills/tests/test_mutation_harness.py`), which blanks each registered block and asserts its guard fails; a completeness meta-test fails CI on an unregistered guard *file*. Registration is per file (≥1 exemplar block), NOT per assertion — so a new `assertIn` in an already-registered file still needs its own `Mutation(...)` entry or the by-hand M47 check ("would this pass against the pre-milestone content?"). M23/M26 (single-line / inside-bold) still apply to *matchability*.
- 2026-07-13 (M54): `test_mutation_harness.py` does a bare `import mutation_engine`, so running it by dotted name (`python3 -m unittest skills.tests.test_mutation_harness`) dies ModuleNotFoundError — run the skills guards via `discover -s skills/tests` (extends the M32 no-pytest lesson to skills/tests). And a mutation-registered `block` must be a *positive* assertion's phrase: an `assertNotIn` (e.g. "for R packages" absent) can't be mutation-proven since blanking can't restore an absence, so pair each negative regression-guard with a positive framing assert that carries the `Mutation(...)` entry.
- 2026-07-16 (M56+M65, consolidated M78): Bash working directory persists across tool calls, and piping a verify through `| tail -1` makes the chain exit-blind (`tail` exits 0 even when `discover` ImportErrors) — a lingering `cd cairn` silently skipped all three suites, and at the M65 gate the pipe hid a red exit and the chain shipped it. Run suites from the repo root, check each exit code explicitly, and never chain a commit/push/marker write after a piped suite run.
- 2026-07-16 (M57, extended M79): a hard validate CHECK that parses human-authored markdown must tolerate cosmetic decoration on the semantic token — the INDEX-line regex double-FAILed correct backticked/`[name](name)` entries (F1/85); D-023's no-false-positive doctrine applies to CHECK parsers, not just scan heuristics. M79 found decoration is only ONE axis of freedom, and fixtures varying it alone pass **vacuously** on the others: PHRASING (requiring the literal word "from" failed a page phrased the way the template sanctions, "retrieved via <url>" — F4/90) and LAYOUT (a label alone on its line, and a decoy `## Provenance` heading above the real block — F3/92, F2/95). Vary every axis the prose is free in, not the one you thought of. And widening a capture class to admit a new form admits non-targets too: adding `/` for nested paths turned prose bullets into phantom catalog entries and let `..` escape the tree unnormalized, so an outside file silently satisfied an entry (F5/85).
- 2026-07-16 (M58): a "sweep repo-wide" task's exclusion list may name only history files (DECISIONS/CHANGELOG/legacy/reviews-archive), never a live directory — the T4 sweep excluded `cairn/references/` and two live citable pages kept present-tense claims that the doctrine lived in tracking-rules (review F1/85); extends the M48 whole-repo-grep rule.
- 2026-07-16 (M59+M62, consolidated M77): an AC whose evidence is a grep will hit the milestone's OWN artifacts — the guard's `assertNotIn` (an absence-assert is a hit for the token it locks) and the milestone file's own Scope/Tasks plus the ROADMAP lineage row on a removal/rename sweep; scope evidence commands to the prose surface being fixed and exempt the tracking lines in the criterion at plan time, else it needs a gated amendment at implement (both hit live).
- 2026-07-16 (M58+M59+M64+M65, consolidated M78): a mutation-registered `block` must sit on ONE physical line, occur exactly once in its target, and include its trailing punctuation — a reflow, a duplicate, or a period→semicolon edit all "found 0"-error the harness. It fires hardest on BRAND-NEW prose, because you author the sentence wrapped and register it afterwards: M78 hit it three separate times in one milestone despite this lesson existing. Author the anchor phrase on its own physical line deliberately, or pick an anchor already unique and unwrapped, and re-run the skills suite after any rewording near a registered block. Also: prose naming a validate finding must use the emitted label verbatim in backticks (`coverage complete`); write evidence counts from command output, never memory (M28).
- 2026-07-16 (M60): refines M19 — hook *registrations* (hooks.json) snapshot at process start, but each script BODY is re-read per invocation from the symlinked checkout: editing an already-registered hook goes live mid-session (the new merge_guard renamed the marker during M60's own merge), while a newly-registered hook stays dead until a fresh conversation; a fixture test still proves only what a hook prints, and live-fire plans must split the two cases.
- 2026-07-16 (M60): shipping a name that contains an existing asserted name as a substring (merge_guard → merge_guard_post) silently unfalsifies every bare `assertIn` on the shorter name AND blocks its mutation registration (the block is no longer unique in the target) — use word-bounded `assertRegex` (`\bname\b`; `_` is a word char, so the suffixed form can't satisfy it) and register a longer unique anchor phrase (review F1/90).
- 2026-07-16 (M61): the mutation harness runs a guard as a SINGLE method (`TestCase(method).run()`), which skips `setUpClass` — a guard that caches its source text in setUpClass reads the unmutated file and reports false coverage on itself; read the target per-test (property/setUp), never a class-level cache.
- 2026-07-16 (M61): don't bulk-edit tracking files with ad-hoc string scripts — a review-time `str.partition` tick-the-boxes script silently truncated the milestone file at the section heading (only `git checkout --` recovered it); tick checkboxes with targeted Edit replacements, and if a script must touch a tracking file, `wc -l`/diff it before moving on.
- 2026-07-16 (M68): writing NEW prose into a section a section-scoped guard asserts on can duplicate the guard's token and silently unfalsify it — "commit history" added a second "commit" to the generic release-walk body whose guard asserts it; the mutation harness caught it at authoring time. Reword the new prose (→ "git history"), don't widen or re-anchor the guard.
- 2026-07-17 (M69): when implementation improves on a planned output token/format (here `+N over` → the more actionable `shed ≥N`), amend the AC via the implement step-6 gate in that same task — don't let plan text drift from the delivered string, or review's "never reinterpret the AC" rule correctly fails it (two independent lenses caught it here).
- 2026-07-18 (M71, corrected M75): a Claude Code hook `matcher` made only of `[a-zA-Z0-9_|]` (or `[a-zA-Z0-9_|, -]`) takes a LITERAL path — split on `|`/`,`, each alternative exact-matched — so `|` does NOT buy regex treatment; only a metacharacter (`mcp__.*__spawn_task`) reaches `new RegExp`, and that path is unanchored. Keep the hook's own tool-name regex the same suffix shape so matcher and hook agree. Full dispatch: `references/claude-code-hooks.md`.
- 2026-07-18 (M71): shipping a new hook touches four places beyond the script itself — `hooks.json`, BOTH `TestNonCairnNoOp` payload collections in `hooks/tests/test_hooks.py` (dict + garbage-stdin tuple), `cairn/DESIGN.md`'s inventory bullet (count words included: "Eight hooks", "five PreToolUse", "three nudges"), and `test_positioning_guard.py`'s hardcoded `HOOKS` tuple, which is checked against DESIGN's prose and NOT derived from `hooks/`.
- 2026-07-17 (M70): a shipped-profile milestone's guard tests lock the profile's slot shape + toolchain tokens but NOT whether its doctrine is technically correct — the fresh-context diff-bug lens is the only check on domain accuracy in profile prose (F1: the docker consistency-gate wrongly blessed `--build-arg` for secrets, which leak via `docker history`). Author slots with real toolchain doctrine, not plausible-sounding commands.
- 2026-07-18 (M72): extends M33 — merge_guard's containment check fires on any Bash command that merely *quotes* the guarded string, not just one that chains it: a heredoc writing docs about the merge command, an evidence grep, and a debug probe were each denied though nothing merged. Author that content with the Write/Edit tool, or assemble the string from parts inside a script file and run the file.
- 2026-07-18 (M72): a containment-based command guard must parse EVERY occurrence, not the first — `re.search` on a chained two-merge command checked only the leading PR and let the second through, fail-open in the exact property the milestone shipped (fan-out F4, scored 82); when a detection regex graduates from "is this guarded?" to "is this *authorized*?", switch it to `finditer` and require every occurrence to clear the check.
- 2026-07-18 (M73): a sub-threshold confidence score gates the ACTIONED list, not the operator's judgment — the scorer dropped "the fork fallback closes a contributor's PR with no approval gate" at 48 on a false equivalence (opening your own PR is reversible; closing someone else's is not). Read every sub-80 finding's substance before merging, and treat any finding that authorizes an outward-facing irreversible action as worth fixing regardless of score.
- 2026-07-18 (M74): when guarded prose is a LABEL→RULE mapping (a disposition list, a routing table), the assert must include the LABEL — pinning only the clause ("the default for anything real but not urgent") passes after the label is swapped to something else entirely, so the guard survives an inversion of the rule it names. The mutation harness cannot catch this: registration is per file, and sound entries elsewhere in the same file mask it. Proven live at M74 review — inverting two disposition labels left all 18 tests green (F3, scored 92).
- 2026-07-18 (M74): a skill step that fetches a field it never consumes is a live bug, not a cosmetic one — `gh pr list --json …,author` with no reader meant "external PRs" existed only in the bullet's title, so the audit enumerated cairn's OWN in-review milestone PR as inbox and could propose adopting a PR the session authored. When prose names a filter, grep the step for the thing that applies it.
- 2026-07-18 (M73): `gh pr create`/`ready`/`merge` all use GraphQL, which has its own quota that can be exhausted while REST is healthy — `gh api repos/{owner}/{repo}/pulls --input <json>` opens a PR (draft included) when `gh pr create` is rate-limited. Never route the MERGE through REST to dodge a limit: merge_guard is a PreToolUse hook matching the merge commands, so a REST merge silently bypasses the approval marker — exactly the enforcement boundary M72 documented.
- 2026-07-18 (M75): a rule inherited from a prior milestone's work-log finding is UNVERIFIED until read out of the implementation — M71's matcher rule was restated into a reference page and shipped wrong, costing two review trips; and when a fix commit adds a worked example, check the example against the rule it illustrates (the corrected page then illustrated the regex path with `Edit`, a matcher that never reaches it).
- 2026-07-18 (M76): extends M74 from label→clause to label→SET — a guard on prose mapping a LABEL to an ENUMERATION must pin the label together with its members on one physical line; pinning only the mechanism sentence ("current knowledge is corrected in place, history is superseded") left the two file-sets swappable AND deletable with all six asserts green (fan-out F1/88 proved both mutations live). The mutation harness cannot catch it: blanking is not swapping, and registration is per file.
- 2026-07-18 (M76, corrected M77): the milestone cap no longer counts the work log — M77/D-046 exempted it, so the "compress the heaviest section" remedy can no longer collide with IP4 and the breakdown never names it. Still true and still worth knowing: a hard-wrapped work-log entry costs ~4× its physical lines (M76's 15 entries measured 58 lines wrapped vs 21 reflowed), which is why the one-line-per-entry mandate now has a `work-log format` advisory behind it.
- 2026-07-18 (M77): when one task authors content and another authors the checker for it, add a test that runs the real checker over the real authored artifact — M77 shipped a 3-line template comment and a single-line comment matcher, so every milestone born from the template would have emitted 3 spurious WARNs, and all 415 tests stayed green because nothing paired the two. The fixture copy is not the artifact; read the shipped file. Extended M80: a TEMPLATE is such an artifact, and its pairing test must INSTANTIATE it — placeholders can't satisfy the regexes the guards apply, so merely reading the template proves nothing; M80's instantiating test caught a defect in the template shipped one commit earlier AND an identical two-milestone-old defect in M78's `source-note.md`, which had been emitting pages that fail M78's own dated-extraction guard since it shipped.
- 2026-07-18 (M77): the plan-time collision sweep must grep `milestones/archive/` for *decisions*, not just DECISIONS.md and candidate rows — M69 recorded "work log stays counted" as a milestone-local decision with no D-entry, and M77 reversed it without citing it; a milestone-local decision is superseded in the same milestone-local form, since DECISIONS.md is history IP4 forbids editing.
- 2026-07-18 (M78): a milestone that ships a records-hygiene rule must run that rule over the artifacts the milestone ITSELF authors — M78's 16-page provenance backfill introduced 16 undated repo-state claims, verbatim the category its own new rule forbids; the compliance fix committed the failure it was fixing, and the diff-bug lens caught it, not the author (F3/90).
- 2026-07-18 (M78): a new rule's home is decided by "would a repo with NO numeric work need this?" — M78 wrote a universal references-page rule into the conditionally-read `validation-doctrine.md`, making the anti-staleness rule invisible to exactly the non-numeric sessions that write those pages; D-031's universal-vs-domain-conditional boundary is the test, and the sentence stating it sat directly above the insertion point (F1/87, blame lens).
- 2026-07-18 (M80): a guard asserting a lowercased substring over a whole template file gets false coverage from the file's OWN HTML comment header — `extraction:` occurred in the comment, in the real Provenance field, and in a body note, so deleting the actual field line left the guard green (review F3/65). Anchor a template field on its LINE (`startswith`, column 0), never on a substring of the file; and the mutation harness will not catch this, because template-asserting classes carry no `Mutation` entries.
