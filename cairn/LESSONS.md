# Lessons

Durable repo lessons — build quirks, testing tricks, gotchas worth
remembering next time — captured at milestone end and surfaced at plan time.
Not status, not decisions: a lesson is a reusable "how this repo actually
behaves" note. Cross-cutting *choices* still go to `DECISIONS.md`.

One line per lesson: `- YYYY-MM-DD (M<NN>): <lesson>`. Two caps, two remedies
(tracking-rules weight-caps): 50 lines — prune the stalest when full — and
20,500 characters, where the fix is compressing or consolidating in place, not
evicting. Current knowledge: a lesson proven false is corrected in place (D-045).

<!-- lessons appended below by /milestone-review post-merge hygiene -->

- 2026-07-11 (M16): this repo has no CI — `gh pr checks --watch` returns "no checks reported" + exit 0; treat as no-CI and merge directly, never wait for green.
- 2026-07-12 (M32): this repo has no `pytest` — the script suite is unittest; run `python3 -m unittest discover -s scripts/tests` (or `-k` a name via `python3 -m unittest scripts.tests.test_scripts -k <sub>`). Don't write `pytest …` into acceptance criteria; it fails "No module named pytest".
- 2026-07-12 (M35): when a milestone's scope was drawn from existing ROADMAP candidate rows, those rows are NOT pruned at plan time (candidates graduate at completion, not at plan) — graduate them in the post-merge hygiene pass or the ROADMAP advertises shipped work as pending; both review lenses flagged M35's two fulfilled rows.
- 2026-07-12 (M51): the archive summary has its own tight ≤25-line cap (separate from the 150-line milestone cap) — a decisions-heavy milestone won't fit a full Goal/Outcome/Decisions/Review skeleton, so budget ~2 wrapped lines per section. Trimming prose only reduces the count when it removes a whole *wrapped* physical line: a shorter sentence that still wraps to N lines saves nothing, so `wc -l` after each edit rather than eyeballing word count.
- 2026-07-13 (M53, extended M54): the false-coverage guard trap (M39/M40) is mechanized — prose-guards register in `skills/tests/test_mutation_harness.py`, which blanks each registered block and asserts its guard fails; a completeness meta-test fails CI on an unregistered guard *file*. Registration is per file (≥1 exemplar block), NOT per assertion — a new `assertIn` in a registered file still needs its own `Mutation(...)` entry or the by-hand M47 check; M23/M26 still apply to *matchability*. A registered `block` must be a *positive* assertion's phrase — `assertNotIn` cannot be mutation-proven — so pair each negative guard with a positive framing assert carrying the entry. Run skills guards via `discover -s skills/tests`: the harness does a bare `import mutation_engine`, so a dotted-name run dies ModuleNotFoundError.
- 2026-07-16 (M56+M65, consolidated M78/M83): "green" is only as wide as what you ran. Bash's working directory persists across tool calls and piping a verify through `| tail -1` makes the chain exit-blind (`tail` exits 0 even when `discover` ImportErrors) — a lingering `cd cairn` silently skipped all three suites, and at the M65 gate the pipe hid a red exit. Run suites from the repo root, check each exit code explicitly, and never chain a commit/push/marker write after a piped run. And `cairn_validate` green is NOT the suites green: a `cairn/references/` edit passing every validate check can still break a `skills/tests` guard, and a trivial-tier docs commit pushed main red because only validate was run — any references or template edit runs the three suites too, trivial tier or not.
- 2026-07-16 (M57, extended M79/M81): a hard validate CHECK parsing human-authored markdown must tolerate cosmetic decoration on the semantic token — the INDEX-line regex double-FAILed correct backticked/`[name](name)` entries; D-023's no-false-positive doctrine binds CHECK parsers, not just scan heuristics. Decoration is only ONE axis of freedom, and fixtures varying it alone pass **vacuously** on PHRASING and LAYOUT (F1/85, F2/95, F3/92, F4/90). Vary every axis the prose is free in, and vary it WHERE THE VALUE UNDER TEST LIVES: M81's 36-cell product wrapped at a split point leaving the date before the cut in every phrasing, so no cell wrapped where it changes the ANSWER, and its `page()` fixture always put the block LAST, making trailing-paragraph absorption untestable. Widening a capture class admits non-targets too (F5/85).
- 2026-07-16 (M58+M59+M62, consolidated M77): a repo-wide sweep and a grep-shaped AC both hit ground you did not mean. An exclusion list may name only history files (DECISIONS/CHANGELOG/legacy/reviews-archive), never a live directory — excluding `cairn/references/` left two live citable pages asserting the doctrine still lived in tracking-rules (F1/85; extends M48). And an AC whose evidence is a grep will hit the milestone's OWN artifacts: the guard's `assertNotIn` (an absence-assert is a hit for the token it locks), plus the milestone file's Scope/Tasks and the ROADMAP lineage row on a removal/rename sweep. Scope evidence commands to the prose surface being fixed and exempt the tracking lines in the criterion at plan time, else it needs a gated amendment at implement (both hit live).
- 2026-07-16 (M58+M59+M64+M65, consolidated M78/M82): a mutation-registered `block` must sit on ONE physical line, occur exactly once, and include its trailing punctuation — a reflow, a duplicate, or a period→semicolon edit all "found 0"-error the harness. It fires hardest on BRAND-NEW prose authored wrapped then registered (M78 hit it three times in one milestone), and just as hard on REFLOWING existing prose. Author the anchor on its own physical line, or pick one already unique and unwrapped; re-run the skills suite after any rewording near a registered block; fix the WRAP, never the assert — loosening an assert is how a label→rule guard stops pinning its rule. **M90 narrows that:** it binds mutation blocks and label→rule pairings; a plain presence check over body prose that legitimately re-wraps may normalize whitespace, since the anchor is still matched exactly. Also: prose naming a validate finding uses the emitted label verbatim in backticks (`coverage complete`); write evidence counts from command output, never memory (M28).
- 2026-07-16 (M60): refines M19 — hook *registrations* (hooks.json) snapshot at process start, but each script BODY is re-read per invocation from the symlinked checkout: editing an already-registered hook goes live mid-session (the new merge_guard renamed the marker during M60's own merge), while a newly-registered hook stays dead until a fresh conversation; a fixture test still proves only what a hook prints, and live-fire plans must split the two cases.
- 2026-07-16 (M60, extended M80/M85): a SUBSTRING anchor gives false coverage — and prose you add can create one for a guard in ANOTHER file. Shipping a name containing an asserted name (merge_guard → merge_guard_post) unfalsifies every bare `assertIn` on the shorter name AND blocks its mutation registration — use word-bounded `assertRegex` (`\bname\b`; `_` is a word char) (F1/90). On a template, `extraction:` occurred in the file's OWN comment header and in the real field (F3/65). M85 hit the REVERSE: new header prose naming the verb set added a second `unverified`, degrading a bare `assertIn` elsewhere (F1/93) — after adding prose, grep every word an existing guard anchors on. Anchor a template field on its LINE (`startswith`, col 0); template classes CAN be registered (M85 added ten).
- 2026-07-16 (M61): the mutation harness runs a guard as a SINGLE method (`TestCase(method).run()`), which skips `setUpClass` — a guard that caches its source text in setUpClass reads the unmutated file and reports false coverage on itself; read the target per-test (property/setUp), never a class-level cache.
- 2026-07-16 (M61): don't bulk-edit tracking files with ad-hoc string scripts — a review-time `str.partition` tick-the-boxes script silently truncated the milestone file at the section heading (only `git checkout --` recovered it); tick checkboxes with targeted Edit replacements, and if a script must touch a tracking file, `wc -l`/diff it before moving on.
- 2026-07-17 (M69): when implementation improves on a planned output token/format (here `+N over` → the more actionable `shed ≥N`), amend the AC via the implement step-6 gate in that same task — don't let plan text drift from the delivered string, or review's "never reinterpret the AC" rule correctly fails it (two independent lenses caught it here).
- 2026-07-18 (M71, corrected M75): a Claude Code hook `matcher` made only of `[a-zA-Z0-9_|]` (or `[a-zA-Z0-9_|, -]`) takes a LITERAL path — split on `|`/`,`, each alternative exact-matched — so `|` does NOT buy regex treatment; only a metacharacter (`mcp__.*__spawn_task`) reaches `new RegExp`, and that path is unanchored. Keep the hook's own tool-name regex the same suffix shape so matcher and hook agree. Full dispatch: `references/claude-code-hooks.md`.
- 2026-07-18 (M71): shipping a new hook touches four places beyond the script itself — `hooks.json`, BOTH `TestNonCairnNoOp` payload collections in `hooks/tests/test_hooks.py` (dict + garbage-stdin tuple), `cairn/DESIGN.md`'s inventory bullet (count words included: "Eight hooks", "five PreToolUse", "three nudges"), and `test_positioning_guard.py`'s hardcoded `HOOKS` tuple, which is checked against DESIGN's prose and NOT derived from `hooks/`.
- 2026-07-18 (M72): extends M33 — merge_guard's containment check fires on any Bash command that merely *quotes* the guarded string, not just one that chains it: a heredoc documenting the merge command, an evidence grep, and a debug probe were each denied though nothing merged. Author that content with Write/Edit, or assemble the string from parts inside a script file and run the file. M83 adds the direction-blind case — `git merge main` INTO a feature branch is denied though tracking-rules prescribes exactly that when main moves, so sync with `git rebase main` (which never touches main) rather than rephrasing to slip past the guard.
- 2026-07-18 (M72): a containment-based command guard must parse EVERY occurrence, not the first — `re.search` on a chained two-merge command checked only the leading PR and let the second through, fail-open in the exact property the milestone shipped (fan-out F4, scored 82); when a detection regex graduates from "is this guarded?" to "is this *authorized*?", switch it to `finditer` and require every occurrence to clear the check.
- 2026-07-18 (M73): a sub-threshold confidence score gates the ACTIONED list, not the operator's judgment — the scorer dropped "the fork fallback closes a contributor's PR with no approval gate" at 48 on a false equivalence (opening your own PR is reversible; closing someone else's is not). Read every sub-80 finding's substance, and treat any finding that authorizes an outward-facing irreversible action as worth fixing regardless of score. **Extended M88:** the scorer's *reasoning* can be wrong while its number is defensible — it refuted a token-gap finding by crediting a token that did match, when the case actually failed on a missing version; verify a refutation against the implementation before accepting it.
- 2026-07-18 (M74, extended M76/M86): when guarded prose maps a LABEL to a RULE or to a SET, the assert must pin the LABEL together with its members on ONE physical line. Pinning only the clause survives swapping the label elsewhere; pinning only the mechanism sentence left two file-sets swappable AND deletable with all six asserts green. The harness catches neither — blanking is not swapping, and registration is per file, so sound entries elsewhere mask it. Proven live three times (M74 F3/92; M76 F1/88; M86 F1/85+F2/88+F3/88). A WRAPPED sentence is the usual cause — the anchor stops at the break, before the predicate carrying the meaning. VERIFY BY INVERSION: relabel, negate, or transpose the rule in place, run the suite, require red, restore and diff.
- 2026-07-18 (M73): `gh pr create`/`ready`/`merge` all use GraphQL, whose quota can be exhausted while REST is healthy — `gh api repos/{owner}/{repo}/pulls --input <json>` opens a PR (draft included) when `gh pr create` is rate-limited. Never route the MERGE through REST to dodge a limit: merge_guard is a PreToolUse hook matching the merge commands, so a REST merge silently bypasses the approval marker — the enforcement boundary M72 documented.
- 2026-07-18 (M75, extended M85): a rule inherited from a prior milestone's finding is UNVERIFIED until read out of the implementation — M71's matcher rule was restated into a reference page and shipped wrong, costing two review trips; when a fix commit adds a worked example, check it against the rule it illustrates. M85 repeated it restating a PARSER into doc prose: the taught verb set listed `unverified` beside the four affirmative verbs, but `_UNVERIFIED` classifies `never` unconditionally — when documenting a set, run EACH member through the implementation, never the set as a whole (F2/80).
- 2026-07-18 (M77, extended M80): when one task authors content and another authors its checker, add a test running the REAL checker over the REAL artifact — M77's 3-line template comment against its single-line matcher would have WARNed on every milestone born from the template, and all 415 tests stayed green because nothing paired the two. The fixture copy is not the artifact. A TEMPLATE is such an artifact, and its pairing test must INSTANTIATE it, since placeholders satisfy no guard regex: M80's caught a defect in the template shipped one commit earlier AND a two-milestone-old one in `source-note.md`, which had been emitting pages failing M78's own dated-extraction guard since it shipped.
- 2026-07-18 (M77): the plan-time collision sweep must grep `milestones/archive/` for *decisions*, not just DECISIONS.md and candidate rows — M69 recorded "work log stays counted" as a milestone-local decision with no D-entry, and M77 reversed it without citing it; a milestone-local decision is superseded in the same milestone-local form, since DECISIONS.md is history IP4 forbids editing.
- 2026-07-18 (M78): a milestone that ships a records-hygiene rule must run that rule over the artifacts the milestone ITSELF authors — M78's 16-page provenance backfill introduced 16 undated repo-state claims, verbatim the category its own new rule forbids; the compliance fix committed the failure it was fixing, and the diff-bug lens caught it, not the author (F3/90).
- 2026-07-18 (M78): a new rule's home is decided by "would a repo with NO numeric work need this?" — M78 wrote a universal references-page rule into the conditionally-read `validation-doctrine.md`, making the anti-staleness rule invisible to exactly the non-numeric sessions that write those pages; D-031's universal-vs-domain-conditional boundary is the test, and the sentence stating it sat directly above the insertion point (F1/87, blame lens).
- 2026-07-18 (M81, extended M82): when a change to a SHARED parser is defended with a safety claim, check the claim answers the right question — "a more generous block means no page that passed can now fail" was true and was the WRONG invariant, because the hard CHECK it feeds asks only EXISTENCE questions, so a wider read cannot create failures, it ERASES them. Three lenses and the author verified the stated invariant; only the diff-bug lens asked whether it was the one that mattered. A gate and an advisory need opposite protections against one parser — parameterize, never share a widening rule. M82 generalizes: a fix that quiets a detector can destroy its job as a REMINDER. When a change makes a check fire LESS, ask what it was FOR.
- 2026-07-18 (M83): negation is a property of a CLAUSE, not a phrase list — matching affirmative verbs against a fixed set of negative PHRASES broke in both directions at once when the verb set grew (`checked against`/`read against` were added but only `verified` had a negative form), so `no claim checked against the source` read as an affirmative verification: a false positive on the very prose that motivated the milestone AND a clean bill for a page saying in plain words it was never checked. When a matcher gains verbs its negation handling gains them too; scope the negator search to the verb's own clause, or a trailing `not re-read since` sweeps up the page it merely qualifies.
- 2026-07-18 (M84): a guard whose only assertion is an `assertNotIn` is vacuous against a CRASH — empty stdout satisfies every absence-assert, so a test named for the absent-file path stayed green while the advisory raised on every run and validate exited 1 (F2/90, reproduced independently by two agents). Pair an absence-assert with a POSITIVE signal that the path ran (`OK <label>` in the output), and check what the exit code SHOULD be — a deleted scaffold file makes it 1 for an unrelated reason, so asserting 0 would have been wrong too.
- 2026-07-19 (M87): a constant derived from another gate's threshold must be derived through that gate's own comparison — `check_caps` FAILs at `n >= cap`, so a `<50` cap permits 49 lines and capacity is `(cap-1) - overhead`. Deriving from the cap inclusively over-derived both density thresholds by one item's mass and shipped wrong percentages into a D-entry and a milestone decision before review caught it (F5/80). Read the operator; never assume the cap value is itself attainable.
- 2026-07-19 (M87, extended M90): a number restated in prose IS an encoding, so a stated<->enforced coupling test is only as wide as the encodings it pairs — the density threshold lived in THREE places (`tracking-rules.md`, `CHAR_CAPS`, `LESSONS.md`'s own header) while the guard paired two, so the header taught the retired number with validate green (F1/90). Count every site before trusting a coupling. M90: cheaper still is not to restate it — README's "two advisory nudges" contradicted `DESIGN.md`'s "three nudges are advisory" and was mutation-registered, pinning the wrong number so a later correction would redden the guard; when prose would restate a count another file owns, name the members and let the owner keep the number.
- 2026-07-19 (M87): at review the cap breakdown can name a section review is forbidden to touch — M87's overrun ranked plan-owned `Scope` heaviest while the growth was review's own evidence in `## Decisions`. Compress what your phase OWNS and cross-reference a durable record instead of restating it; M51's rule that only removing a whole WRAPPED line lowers the count fired three times in one pass, so cut content, don't rephrase.
- 2026-07-19 (M88): a two-signal detector is only as strong as its weaker signal, and a fixture helper that DEFAULTS one input never tests it — the release-window pair (release token AND shipping version) had a version half matching any decimal, so every release-tooling fixture flipped to release-shaped on an ordinary goal ("targets R 4.3", "section 2.1"), and all ten tests passed because the helper's stub goal was "Ship it." (F6/91). Extends M57/M81's vary-it-where-the-value-lives rule with the defaulted-parameter shape: grep the test helper for defaults before believing a discrimination claim, and give every false-positive fixture a realistic value on the axis it defends.
- 2026-07-19 (M90): `Edit` with `replace_all` applies ONE fixed indentation to every match — in Python that silently changes block structure and stays valid. A nested loop dedented to its parent's level ran once after the parent finished, against the last-bound variable, so a two-surface guard checked one surface and passed green (F1/96). Never `replace_all` a line whose correct indentation differs between occurrences; prove a guard reddens for EACH input it covers.
- 2026-07-19 (M89): a guard over a taught vocabulary must build its fixture in the shape an AUTHOR writes, not the shape that makes the assertion convenient — M89's guard ran each taught partiality qualifier as `{q} verified against the source`, which hands every member an independent verb, so it passed on `spot-checked verified against the source` while the phrasing the templates actually teach (`spot-checked against the source`, where the qualifier OVERLAPS the verb) classified as a full verification (F1/95). Where a vocabulary member can textually collide with the pattern it modifies, one clause shape is not a test: run several, and assert the failure direction (never a bare `verified`) rather than only the success case.
