# Lessons

Durable repo lessons — build quirks, testing tricks, gotchas worth
remembering next time — captured at milestone end and surfaced at plan time.
Not status, not decisions: a lesson is a reusable "how this repo actually
behaves" note. Cross-cutting *choices* still go to `DECISIONS.md`.

One line per lesson: `- YYYY-MM-DD (M<NNN>): <lesson>`. Two caps
(tracking-rules weight-caps): 50 lines and 20,000 bytes, met by retiring or pruning entries.
Current knowledge: a lesson proven false is corrected in place (D-045).
Lessons also *leave*: one retires when a test fails on the mistake it warns
about, when another file's slot owns its content, or when a matured family
graduates whole into a doctrine module; pruning the stalest is the last resort
rather than the first (D-051, D-055).

<!-- lessons appended below by /milestone-review post-merge hygiene -->

- 2026-07-16 (M56+M65, consolidated M78/M83/M111/M124, extended M121/M137/M138/M140/M164, corrected M144, trimmed M147): "green" is only as wide as what you ran — run each gating suite from the repo root and check each exit code explicitly, never piping (`| tail` is exit-blind) or `;`-chaining a commit past a red check; scope a verification command to the tree that holds the claim, and write git pathspecs out (wildmatch `*` crosses `/`, and unquoted the shell binds it to the working tree); probe a sweep instrument against a known-positive hit before believing an empty result (a decorating flag like `grep -n` re-alphabets later stages; `unittest -k` takes substrings, never pytest `or`, and NO TESTS RAN reads like a red gate); and a probe that mutates or restores files commits pending fixes first, restores in a `finally:`, and re-verifies the fix on disk after the run; and `cairn_validate` green is NOT the gating suites green — any edit under `scripts/` or `hooks/`, or to `skills/shared/templates/` or a `skills/*/SKILL.md` (files the gating tests read), runs both gating suites, trivial tier or not; and a probe run in a copied or `git archive`-exported tree is not the repo — carry the full import graph (`scripts/` imports `hooks/cairn_common`), expect git-shelling tests to red without `.git`, and show the unmutated control green before planting the mutation, so a crash-red is never mistaken for the discriminating red (M164).
- 2026-07-18 (M71, corrected M75, consolidated M92, trimmed M147): hooks trap on MATCHING and on SHIPPING — a `matcher` containing only `[a-zA-Z0-9_|, -]` is a LITERAL split on `|`/`,` (only a metacharacter reaches `new RegExp`, and that path is unanchored; full dispatch: `references/claude-code-hooks.md`), and a new hook ships four surfaces beyond its script: `hooks.json`, BOTH `TestNonCairnNoOp` payload collections in `hooks/tests/test_hooks.py`, `cairn/DESIGN.md`'s inventory bullet (count words included), and `test_positioning_guard.py`'s hardcoded `HOOKS` tuple.
- 2026-07-18 (M72, extends M33, trimmed M92/M98): the merge guard is direction-blind — `git merge main` INTO a feature branch is denied though tracking-rules prescribes merging when main moves, so sync a feature branch with `git rebase main` instead.
- 2026-07-18 (M73): `gh pr create`/`ready`/`merge` all use GraphQL, whose quota can be exhausted while REST is healthy — `gh api repos/{owner}/{repo}/pulls --input <json>` opens a PR (draft included) when `gh pr create` is rate-limited. Never route the MERGE through REST to dodge a limit: merge_guard is a PreToolUse hook matching the merge commands, so a REST merge silently bypasses the approval marker — the enforcement boundary M72 documented.
- 2026-07-18 (M81/M91, trimmed M98): re-dating a DERIVED references page restarts its 180-day staleness clock though half its inputs were never re-read — age a synthesis note from its OLDEST un-re-read input (F4/88).
- 2026-07-19 (M90, trimmed M147): `Edit` with `replace_all` applies ONE fixed indentation to every match — in Python that silently changes block structure and stays valid (a dedented nested loop ran once after its parent, green); never `replace_all` a line whose correct indentation differs between occurrences, and prove a guard reddens for EACH input it covers.
- 2026-07-20 (M99, extended M115/M118, trimmed M147): a figure stated INSIDE the artifact it measures is a fixed point — writing it changes it; prefer a tool that prints the number over prose that states it, and where the number is itself the deliverable, the derived-figures rule's pin is the remedy (M137).
- 2026-07-20 (M99): rewriting an instruction from "transform X into Y" to "author a fresh Y" silently drops X's disposal — step 9 said "compress the milestone file and move it", where the move deleted the live copy implicitly; authoring from a template made the summary a NEW file and nothing removed the old one, orphaning it until `cairn_validate` caught it (F4/85). When a rewrite changes what an artifact IS, re-derive every side effect the old phrasing carried implicitly.
- 2026-07-20 (M100, trimmed M147): an enforcement check must fail LOUD, never open — silence means "nothing binds", never "binding content the parser missed"; a present-but-unparseable input is a finding, not a skip, and id tokens in matchers get word bounds.
- 2026-07-20 (M104): a rule generalized to "every X" can silently override a scoped sibling that already handles a subclass of X differently — a work-only staleness bullet applied to "every in-progress milestone" collided with `check_release_window`'s deliberate any-entry leniency for release-shaped ones (D-017); the blame-history lens caught the regression the diff-bug lens read past. When broadening a scope word, check every existing mechanism that treats a subclass differently.
- 2026-07-20 (M105): `gh pr merge` squashes the REMOTE branch as GitHub sees it, not your local HEAD — review-phase commits made AFTER the review-start `git push` (AC ticks, the Review section, the fan-out record) are silently dropped from the squash unless re-pushed. M105's deliverable survived only because the code landed in the pre-review push; the milestone-file bookkeeping did not (the archive summary caught it, but main's squash lacks it). Re-push the branch right before writing `.merge-approved`.
- 2026-07-23 (M109): a `unittest` skip can't be asserted directly — `skipTest` raises, it doesn't return — so a machine-coupled live test whose skip path must be proven on any machine factors the skip decision into a helper that raises `SkipTest`, then a second test does `assertRaises(SkipTest)` against a synthetic empty input. And to prove a resource-backed test hit a fixture and never the real store, spy on the reader (`read_records`) and assert the captured path is the fixture — an exit-code-only assertion passes just as happily against the real store.
- 2026-07-23 (M111, trimmed M147): post-merge hygiene archives a milestone by ADDING a `done` row, which can push the ROADMAP over the 5-row terminal-row retention cap — prune the oldest `done` row (it survives in `archive/` + git) in the SAME archive commit, not as a later fix.
- 2026-07-24 (M112): a milestone that changes doctrine *wording* has more surfaces than the skills it edits — README, templates, and other user-facing docs restate the same doctrine and go stale silently. Scope the AC to the whole surface (grep the repo for the OLD phrasing), not just the skill files; M112 scoped AC1 to "the three skill files" and shipped a README/doctrine contradiction only the blame-history reviewer caught at the gate.
- 2026-07-25 (M113): a second bounding pass with its own floor silently undoes the first pass's floor. A block-level bound with a 3-entry floor, backstopped by a line-level bound with a 1-line floor, went NON-MONOTONIC in the budget — at budget 100-304 a work log showed zero entries and reported "newest 1 of 42 lines shown", the shown line being blank, while budget 0 showed three entries. Two floors on one quantity is the smell; bound the thing that is actually unbounded (there, the prose above the first entry) instead of adding a pass over everything.
- 2026-07-25 (M113, trimmed M122): a hook needing a rule the scripts already implement must reuse THEIR normalization, not re-derive it — the two layers cannot share code (hooks import only `cairn_common`), so their agreement is matched BY HAND and is a review item, never an assumption. The heading instance is retired: `TestHeadingNormalizationContract` (M122) now fails when the two diverge on case, whitespace, the `## ` prefix or fences, so what is still carried here is only the rule for the NEXT behaviour the two layers have to share.
- 2026-07-26 (M115, corrected M135): `check_dangling_ids` SKIPS tokens numerically above the max assigned id (forward prose, D-023's false-positive tolerance) — so appending one D-entry raises the ceiling and unmasks every previously-invisible dangling reference below it at once. Three appeared the moment D-067 landed. A clean `dangling id tokens` line is therefore not evidence that no dangler exists, only that none sits above the legacy-max floor (M135: with `cairn/legacy/` present, M-tokens at/below the highest entombed id are also skipped) and below the current max; expect a batch when the next entry is appended.
- 2026-07-26 (M115): a citation to an artifact living on ANOTHER milestone's unmerged branch (an RR under `reviews/archive/`, a D-entry) resolves to nothing on main and ships as a pointer to a missing file. Convention, verified: only RR01 and RR04 are cited in non-test prose under `skills/`, both archived on main. When the source is unmerged, carry its RULE verbatim into the shipped prose and name the source only in the milestone file or D-entry, where the unmerged status can be stated.
- 2026-07-27 (M114): work-log entries state decision-relevant facts, never characterizations — adjectives and superlatives ("terser", "narrower", "the longest") are falsifiable surface a certification round reds on. M114's late rounds found four such conclusions false while every measured number beside them reproduced exactly; the numbers were never the problem, the rhetoric drawn from them was.
- 2026-07-31 (M127): a whole-file deletion disposes of every class the file carries, not only the one the plan named — `test_section_ledger.py` was deleted as "M124 ledger machinery" and took `TestSectionNineDoctrine` with it, §9-content guards whose subject survives; all three review lenses converged on the gap. Before deleting a test file whole, enumerate its classes and disposition each against its own subject, exactly as a per-hit search dispositions prose.
- 2026-08-04 (M133): in zsh a `while read` variable named `path` clobbers `$PATH` (a tied array) and every later command in the loop dies "not found" — commands stated in pages or skills never use `path` as a shell variable name.
- 2026-08-16 (M146): a scripted registry prune leaves survivors its parser skipped — recount by AST against the source afterward, never trust the script's own kept-count (the stranded 325-vs-326).
- 2026-08-17 (M149): the /milestone rulebook-mass baseline lives in THREE places — the skill line plus two hand-run pins (`test_cost_audit_line.py:67`, `test_mutation_harness.py:117`) — and a deliberate rulebook change re-seeds all three in the same branch; the suite pins only the three sites against each other, so pin-vs-file drift stays silent and no test reds on it (corrected M159 — F1 was exactly this silent drift).
- 2026-08-17 (M148): a prose-guard fixture pins a unique start-marker phrase — new skill prose inserted near a pinned slice that echoes the marker's opening words breaks locator uniqueness (4 reds from one echoed phrase); reword the NEW sentence, never the pinned one, and hand-run `skills/tests` after any edit near a guarded region.
- 2026-08-17 (M150): GitHub renders a mermaid fence inside a cross-origin viewscreen.githubusercontent.com iframe — the parent DOM proves only that the fence was ACCEPTED as a diagram (its source pre moves under render-plaintext-hidden), never what got drawn; anything whose correctness lives inside the picture (an HTML <br> line break, label overlap) needs an actual screenshot, so prefer diagram constructs that are plain quoted text.
- 2026-08-22 (M152): a "doctrine-prose repair" (D-090) that changes a recorded decision's OPERATIVE text — here D-051's ownership-exit definition — is a decision change owing a D-entry, not a repair; blame the sentence being repaired before shipping the fix (the review fan-out caught and reverted it).
- 2026-08-22 (M154): a promoted candidate row is pruned at post-merge hygiene, never at plan (§1 of the candidate-row lifecycle module) — M153's review recorded exactly this plan-time prune and M154's plan gate repeated it the next milestone, because that module is read only when remembered: open it at the moment a row is promoted, before touching the ROADMAP.
- 2026-08-22 (M153): a pre-archive `## Review` section is recoverable from git (read the file at the parent of its archiving commit), but only what was WRITTEN there — M143/M152 recorded AC evidence and counts only, leaving 27 findings' identities unrecoverable for later measurement; log the fan-out's per-finding list in the milestone file's Review section before archiving, never only in chat.
- 2026-08-22 (M156): a token can defeat BOTH a line-based grep sweep and a raw-substring guard by wrapping across a newline ("routing\nchip" shipped past `grep -rn` and an `assertNotIn` on the raw text) — whitespace-normalize text before a token match, and remember a line-returning sweep's domain is the lines it returns, never the prose those lines wrap.
- 2026-08-23 (M157): a repo-wide count claim ("N cites left dangling") is only as wide as the trees actually swept — milestone path cites also live under `cairn/reviews/`, and the M157 count was stated too low twice (1, then 3; ten real) because reviews/archive was never in the sweep; before stating a total over a record type, enumerate every directory that holds that type and sweep them all.
- 2026-08-23 (M158): a change minting a new durable-record type also adds it to the durable-record preview enumerations (review skill step 9 and tracking-rules) in the same change — M158 shipped the Known issues entry without preview coverage and the review fan-out's top finding was exactly that omission.
- 2026-08-29 (M163): two guard/harness gotchas from the adoption pass — quoted script text carrying a guarded merge command at line start trips merge_guard from inside a `python3 -c` string or heredoc (CMD_POS treats newlines as separators; bit twice in one milestone — author via the Write tool and run the file); and in a multi-repo session the harness resets shell cwd to the primary repo after every Bash call while the directory-change tool applies only at a user turn boundary, so a secondary repo's merge (which must run from a session cwd inside that repo) cannot complete autonomously — plan it around a user nudge.
