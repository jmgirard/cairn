# M181: cairn-init applies the `cairn/**` `paths-ignore` edit under a chip, parsed by PyYAML

- **Status:** review
- **Priority:** high
- **Depends on:** —
- **Driving RR:** —
- **Principles touched:** —
- **Resolves:** —
- **Surface tier:** user-facing — an adopting repo's workflow files are edited, and shipped skill prose changes
- **Branch/PR:** m181-apply-paths-ignore · https://github.com/jmgirard/cairn/pull/188

## Goal

An adopter whose Python has PyYAML gets the `cairn/**` `paths-ignore` item added to each editable `push` trigger by `/cairn-init` under one approve/decline chip, the edit verified against PyYAML's own reading before it is written and left uncommitted for the operator.

## Scope

**In:** `scripts/cairn_ci_paths.py --apply [--dry-run]` reading each workflow with PyYAML's node marks and inserting lines only, with a `safe_load` post-edit check before any write and exit 3 when PyYAML is absent; `--report` unchanged (stdlib); the §0 bullet's clause (d) rewritten around the dry run and chip; both cairn-init commit bullets excluding the edited workflow files; apply fixtures and tests, the stdlib whitelist admitting `yaml` for this script alone; a D-entry annotating D-133 and recording the optional dependency; DESIGN, CHANGELOG (M178's unreleased entry amended), tracking-rules, and prose-guard updates. Lineage: the `[high]` candidate row "Chip-applied `cairn/**` `paths-ignore` edit" (pruned at post-merge hygiene, never here); extends M178.

**Out:** rewriting a scalar `on: push` or flow-list `on: [push, …]` into block form (refused; the by-hand suggestion stands — gate choice 2026-09-06); editing `pull_request` triggers (their filter reads the whole PR diff); a `push` trigger carrying `paths` (GitHub accepts one of `paths`/`paths-ignore`; left to the operator, as M178); committing the edited workflow file (left in the working tree — gate choice); required-check handling under branch protection (the Branch-protection compatibility candidate row); switching `--report` to PyYAML (the line reader stays fixture-held to PyYAML by M178's tests).

## Acceptance criteria

- [x] AC1: `skills/cairn-init/SKILL.md` §0's "CI runs on tracking-only pushes" bullet, after clauses (a)–(c), (d) runs `python3 "${CLAUDE_PLUGIN_ROOT}/scripts/cairn_ci_paths.py" --apply --dry-run`; when at least one dry-run line reads `would apply`, it poses one approve/decline chip naming those files — in the same confirmation round as §0's other options when one is posed, else its own chip — the decline writing nothing; on approval it runs `--apply` without `--dry-run`, re-runs `--report`, and states that the edited workflow files are left uncommitted for the operator to commit; a file the dry run refuses keeps the by-hand suggestion for a `push` verdict lacking both `cairn/**` and `paths`; on dry-run exit 3 the by-hand suggestion stands and the bullet says installing PyYAML enables the applied edit; the §1 scaffold and §3 repair commit bullets each exclude the edited workflow files by name, and both close blocks name them.
- [x] AC2: `scripts/cairn_ci_paths.py [ROOT] --apply [--dry-run]` imports PyYAML only inside the apply path and, when the import fails, exits 3 with a message naming PyYAML, writing nothing; `--report` and `--apply` together is a usage error (exit 2); `--report` stays stdlib-only and unchanged in output; `--apply` prints one line per `*.yml`/`*.yaml` file directly under `.github/workflows/` — `<file>: applied` (`would apply` under `--dry-run`) or `<file>: refused: <reason>` — exits 0 whenever PyYAML imported, whatever the per-file verdicts, and edits a file exactly when PyYAML composes it as one document whose top-level mapping has a key `on` (plain, single- or double-quoted) holding a non-flow mapping, with no event carrying an anchor, no alias event, and no plain `<<` scalar (a merge key, wherever in the span it sits) among PyYAML's parse events between the `on` key's start and its value's end (an `on` value composed at a position before its key counting as an alias), with a `push` key whose value composes as a non-flow mapping node or a null scalar, carrying no `paths` key, and whose `paths-ignore` is absent or a block sequence not containing `cairn/**`, and the post-edit check below passes; the edit inserts lines only — `- 'cairn/**'` at the existing items' column after the last item, or, after `push`'s last child line, a `paths-ignore:` key at the `push` children's column followed by the item (for a null `push`, directly after the `push` line, at the `on` children's column plus the file's own indent step) — preserving the file's line endings, a final line lacking its ending gaining one so the inserted lines follow it; before writing, `yaml.safe_load` of the edited text must equal `yaml.safe_load` of the original with `cairn/**` appended under the top-level key (`True` for a plain `on` under YAML 1.1, `'on'` when quoted) → `push` → `paths-ignore`, the key created when absent, else — or when either load, constructing that expected value, or the comparison raises — the file is refused with reason `post-edit check failed`; every refusal leaves the file byte-identical and names the first reason that applies, in this order: PyYAML cannot parse the file, more than one document, no `on` key, `on` is not a block mapping, `on` holds an anchor, alias, or merge key, no `push` trigger (the key absent, or its value composing as a block sequence or a non-null scalar), `push` holds a flow mapping (its value composing as a flow mapping or flow sequence), `push` carries `paths`, `paths-ignore` is not a block sequence, already ignores `cairn/**`, post-edit check failed, the file cannot be written (the write raising `OSError`); `--dry-run` writes nothing and prints `would apply` exactly for the files `--apply` would edit or refuse as `the file cannot be written` (the write itself being the only writability probe); `scripts/tests/test_scripts.py`'s `TestStdlibOnly` admits `yaml` in `cairn_ci_paths.py` alone and still rejects it in every other `scripts/*.py`.
- [x] AC3: `--apply` edits each of these forms to a byte-exact expected output — bare `push:`, `push` with `branches`, an existing deeper-indented `paths-ignore`, a flush-left `paths-ignore`, a comment inside the `on:` block, a column-0 comment inside the block, a double-quoted `"on":` key, a single-quoted `'on':` key, a file indented in 4-space steps with a bare `push:`, CRLF endings, a `pull_request` carrying its own `paths-ignore`, and a non-trigger key between `push` and `pull_request` — and refuses one input per AC2 reason except `post-edit check failed` and `the file cannot be written` byte-identically; evidenced by an `apply/` fixture set in `scripts/tests/test_ci_paths.py` asserting each applied output byte-equal to its expected fixture, each refused input byte-identical after `--apply`, every fixture byte-identical after `--dry-run` with `would apply` printed exactly for the pair inputs, a unified diff of each pair containing added lines only, and `yaml.safe_load` of each expected fixture equal to `yaml.safe_load` of its input with `cairn/**` appended under `push` → `paths-ignore`; the PyYAML-dependent tests skip with a message when PyYAML is absent; one test runs `--apply` with a shadowing `yaml` module on `PYTHONPATH` raising `ImportError`, asserting exit 3, the PyYAML-naming message, and the file byte-identical; M178's `--apply`-is-a-usage-error assertions are retired, `--report --apply` together staying asserted as a usage error.
- [x] AC4: `cairn/DECISIONS.md` gains an entry annotating D-133 that narrows its "writes nothing" clause to `--report`, records PyYAML as an optional dependency of `--apply` alone (the dependency rule's D-entry), and names the observation class that overturns it; `CHANGELOG.md`'s unreleased M178 entry is amended to the shipped behaviour (chip-applied where PyYAML imports, by hand otherwise) instead of gaining a contradicting second entry; `cairn/DESIGN.md`'s `scripts/` layer drops its unqualified `stdlib` and `Read-only` clauses to name `cairn_ci_paths --apply` as the layer's only writing mode and its guarded PyYAML import as the one non-stdlib import `TestStdlibOnly` admits; `skills/shared/tracking-rules.md`'s git-model bullet says `/cairn-init` §0 offers the edit under a chip where PyYAML is present, else shows the item to add by hand.
- [x] AC5: every file `git grep -ln cairn_ci_paths -- skills scripts hooks README.md CHANGELOG.md cairn/` lists is read, and no line outside `cairn/DECISIONS.md`, `cairn/milestones/archive/`, and this milestone's file describes the script as report-only or writing nothing; §0's bullet states the chip, the dry run, the decline writing nothing, and the uncommitted edit each on one physical line, pinned in `skills/tests/test_ci_paths_note.py` and registered in `test_mutation_harness.py` with `paths-ignore` and `cairn/**` spelled by concatenation, the retired "poses no chip" pin removed from both.
- [x] AC6: from the repo root, `python3 -m unittest discover -s scripts/tests` exits 0 and `python3 -m unittest discover -s hooks/tests` exits 0, their reported counts at or above 350 and 126 (no test lost); the hand-run `python3 -m unittest discover -s skills/tests` reports a count at or above 654 and no failure or error other than `test_lesson_graduation.TestFamilyActuallyLeft.test_partial_coverage_was_trimmed_not_deleted`, the pre-existing red the ROADMAP hygiene stamp records.

## Coverage

- AC1 → T3
- AC2 → T1, T2
- AC3 → T2
- AC4 → T4
- AC5 → T3, T4, T5
- AC6 → T5
- AC2 → T6, T7 (return fixes)
- AC3 → T7 (return fix)
- AC1 → T8 (return fix)
- AC4 → T8 (return fix)
- AC2 → T9 (return 2 fix); AC4 → T9 (return 2 fix)

## Tasks

- [x] T1: Extend `scripts/cairn_ci_paths.py` — argv handling for `--apply [--dry-run]` (`--report --apply` a usage error), a guarded `import yaml` inside the apply path (exit 3 with a PyYAML-naming message when it fails), placement by `yaml.compose` node marks (`start_mark`/`end_mark` of the `on`, `push`, `paths-ignore`, and item nodes; flow-style nodes refused), the insertion writer preserving line endings, the `safe_load` post-edit check (key `True` or `'on'`), the ten refusal reasons, and the module docstring rewritten for two modes; hand-probe each scratch shape and PyYAML-load the result before T2 (M178's parser-oracle lesson; clear `__pycache__` or run under `PYTHONDONTWRITEBYTECODE=1` between edits — M180).
- [x] T2: Write the `apply/` fixture set (12 in/expected pairs per AC3, 9 refusal inputs) and tests in `scripts/tests/test_ci_paths.py` — byte-equality, byte-identity, dry-run agreement, added-lines-only diff, the `safe_load` oracle, the stated skips, the shadowing-`yaml` exit-3 test — retiring `test_apply_writes_nothing` and the `["--apply"]` usage-error case while keeping `--report --apply`; widen `TestStdlibOnly` in `scripts/tests/test_scripts.py:3812-3829` to admit `yaml` for `cairn_ci_paths.py` alone; plant one unquoted `cairn/**` item and one wrong-column insertion to see the suite red before trusting green (M164: control green first).
- [x] T3: Rewrite clause (d) of the §0 bullet at `skills/cairn-init/SKILL.md:44-66` (dry run, chip, decline, apply + re-report, uncommitted edit, exit-3 branch), add the workflow-file exclusion to the §1 commit bullet (`:200`) and the §3 repair commit bullet (`:284`), name the edited files in both close blocks; update `skills/tests/test_ci_paths_note.py` pins and their `test_mutation_harness.py` registrations (`:1970-1995`) — new sentences reworded away from pinned phrases (M148), tokens by concatenation (M169).
- [x] T4: Append the D-entry annotating D-133 (per AC4, with the falsifier); amend the M178 CHANGELOG entry; rewrite the DESIGN.md `scripts/` layer lines (`cairn/DESIGN.md:64-76`); amend the tracking-rules git-model bullet's last sentence (`skills/shared/tracking-rules.md:237-238`).
- [x] T5: Run both gating suites and the hand-run `skills/tests` from the repo root, checking each exit code and count (AC6); run the AC5 file enumeration and read every listed file, dispositioning each report-only phrase; `cairn_validate` green; status → review.
- [x] T6: Fix F1 — `expected_after` uses the `on` key `plan_edit` resolved (plain → `True`, quoted → `'on'`); wrap the post-edit check so any exception refuses with `post-edit check failed`; regression fixture with `"on":` plus a `true:` key, asserting exit 0 and `applied`.
- [x] T7: Fix F2–F5 — refuse a file whose composed `on` → `push` subtree, or `paths-ignore` sequence, is or contains an anchor target, alias, or `<<` merge key (one new closed-list reason, via the step-6 criterion amendment to AC2 and AC3's per-reason fixture); fix `_end_index` for a flow-collection last item (F4) and end-of-file mappings (F6); recognize a block-scalar `cairn/**` item as already ignoring (F7); fixtures for each, control-red first (M164).
- [x] T8: Fix F10 — DESIGN.md `scripts/` layer heading no longer says "reporters — the deterministic read layer"; fix F11 — §0's by-hand suggestion says a scalar or flow-list `on:` is first rewritten in block form before the item can be added; re-run both suites and the hand-run guards; status → review.
- [x] T9: Fix G1 structurally — in `apply_file`, any exception raised while composing the document refuses as `PyYAML cannot parse the file` and any raised later in the plan as `post-edit check failed` (never exit 1; control-red first with the 500-deep nested flow value, M164), with a fixture or test for the deep-nesting input; fix G6 — drop the `step <= 0` pre-emptive raise so `post-edit check failed` is given only by the check; fix G9 — CHANGELOG's refusal list names all twelve reasons, `paths-ignore is not a block sequence` stated as such; at step 6, the gated AC2 amendments for G2 (a final line lacking its ending gains one), G3 (the dry-run agreement clause excepts the unwritable reason), and G5 (a plain `<<` anywhere in the span), with AC3's fixture clause adjusted where the wording moves; re-run both suites and the hand-run guards; status → review.

## Work log

- 2026-09-06: created by /milestone-plan from the `[high]` candidate row (lineage: M178's descoped `--apply`). Criteria audit ran in full mode ([O] reader, user-facing tier): 18 findings, all fixed in the criteria before the gate — the stdlib whitelist rejecting any `import yaml`, M178's `--apply`-is-an-error tests, PyYAML's `True` key for a plain `on`, `push: {}` being both empty and flow, a non-exhaustive refusal list, unstated exit codes, two unvaried fixture axes (quote style, indent step), a two-condition refusal reason, three instrument-bound promises (fixture set, prose pins, test counts), AC5's recalled phrase list as a proxy enumeration, M178's unreleased CHANGELOG entry exempted rather than amended, cairn-init's repair commit staging the workflow file, an unnamed close block, a redundant import probe, and DESIGN's layer heading left contradicting. Inbox sweep: 0 open issues, 0 open PRs. Not the checker-regress shape (a writer over adopter files).
- 2026-09-06: plan gate chose PyYAML as an optional dependency (apply needs it, report does not) over a hard requirement or a vendored parser because the report and by-hand path keep working without it and the plugin stays stdlib elsewhere; falsified by an adopter population where PyYAML is routinely absent so the chip never appears, or by `--report` and `--apply` disagreeing on a file's applicability in practice.
- 2026-09-06: plan gate chose refusing scalar and flow-list `on:` forms over rewriting them into block form (M178's approach) because every applied edit is then a pure insertion and the added-lines-only guarantee holds for all of them; falsified by adopters' workflows using those two forms often enough that the chip rarely applies.
- 2026-09-06: plan gate chose leaving the edited workflow file uncommitted, named in the close block, over including it in the cairn-init commit because a CI-config change is not a docs-only tracking commit; falsified by adopters reporting the uncommitted file forgotten or lost after `/cairn-init`.

- 2026-09-06: T1 written — `--apply [--dry-run]` on PyYAML node marks; probed 17 applying and 12 refusing scratch shapes (CRLF, folded item, trailing comment, push last in `on:`, no trailing newline) and PyYAML-loaded each result. Choices left to implement: a created `paths-ignore:` puts its item one indent step deeper (step read from the file: `push` children minus `push` column, or `push` minus `on` column for a null `push`); a `push` value that is neither a block mapping nor null is refused as `no `push` trigger` (a scalar or block sequence is no trigger GitHub accepts), a flow sequence as `push` holds a flow mapping`. Three suite reds at this checkpoint are the tests T2 retires or widens (`["--apply"]` usage error, `test_apply_writes_nothing`, `TestStdlibOnly`).

- 2026-09-06: T2 — `apply/pairs/` 12 in/expected pairs and `apply/refused/` 9 inputs; 15 apply tests plus the shadowed-`yaml` exit-3 test and a `--report`-without-PyYAML test; `TestStdlibOnly` admits `yaml` for `cairn_ci_paths.py` alone and asserts every other script neither imports nor is allowed it; retired `test_apply_writes_nothing` and the `["--apply"]` usage case, `--report --apply` still asserted. Control (M164): an unquoted item reddened all 12 pair tests, a one-column-off insertion reddened 6 (the post-edit check refused it); both restored. Suites: scripts 366 green, hooks 126 green; T1 checked off here.

- 2026-09-06: T3 — §0 clause (d) rewritten (dry run, one chip in §0's confirmation round or its own, decline writes nothing, apply + re-report, uncommitted edit, refused files keep the by-hand suggestion, exit 3 names PyYAML as the enabler); §1 and §3 commit bullets never stage a chip-edited workflow file; both close blocks name it. `test_ci_paths_note.py`: the `poses no chip` pin retired, 7 new pins (5 harness-registered phrases, 2 structural checks); harness 9 green, note guard 22 green.

- 2026-09-06: T4 — D-135 appended (annotates D-133: `writes nothing` now binds `--report`; PyYAML optional for `--apply` alone; four plan-gate rejections with their reopeners; falsifier: a PyYAML-loads-as-expected edit GitHub reads differently, or PyYAML routinely absent); CHANGELOG's unreleased M178 entry amended to the shipped behaviour; DESIGN `scripts/` layer names `cairn_ci_paths --apply` as the one writing mode and the guarded `import yaml` as the one admitted non-stdlib import; tracking-rules git-model bullet's last sentence amended.
- 2026-09-06: T5 — scripts 366 (exit 0), hooks 126 (exit 0), skills/tests 660 with the one pre-existing lesson-graduation red; `cairn_validate` all checks passed. AC5: the 11 files `git grep -ln cairn_ci_paths` lists read; outside DECISIONS, the archive, and this file every `writes nothing`/`by hand` phrase is scoped to `--report`, `--dry-run`, a decline, or the exit-3 branch (CHANGELOG:39, script docstring, SKILL.md:59/65, tracking-rules:239, the two test files); README names the script nowhere. Status → review.
- 2026-09-06: review return 1 (defect, return floor): AC2 fails — a quoted `"on":` file with a truthy top-level key crashes `--apply` (exit 1) via `expected_after`'s key choice (F1); anchors/aliases/merge keys under `on` let the edit reach `pull_request` or a `paths`-carrying `push` (F2, F3). Tasks T6–T8 added; the new refusal reason takes a gated AC2/AC3 amendment at implement step 6. Status → in-progress. PR #188 stays open as draft.
- 2026-09-06: amendment (substantive, gated): AC2 rewritten — the edit condition adds "no event carrying an anchor, no alias event, and no merge key … between the `on` key's start and its value's end" and "the post-edit check below passes"; the created `paths-ignore:` key goes "after `push`'s last child line"; the post-edit refusal covers a raise in either load, the expected value's construction, or the comparison; the reason list is the check order, defines the two `push` reasons' predicates, and gains `on` holds an anchor, alias, or merge key and `the file cannot be written`; AC3's fixture clause excepts the unwritable reason beside post-edit. Gate choices: refuse anywhere under `on` over the review's push-only span (catches `on: *x`); fold the second reader's three fixes in over leaving a read-only file at exit 1. Read as narrowing (anchored files move from edited to refused; a crash path becomes a named refusal), no criterion added — D-118's recommended direction. AC3's tick (nine refusal inputs) is review's mark and stands for re-review to re-verify against the twelve-reason list.
- 2026-09-06: re-audit: AC2 (full) — five wording gaps folded in (PyYAML has no anchor event, so the clause names an event carrying an anchor; a merge key defined as a plain `<<` key; a raise while constructing the expected value inside the refusal; a plain `on` beside `true:` made a named refusal by "and the post-edit check below passes"; the list declared as check order), one disposed at task level (one fixture per anchor axis — the T7 return set holds them), one logged (AC3's tick).
- 2026-09-06: re-audit: AC2 (full) — second entry on the fixed wording: four findings, three folded in at the user's selection (predicates for `no push trigger` and `push holds a flow mapping`; the created key "after `push`'s last child line"; an unwritable file as a last refusal reason), one disposed at task level (one fixture per anchor mechanism — held in T7's return set; AC3's per-reason rule unchanged). Further churn on AC2 goes to the user (the stop).
- 2026-09-06: re-audit: AC3 (full) — three findings, all disposed at task level with the wording held: post-edit is fixture-reachable (`returns/refused/plain_on_true_key.yml` probes it, the exception only not requiring it); both excepted reasons are probed outside the fixture set (`test_an_unwritable_file_is_refused_after_a_would_apply`, the post-edit return fixture) with exit 0, reason verbatim, and byte-identity; the anchor reason's three mechanisms each have a return fixture (`on_anchor`, `ignore_alias`/`on_alias`, `merge_key_under_push`).
- 2026-09-06: T6 — `plan_edit` returns the key `safe_load` gives `on` (`True` plain, `"on"` quoted) and `expected_after` takes it; the post-edit check refuses on any raise. Return fixture `returns/pairs/quoted_on_true_key` applies; `returns/refused/plain_on_true_key` (plain `on` beside `true:`, the two folding onto one key) refuses as post-edit. Control (M164): 21 reds against the checked-in script — F1 exit 1, F4 refused post-edit, F6/F7 and the anchor shapes applied.
- 2026-09-06: T7 — `_anchor_reach` scans `yaml.parse` events over the span from the `on` key to its value's end (an `on` value composed before its key refused as an alias); `_end_index` reads whether text precedes the end mark on its line (F4 flow last item, F6 no final newline); `_is_entry` matches `cairn/**` in any scalar style (F7); a write raising `OSError` refuses as unwritable (control: exit 1 traceback before the catch). Fixtures: `refused/on_anchor` (AC3's tenth reason), `returns/pairs` 4, `returns/refused` 6; `TestReturnFixtureSet` + `TestApplyReturns` 9 tests, the unwritable test in `TestApply`; ci_paths 39 green.
- 2026-09-06: T8 — DESIGN `scripts/` heading reads "the deterministic scripts layer, one writing mode among its readers" (F10); §0's by-hand suggestion says a scalar or flow-list `on:` is first rewritten in block form before the item can go under it (F11), pinned in `test_ci_paths_note` and registered in the harness; CHANGELOG's refusal list gains the anchor and unwritable reasons. AC5 enumeration re-run: 12 files, no report-only phrase outside the exempt files. Suites: scripts 376 (exit 0), hooks 126 (exit 0), skills/tests 661 with the one pre-existing lesson-graduation red; `cairn_validate` all checks passed. Status → review.
- 2026-09-06: review return 2 (defect, return floor): AC2 fails — a 500-deep nested flow value raises `RecursionError` past the `YAMLError`-only guard, exit 1, later files unprocessed (G1); the second uncaught-raise failure of the exit-0 clause, so thrash trigger (b) fired and the gate chose the structural whole-plan guard over reconsidering the recorded alternatives (a hard PyYAML requirement; a vendored parser). T9 added: guard + G6 + G9; G2/G3/G5 to gated AC2 amendment at implement step 6; G7/G8 to a candidate row; G4/G10–G12 rejected. AC2 tick withdrawn. Defect-return count 2, amendment-return count 0. Status → in-progress. PR #188 stays open as draft.
- 2026-09-06: T9 (code half) — `plan_edit` refuses any raise while composing as `PyYAML cannot parse the file`; `apply_file` refuses any raise past composing as `post-edit check failed`; the `step <= 0` pre-emptive raise dropped (G6: a null-push `? on` file now refuses through the check, the same reason). Control (M164): the checked-in script reddened the deep-nesting test twice (both flags), `RecursionError` confirmed as the raise; G6's test holds the outcome and does not discriminate the fix. CHANGELOG's refusal list names the twelve reasons in check order (G9). `TestApplyReturn2` 2 tests; scripts 378 (exit 0), hooks 126 (exit 0), skills/tests 661 with the one pre-existing red; `cairn_validate` all checks passed. AC2 amendments (G2/G3/G5) pending at the step-6 gate.
- 2026-09-06: amendment (substantive, gated): AC2 rewritten in three clauses at the user's selection of each recommended option — the merge-key parenthetical reads "a plain `<<` scalar (a merge key, wherever in the span it sits)" (G5); the insertion clause adds "a final line lacking its ending gaining one so the inserted lines follow it" (G2); the dry-run clause adds "or refuse as `the file cannot be written` (the write itself being the only writability probe)" (G3). Each narrows or holds the promise (the refusal set grows by a `<<` branch name; a last-line ending and the unwritable disagreement are admitted), no criterion added; the alternatives offered and declined: key-position tracking, a thirteenth refusal reason, a pre-write permission probe. AC3 unchanged — its twelve pairs end in a newline and its dry-run clause covers only them. No re-audit reader spawned: AC2's two `re-audit: AC2` lines are the stop, so the wording went to the user. Script docstring aligned to the three clauses.
- 2026-09-06: T9 done; `cairn_validate` all checks passed; scripts 378 (exit 0), hooks 126 (exit 0), skills/tests 661 with the one pre-existing lesson-graduation red. Status → review.
- 2026-09-06: re-review 3: AC1–AC6 re-verified, AC2 re-ticked; three lenses, 8 findings from [O]; gate fix-now for H1 (import guard widened to any exception, test added), H4 and H6 (prose); H2/H3 to a candidate row; H5/H7/H8 rejected. Defect-return count stays 2, amendment-return count 0. Suites 379/126 green, skills 661 with the one pre-existing red.

## Decisions

## Review

### Acceptance-criteria evidence (2026-09-06, PR #188, branch head 92b0793 over origin/main 2026-09-06)
- AC1 ✓: `skills/cairn-init/SKILL.md` §0 bullet (lines 44–78) read: clause (d) runs `--apply --dry-run`, poses one approve/decline chip naming the `would apply` files in §0's confirmation round or its own, a decline writes nothing, approval runs `--apply`, re-runs `--report`, and states the files are left uncommitted; a refused file keeps the by-hand suggestion for a `push` verdict lacking both `cairn/**` and `paths`; exit 3 → by-hand stands and installing PyYAML enables the edit. §1 commit bullet (:213–216) and §3 repair bullet (:301–304) exclude `.github/workflows/*.yml`, `*.yaml` by name; both close blocks (:222–224, :309–311) name the edited file. Prose guard `test_ci_paths_note` 22 green.
- AC2 ✓: hand probe on a temp git root (a.yml = bare_push, b.yaml = push_paths): `--report --apply` → usage, exit 2; `--apply --dry-run` → `a.yml: would apply`, `b.yaml: refused: \`push\` carries \`paths\``, exit 0, a.yml md5 unchanged; shadowed `yaml` on PYTHONPATH → exit 3, stderr names PyYAML, empty stdout, md5 unchanged; `--apply` → a.yml applied (diff: 2 added lines `paths-ignore:` + `- 'cairn/**'` at the push children's column), b.yaml refused, exit 0; `--report` afterwards shows `push (paths-ignore, cairn/**)`. `import yaml` occurs once, inside `apply_files` (:500); module imports are os/re/sys. `--report` output of the branch script equals origin/main's over all 61 fixture files (28 report + 33 apply). The ten `R_*` reason constants match AC2's list verbatim. `TestStdlibOnly.test_yaml_is_admitted_for_ci_paths_alone` green within scripts 366.
- AC3 ✓: `apply/pairs/` holds 12 in/expected pairs whose stems are AC3's twelve forms (bare_push, push_branches, ignore_deeper, ignore_flush, comment_in_block, comment_column0, quoted_on_double, quoted_on_single, indent4_bare_push, crlf, pr_own_ignore, third_key_between); `apply/refused/` holds 9 inputs, one per reason except post-edit. `test_ci_paths.py` (29 tests, green): byte-equal apply per pair, byte-identical refusal with reason, dry-run identity + `would apply` for exactly the pairs, added-lines-only unified diff, safe_load oracle plus a discriminating negative, second-apply refuses as already-ignoring, shadowed-`yaml` exit 3 with PyYAML message and byte-identity, `--report` still runs without PyYAML. With `yaml` shadowed the module reports OK (skipped=11) with the 'PyYAML not importable' message. `test_apply_writes_nothing` and the `["--apply"]` usage case are gone from the diff; `["--report", "--apply"]` still asserted exit 2.
- AC4 ✓: D-135 read whole — annotates D-133, narrows 'writes nothing' to `--report`, records PyYAML as an optional dependency of `--apply` alone with four plan-gate rejections and the overturning observation class (a PyYAML-loads-as-expected edit GitHub reads differently; PyYAML routinely absent). CHANGELOG diff amends the unreleased M178 entry in place, no second entry. DESIGN diff drops `(stdlib)` and `Read-only`, names `cairn_ci_paths --apply` the layer's one writing mode and its guarded `import yaml` the one non-stdlib import `TestStdlibOnly` admits. tracking-rules:237–239 says §0 offers the edit under a chip where PyYAML is present, else shows the item to add by hand.
- AC5 ✓: `git grep -ln cairn_ci_paths -- skills scripts hooks README.md CHANGELOG.md cairn/` lists 12 files (README absent), each read. Outside DECISIONS, the archive, and this file every writes-nothing/read-only/by-hand line is scoped: CHANGELOG:39 (`--report`), DESIGN:73 (every script except `--apply`), script docstring :29/:32/:47 (`--report`, exit 3, `--dry-run`), SKILL.md:65 (a decline), test_ci_paths.py:12/:435 (dry run, exit 3). Pins: dry-run command, chip, decline, uncommitted edit, PyYAML enabler in `test_ci_paths_note.py` with `IGNORE = "paths-" + "ignore"`, `GLOB = "cairn/" + "**"` (:28–29), registered at test_mutation_harness.py:2021–2051; 'poses no chip' occurs in neither file nor SKILL.md (grep 0/0/0); the note guard's raw-file assertIn passing shows each phrase on one physical line.
- AC6 ✓: from the repo root, `python3 -m unittest discover -s scripts/tests` Ran 366, OK, exit 0; `hooks/tests` Ran 126, OK, exit 0; hand-run `skills/tests` Ran 660, one failure — `test_lesson_graduation.TestFamilyActuallyLeft.test_partial_coverage_was_trimmed_not_deleted`, the pre-existing red the ROADMAP stamp records — no other failure or error.

### Consistency gate (2026-09-06)
- `cairn_validate.py`: all checks passed, exit 0 (16 PASS, 7 OK advisories; `release window` did not fire).
- `cairn_impact.py --changed`: skipped — Principles touched `—`, no IP/GP changed.
- Toolchain checks: profile `generic`, `consistency-gate` slot names none — clean no-op; the two `verify` suites are AC6's evidence.
- Driving RR `—`: projection-vs-outcome no-ops.

### Independent review (2026-09-06, three lenses, fresh context)
- [S] prior-review-record: no prior-review evidence of a regression; M178's three missed forms (comment in block, flush-left items, column-0 comment) each covered by a named `apply/pairs/` fixture; `gh api pulls/comments` probe empty repo-wide, walk skipped; zero findings.
- [S] blame-history: no conflicts with M178, D-133/D-135, the stdlib rule, or the M148/M169 pin lessons. One observation: the in-place amendment of the unreleased M178 CHANGELOG entry has no precedent in this repo's history — noted; AC4 called for it and IP4's append-only scope names work-logs and DECISIONS, not the changelog.
- [O] diff-bug: 12 findings, ranked; F1–F3 reproduced by the session before triage.
  - F1 (defect return, fails AC2 "exits 0 whenever PyYAML imported"): `expected_after` picks the `True` key whenever any top-level key loads truthy (`true:`, `yes:`, `1:`) even when `on` is quoted — `"on":\n  push: …\ntrue: x` → AttributeError, exit 1, no further file processed. Fix: use the key `plan_edit` resolved; any exception in the check → `post-edit check failed`.
  - F2 (fix with the return, load-bearing): `push: &p` aliased by `pull_request: *p` — the inserted item lands inside the anchor and reaches `pull_request`; the post-edit check passes because safe_load shares the object. Violates Scope Out (pull_request untouched) and the chip's "nothing else".
  - F3 (fix with the return, load-bearing): `paths` reaching `push` through a merge key `<<: *b` is not seen by `_mapping_get`; the file is edited to carry both `paths` and `paths-ignore`.
  - F4 (fix with the return): a flow-collection last `paths-ignore` item (`- {a: b}`, `- [x, y]`) is placed before the last item — `_end_index` returns `end_mark.line` for non-scalars; caught only by the post-edit check, so `post-edit check failed` is reachable from an editable input.
  - F5 (fix with the return): `paths-ignore: *pi` aliasing a top-level sequence — marks point at the anchor, insertion aimed at the wrong site, refused `post-edit check failed` with a misleading reason.
  - F6 (fix with the return, same `_end_index` site): a file with no trailing newline gets the created `paths-ignore:` before `branches:` — semantically equal, check passes; no pair fixture omits the final newline.
  - F7 (fix with the return, trivial): a block-scalar item `>-\n  cairn/**` is not recognized as already ignoring, so a duplicate is appended.
  - F8 (reject): `--apply` exits 0 with "no workflow files" when the directory is empty and PyYAML is absent — no file could be edited, so no chip and no exit-3 note are owed.
  - F9 (reject): `TestStdlibOnly` admits `yaml` file-wide rather than apply-path-only; the placement is enforced by `test_report_still_runs_without_pyyaml` and verified by read (AC2 evidence).
  - F10 (fix with the return, docs): DESIGN.md:64 still heads the layer "reporters — the deterministic read layer" two lines above the writing mode.
  - F11 (fix with the return, docs): the by-hand suggestion for a refused scalar/flow-list `on:` never says the trigger must first be written in block form — pre-existing M178 wording made load-bearing now that those forms are exactly the refusals.
  - F12 (reject): unreadable or non-UTF-8 file refused as `PyYAML cannot parse the file` — inside the closed reason list, pinned deliberately by `test_an_unreadable_file_is_refused`.
- Defect-return count for M181: 1 (this return). Amendment-return count: 0. F2–F5's refusal of anchors, aliases, and merge keys under `on` adds a reason to AC2's closed list, so it goes through the gated criterion amendment at `/milestone-implement` step 6 (AC2's list and AC3's one-refusal-per-reason fixture set), not a review-side patch.
- AC2 tick withdrawn 2026-09-06 at return 1 (F1 refutes its exit-0 clause); AC1, AC3–AC6 ticks stand on their recorded evidence and are re-verified at re-review.

### Acceptance-criteria evidence (re-review after return 1, 2026-09-06, PR #188, branch head 904a577 over origin/main, unmoved)
- AC1 ✓: `skills/cairn-init/SKILL.md` §0 bullet (:43–78) read: clause (d) runs `--apply --dry-run`; a `would apply` line poses one approve/decline chip naming those files, in §0's confirmation round or its own; a decline writes nothing; approval runs `--apply`, re-runs `--report`, and states the files are left uncommitted; a refused file keeps the by-hand suggestion for a `push` verdict lacking both `cairn/**` and `paths` (now saying a scalar or flow-list `on:` is first rewritten in block form); exit 3 → by-hand stands, installing PyYAML enables the edit. §1 commit bullet (:214–218) and §3 repair bullet (:302–306) exclude `.github/workflows/*.yml`, `*.yaml` by name; both close blocks (:219–226, :308–312) name the edited file. `test_ci_paths_note` 23 green, mutation harness 9 green.
- AC2 ✓: hand probe on a temp git root (a.yml bare_push, b.yaml push_paths, c.yml quoted `"on":` beside `true:` — return 1's F1): `--report --apply` → usage, exit 2; `--apply --dry-run` → a/c `would apply`, b `refused: \`push\` carries \`paths\``, exit 0, all three md5-unchanged; shadowed `yaml` on PYTHONPATH → exit 3, message names PyYAML, files unchanged; `--apply` → a and c byte-equal to their expected fixtures (a: 2 added lines at the push children's column), b byte-identical, exit 0; `--report` after shows `push (paths-ignore, cairn/**)`; a second `--apply` refuses all three (already ignores). All 16 refusal fixtures (10 + 6 return) refuse with their named reason, exit 0, byte-identical; a read-only file refuses `the file cannot be written`, exit 0. Return-1 reach shapes: `push: &p` aliased by `pull_request: *p`, `<<: *b` under `push`, and `on: *o` composed before its key each refuse as `on` holds an anchor, alias, or merge key; a flow-collection last item gets the item after it; a file without a final newline applies; a block-scalar `cairn/**` item refuses as already ignoring; CRLF output byte-equal to its fixture. `plan_edit` (:424–484) raises the reasons in AC2's list order; `import yaml` occurs once, inside `apply_files` (:543); module imports are os/re/sys. `--report` output and exit equal origin/main's over all 76 fixture files. `TestStdlibOnly` (`PER_SCRIPT = {"cairn_ci_paths.py": {"yaml"}}`, :3820) admits `yaml` there alone and asserts every other script neither imports nor is allowed it; green within scripts 376.
- AC3 ✓: `apply/pairs/` holds 12 in/expected pairs whose stems are AC3's twelve forms (bare_push, push_branches, ignore_deeper, ignore_flush, comment_in_block, comment_column0, quoted_on_double, quoted_on_single, indent4_bare_push, crlf, pr_own_ignore, third_key_between); `apply/refused/` holds 10 inputs, one per reason except post-edit and unwritable (`on_anchor` the tenth); `apply/returns/` adds 4 pairs and 6 refusals from return 1. `test_ci_paths.py` 39 tests green with PyYAML: byte-equal apply per pair, byte-identical refusal with reason, dry-run identity plus `would apply` for exactly the pairs, added-lines-only unified diff, the safe_load oracle with a discriminating negative, second-apply refuses, the unwritable refusal, the alias-reaching-`pull_request` refusal; with `yaml` shadowed the module reports OK with 19 skips carrying the 'PyYAML not importable' messages, `test_shadowed_yaml_module_exits_3` asserting exit 3, the PyYAML message, and byte-identity. An independent oracle this session (unified diff + safe_load equality) confirms all 12 AC3 pairs added-lines-only and load-equal; the 2 EOF return pairs differ only by the last line gaining its newline. `test_apply_writes_nothing` and the `["--apply"]` usage case are removed in the diff; `["--report", "--apply"]` still asserted exit 2 (:293).
- AC4 ✓: D-135 (DECISIONS:4967–5007) read whole — annotates D-133 ("annotated, not superseded"), narrows 'writes nothing' to `--report`, records PyYAML as an optional dependency of `--apply` alone with four plan-gate rejections and the overturning observation class. CHANGELOG diff amends the one unreleased M178 entry in place (its heading now "adds the ignore under a chip where PyYAML is present, else shows it to add by hand"; the refusal list carries the anchor and unwritable reasons), no second entry. DESIGN diff: the `scripts/` heading reads "the deterministic scripts layer, one writing mode among its readers" (F10 fixed), `(stdlib)` and `Read-only` dropped, `cairn_ci_paths --apply` named the one writing mode and its guarded `import yaml` the one non-stdlib import `TestStdlibOnly` admits. tracking-rules:237–239: §0 reports the fact and offers the edit under a chip where PyYAML is present, else shows the item to add by hand.
- AC5 ✓: `git grep -ln cairn_ci_paths -- skills scripts hooks README.md CHANGELOG.md cairn/` lists 12 files (README absent), each read. Outside DECISIONS, the archive, and this file every writes-nothing/read-only/by-hand line is scoped: CHANGELOG:41 (`--report`), DESIGN:73 ("except `cairn_ci_paths --apply`"), script docstring :29/:32/:54 (`--report`, exit 3, `--dry-run`), :547 (the exit-3 message), SKILL.md:59/:65 (dry run, a decline), test_ci_paths.py:12/:456/:583 (dry run, the unwritable test, exit 3). `test_ci_paths_note.py` pins the dry-run command, the chip, the decline, the uncommitted edit, the PyYAML enabler, and the block-form rewrite each on one physical line (raw-file assertIn), with `IGNORE = "paths-" + "ignore"`, `GLOB = "cairn/" + "**"` (:28–29); 10 harness registrations at test_mutation_harness.py:1974–2030; 'poses no chip' occurs in neither test file nor SKILL.md (0/0/0).
- AC6 ✓: from the repo root, `python3 -m unittest discover -s scripts/tests` Ran 376, OK, exit 0; `hooks/tests` Ran 126, OK, exit 0; hand-run `skills/tests` Ran 661, one failure — `test_lesson_graduation.TestFamilyActuallyLeft.test_partial_coverage_was_trimmed_not_deleted`, the pre-existing red the ROADMAP stamp records — no other failure or error.

### Consistency gate (re-review, 2026-09-06)
- `cairn_validate.py`: all checks passed, exit 0 (16 PASS, 7 OK advisories; `release window` did not fire).
- `cairn_impact.py --changed`: skipped — Principles touched `—`, no IP/GP changed.
- Toolchain checks: profile `generic`, `consistency-gate` slot names none — clean no-op; the two `verify` suites are AC6's evidence.
- Driving RR `—`: projection-vs-outcome no-ops.

### Independent review (re-review, 2026-09-06, three lenses, fresh context)
- [S] prior-review-record: no prior-review evidence of a regression; M178's three missed forms stay confined to `--report` and are covered by named `apply/pairs/` fixtures; F1–F7, F10, F11 verified fixed in the diff, F8/F9/F12 unchanged as rejected; `gh api pulls/comments` probe empty, walk skipped; zero findings.
- [S] blame-history: no conflicts — `--report`'s line reader untouched, `--apply` never calls it (M178's parser-oracle lesson honoured); `TestStdlibOnly` narrowed to the one admitted exception; D-133's retained clauses reaffirmed in D-135; only the `poses no chip` pin retired; §0 clauses (a)–(c) byte-identical. Observation repeated from round one: the in-place CHANGELOG amendment has no precedent (AC4 called for it).
- [O] diff-bug: 12 findings, ranked; G1–G6 and G9 reproduced by the session before triage.
  - G1 (defect return 2, return floor — fails AC2 "exits 0 whenever PyYAML imported"): `yaml.compose_all` is guarded by `except yaml.YAMLError` alone (:431–434), so a `RecursionError` from a 500-deep nested flow value escapes — traceback, exit 1, later files unprocessed. Second failure of the same clause by the same shape (an uncaught raise; F1 was the first): the remedy is structural — any raise while composing refuses as `PyYAML cannot parse the file`, any raise later in the plan as `post-edit check failed` — never another mechanism-by-mechanism catch.
  - G2 (amendment-shaped, to AC2 at implement step 6 with the return): a file without a final newline gets that newline appended to its last line (:496–497) before the inserted lines, so a `keepends` diff shows the last line changed — against "inserts lines only", CHANGELOG's "lines added only", and D-135's "pure insertion"; the return-pair test compares with endings stripped (:541–553). Proposed: AC2 states that a final line lacking its ending gains one (the alternative, refusing such files, widens the closed reason list).
  - G3 (amendment-shaped, to AC2 with the return): `--dry-run` prints `would apply` for a read-only file `--apply` then refuses as unwritable — against "`would apply` exactly for the files `--apply` would edit", while `test_an_unwritable_file_is_refused_after_a_would_apply` (:455), accepted at the return-1 re-audit, asserts that disagreement. Proposed: the dry-run clause excepts the unwritable reason (a writability probe before the write is unreliable and not recommended).
  - G4 (reject): duplicate `push` keys under `on` edit the last one even when the first carries `paths` — a duplicate mapping key is invalid YAML that GitHub refuses, so the file is outside what the criterion's "a `push` key" quantifies over; the reverse order refuses correctly.
  - G5 (amendment-shaped or fix, to AC2 with the return): `_anchor_reach` (:393) refuses a plain `<<` scalar anywhere in the span, AC2 defines a merge key as one "in a mapping's key position", so a branch literally named `<<` is refused though editable by the criterion. Proposed: amend AC2 to "anywhere between the `on` key's start and its value's end" — refusal is the safe direction and key-position tracking over the event stream buys nothing for a real workflow; the implement gate may instead keep the wording and track key position.
  - G6 (fix with the return): a complex-key `? on` file refuses as `post-edit check failed` without the check running (:481–482, `step <= 0`); fix: drop the pre-emptive raise and let the check decide, so the reason is true when given.
  - G7 (follow-up, candidate row): a quoted `"push":` key — `--report` prints `unrecognized`, `--apply --dry-run` prints `would apply`; the post-approval `--report` re-run then never shows the edit. `--report`'s stdlib reader is Scope Out ("switching `--report` to PyYAML"); the disagreement class is D-135's own reopener, recorded as its promotion condition.
  - G8 (follow-up, same row): a block-scalar `cairn/**` item — `--report` prints `push (paths-ignore)` while `--apply` refuses as already ignoring, so §0's by-hand suggestion shows an item already present; the same reader divergence.
  - G9 (fix with the return, docs): CHANGELOG's refusal list names nine of the twelve reasons (`no on key`, `no push trigger`, `post-edit check failed` absent) and "a flow-sequence `paths-ignore`" understates `paths-ignore is not a block sequence` (a null `paths-ignore:` also refuses) — the derived-claims rule; AC4.
  - G10 (reject): a comment trailing `push`'s last child ends up after the inserted lines — lines only added, content unchanged, semantically inert.
  - G11 (reject): no test pins the refusal check order — the order was verified by read this review; a test on it is checker hardening past the stakes.
  - G12 (reject): unused `ignore_key` binding (:463) — linter-class.
- Thrash rule: defect-return count for M181 becomes 2 with G1; amendment-return count 0. Trigger (b) fires — AC2's exit-0 clause failed at return 1 (F1, an AttributeError) and again here (G1, a RecursionError), each an uncaught raise. The plan gate's recorded alternatives (PyYAML as a hard requirement; a vendored parser) do not bear on exception handling, so reconsidering them is offered, not recommended; the structural whole-plan guard above is the remedy that closes the shape. Trigger (a) does not fire (a third return would).
- conversation: PR #188 — empty (no reviews, no comments, no unresolved threads); nothing to triage.
- AC2 tick withdrawn 2026-09-06 at return 2 (G1 refutes its exit-0 clause); AC1, AC3–AC6 ticks stand on their re-recorded evidence.

### Acceptance-criteria evidence (re-review after return 2, 2026-09-06, PR #188, branch head db04c78 over origin/main, unmoved)
- AC1 ✓: `skills/cairn-init/SKILL.md` §0 bullet (:43–78) read: clause (d) runs `--apply --dry-run`; a `would apply` line poses one approve/decline chip naming those files, in §0's confirmation round or its own; a decline writes nothing; approval runs `--apply`, re-runs `--report`, and states the files are left uncommitted; a refused file keeps the by-hand suggestion for a `push` verdict lacking both `cairn/**` and `paths`; exit 3 → by-hand stands, installing PyYAML enables the edit. §1 commit bullet (:214–218) and §3 repair bullet (:302–306) exclude `.github/workflows/*.yml`, `*.yaml` by name; both close blocks (:219–226, :308–312) name the edited file. `test_ci_paths_note` 23 green, mutation harness 9 green.
- AC2 ✓: hand probe on a temp git root (a bare_push, b push_paths, c quoted `"on":` beside `true:`, d `branches` with no final newline, e a 500-deep nested flow value — return 2's G1): `--report --apply` → usage, exit 2; `--apply --dry-run` → a/c/d `would apply`, b `refused: \`push\` carries \`paths\``, e `refused: PyYAML cannot parse the file`, exit 0, all five md5-unchanged; shadowed `yaml` on PYTHONPATH → exit 3, message names PyYAML, files unchanged; `--apply` → a and c byte-equal to their expected fixtures, d's last line gains its newline with the two inserted lines after it (od read), b and e byte-identical, e's refusal not stopping later files, exit 0; `--report` after shows `push (paths-ignore, cairn/**)` for a/c/d; a second `--apply` refuses a/c/d as already ignoring. Amended-clause probes: a branch literally named `<<` refuses as `on` holds an anchor, alias, or merge key (G5); a complex-key `? on` file refuses as `post-edit check failed` with no pre-emptive raise in `plan_edit` (G6); a null `paths-ignore:` refuses as not a block sequence; all byte-identical, exit 0. `plan_edit` (:429–484) wraps `compose_all` in `except Exception` → parse reason and raises the remaining reasons in AC2's list order; `apply_file` (:514–544) refuses any later raise as post-edit and an `OSError` write as unwritable; `import yaml` occurs once, inside `apply_files` (:548); module imports are os/re/sys. `--report` output and exit equal origin/main's over all 76 fixture files. `TestStdlibOnly` (`PER_SCRIPT = {"cairn_ci_paths.py": {"yaml"}}`, :3820) admits `yaml` there alone and asserts every other script neither imports nor is allowed it; green within scripts 378. The dry-run/unwritable disagreement is asserted by `test_an_unwritable_file_is_refused_after_a_would_apply` (:455), green.
- AC3 ✓: `apply/pairs/` holds 12 in/expected pairs whose stems are AC3's twelve forms; `apply/refused/` holds 10 inputs, one per reason except post-edit and unwritable; `apply/returns/` adds 4 pairs and 6 refusals. `test_ci_paths.py` 41 tests green with PyYAML (byte-equal apply per pair, byte-identical refusal with reason, dry-run identity plus `would apply` for exactly the pairs, added-lines-only diff, the safe_load oracle with a discriminating negative, second-apply refuses, the unwritable refusal, the alias-reaching-`pull_request` refusal, the deep-nesting refusal with later files still run, the complex-key check); with `yaml` shadowed the module reports OK with 21 skips carrying the 'PyYAML not importable' message, `test_shadowed_yaml_module_exits_3` (:630) asserting exit 3, the PyYAML message, and byte-identity. An independent oracle this session (endings-stripped unified diff, safe_load equality, CRLF presence preserved) confirms all 16 pairs added-lines-only and load-equal, the 2 EOF return pairs differing only by the last line's gained ending. `test_apply_writes_nothing` and the `["--apply"]` usage case are absent (`--apply` appears only as a run flag, :305/:595/:637); `["--report", "--apply"]` still asserted exit 2 (:293).
- AC4 ✓: D-135 read whole — annotates D-133 ("annotated, not superseded"), narrows 'writes nothing' to `--report`, records PyYAML as an optional dependency of `--apply` alone with four plan-gate rejections and the overturning observation class. CHANGELOG diff amends the one unreleased M178 entry in place, its refusal list naming the twelve reasons in check order with `paths-ignore` not a block sequence stated as such (G9), no second entry. DESIGN diff: the `scripts/` heading reads "the deterministic scripts layer, one writing mode among its readers", `(stdlib)` and `Read-only` dropped, `cairn_ci_paths --apply` named the one writing mode and its guarded `import yaml` the one non-stdlib import `TestStdlibOnly` admits. tracking-rules:237–239: §0 reports the fact and offers the edit under a chip where PyYAML is present, else shows the item to add by hand.
- AC5 ✓: `git grep -ln cairn_ci_paths -- skills scripts hooks README.md CHANGELOG.md cairn/` lists 12 files (README absent), each read. Outside DECISIONS, the archive, and this file every writes-nothing/read-only/by-hand line is scoped: CHANGELOG:44 (`--report`), DESIGN:73 ("except `cairn_ci_paths --apply`"), script docstring :29/:32/:58 (`--report`, exit 3, `--dry-run`), :552 (the exit-3 message), SKILL.md:59/:65 (dry run, a decline), test_ci_paths.py:12/:456/:628 (dry run, the unwritable test, exit 3). `test_ci_paths_note.py` pins the dry-run command, the chip, the decline, the uncommitted edit, the PyYAML enabler, and the block-form rewrite each on one physical line (raw-file assertIn), with `IGNORE = "paths-" + "ignore"`, `GLOB = "cairn/" + "**"` (:28–29); harness registrations at test_mutation_harness.py:1974–2099 spell both tokens by concatenation; 'poses no chip' occurs in neither test file nor SKILL.md (0/0/0).
- AC6 ✓: from the repo root, `python3 -m unittest discover -s scripts/tests` Ran 378, OK, exit 0; `hooks/tests` Ran 126, OK, exit 0; hand-run `skills/tests` Ran 661, one failure — `test_lesson_graduation.TestFamilyActuallyLeft.test_partial_coverage_was_trimmed_not_deleted`, the pre-existing red the ROADMAP stamp records — no other failure or error.

### Consistency gate (re-review after return 2, 2026-09-06)
- `cairn_validate.py`: all checks passed, exit 0 (16 PASS, 7 OK advisories; `release window` did not fire).
- `cairn_impact.py --changed`: skipped — Principles touched `—`, no IP/GP changed.
- Toolchain checks: profile `generic`, `consistency-gate` slot names none — clean no-op; the two `verify` suites are AC6's evidence.
- Driving RR `—`: projection-vs-outcome no-ops.

### Independent review (re-review after return 2, 2026-09-06, three lenses, fresh context)
- [S] prior-review-record: no prior-review evidence of a regression; M178's parser-oracle lesson honoured (`--apply` uses PyYAML's composer, `--report`'s reader untouched); F1–F12 and G1–G12 each verified fixed or unchanged as rejected; `gh api pulls/comments` probe empty, walk skipped; zero findings.
- [S] blame-history: no conflicts — `TestStdlibOnly` narrowed by one named exception (D-135), D-133 annotated consistently across DECISIONS/DESIGN/CHANGELOG/tracking-rules, M148/M169 pin lessons honoured, IP4's scope excludes the CHANGELOG amendment AC4 called for; zero findings.
- [O] diff-bug: ~70 adversarial shapes and a 4,000-case mutation fuzz, 0 crashes, 0 removed lines; 8 findings, ranked; H1, H2, H4 reproduced by the session before triage. Gate triage (user's selection of each recommended option):
  - H1 (fix now at the gate): `import yaml` guarded by `except ImportError` alone, so an install whose import raises anything else (a shadow `yaml.py` raising `ValueError`) escapes as a traceback, exit 1 — the one site T9's whole-plan guard did not cover. Fixed: `except Exception` at the import; `test_a_yaml_module_raising_anything_else_exits_3` asserts exit 3, the PyYAML message, no traceback, empty stdout, and byte-identity (control: the checked-in script gave exit 1 with a traceback on the same shadow before the fix); docstring aligned. Treated as a gate fix, not defect return 3: the Goal and Scope name the exit-3 branch as "PyYAML absent", which was already caught; the defect-return count stays 2.
  - H2 (follow-up, candidate row): an explicit null `push` (`push: null`, `push: ~`) has its created key placed after a line already carrying a value and refuses as `post-edit check failed` — safe, byte-identical, the reason naming nothing actionable, though AC2's "null scalar" names it editable.
  - H3 (follow-up, same row): `_first_content_line_before` walks back into a block scalar whose last line opens with `#`, refusing as post-edit — a safe refusal of an editable shape.
  - H4 (fix now, docs): CHANGELOG:26 "lines added only" and §0's chip sentence "nothing else" lacked G2's final-line-ending exception (the derived-claims rule; AC4). Fixed in both; D-135 is history and stands.
  - H5 (reject): the refusal-coverage test asserts over literals in its own file — checker hardening past the stakes, the G11 call.
  - H6 (fix now, docs): a declined chip showed no by-hand item, the suggestion being scoped to refused files; §0's chip sentence now says the by-hand suggestion stands for the declined files too.
  - H7 (reject): the `if not seq.value` branch at :474 is unreachable — linter-class (G12).
  - H8 (reject): duplicate-key files are edited at the last key — invalid YAML GitHub refuses, outside AC2's domain (G4).
- Post-fix: scripts 379 (exit 0), hooks 126 (exit 0), skills/tests 661 with the one pre-existing red; note guard and harness green; `cairn_validate` all checks passed.
- conversation: PR #188 — empty (0 reviews, 0 comments, 0 unresolved threads); nothing to triage.
- Thrash rule: defect-return count for M181 stays 2; amendment-return count 0. Trigger (b)'s shape (an uncaught raise against AC2) recurred at the import site and was closed by the same structural remedy as T9's, under the gate's fix-now selection.
