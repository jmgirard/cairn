# M170: Waiting on CI and background work follows a tested rule

**Status:** done (2026-09-02, PR #173 https://github.com/jmgirard/cairn/pull/173)

**Goal:** Replace the rulebook's untested "one blocking wait" rule with a wait rule derived from an experiment over the harness's actual wait mechanisms, so sessions stop leaving stale or runaway watchers behind.

**Outcome:** `cairn/references/wait-mechanisms.md` — a first-hand synthesis note (W1–W23) from a scratch repo `jmgirard/cairn-wait-lab` with a sleep-60/360/720 matrix and a FAIL-file gate: a foreground Bash call at the 600 000 ms ceiling is moved to the background, not killed; `run_in_background` ignores its `timeout`; a Monitor is killed at `timeout_ms` and says so; `--fail-fast` ends a red wait in 17 s; a no-checks PR exits 1 at once; `TaskStop` ends a live watcher and finds a finished one gone; `/clear` survival, `-p` teardown, and `--resume` recorded as documented-not-observed with doc URLs. tracking-rules "Waiting on CI and background work" replaces the old paragraph — one watcher per wait; foreground `gh pr checks --watch --fail-fast` with a timeout below the ceiling for CI; `run_in_background` for a local command that will not fit; a subagent awaited by its own notification; on timeout report fresh state, log, `TaskStop`, stop; no watcher armed at a commit, turn end, or `/clear` point. `skills/cairn-release`, `skills/hotfix`, `skills/milestone-review` restate mechanism and stop point; `cairn/PROFILE.md` no-checks exit corrected to 1; hand-run pin `skills/tests/test_wait_rule.py` (wrap-tolerant regexes, two mutation entries).

**Decisions:** D-128 (supersedes the one-blocking-wait rule; the overturning observation is a watcher outliving a stop point under the new rule). Plan gate: scratch synthetic matrix over a bsync live PR; one rule spanning CI, local commands, and subagents; prose plus pin over a validator or hook.

**Review:** one pass. AC1–AC3 verified; three-lens fan-out — blame-history and prior-review clean (PR-comments probe empty); diff-bug 14 findings: F1 (stop-point premise "ends only at completion or `TaskStop`" contradicted W11 — an AC2 failure, fixed at the gate and merged as a user-accepted logged deviation from the return floor), F2 (`/clear` "documented to survive" overstated W20), F3 (two sites silent on the session stopping after a timeout), F4 (`TaskStop` on a timed-out Monitor), F5 (note Disposition mis-landed W21–W23), F7 (PROFILE correction kept the wrong figure readable), F9 (W3 mixed tag), F10 (auto-background stated without W3's killed exceptions), F11 (cairn-release `TaskStop` on a foreground call) fixed on-branch; F6, F8, F12 rejected (planned pin count, URLs resolve via the snapshot, sweep bound by design); F13 no change; F14 refuted by `gh pr checks 173` exiting 1 on this repo. Merge hit a GitHub 502 and a stuck "merge already in progress"; landed on the third attempt. Hygiene: one LESSONS line; nothing graduated or retired; scratch repo left for the user to delete.
