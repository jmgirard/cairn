# M185: Guest collaboration mode — fork-aware remotes and review handoff

**Status:** done (2026-09-10, PR #192 https://github.com/jmgirard/cairn/pull/192)

**Goal:** In guest mode the branch is cut from and the PR targets the upstream repo, and review ends by handing the PR to the maintainers, the milestone reaching `done` once they merge.

**Outcome:** `cairn_common.base_remote(cwd)` — `upstream` in guest mode when `git remote` lists it, else `origin` — and `default_branch` reads `refs/remotes/<base>/HEAD` with the `ls-remote` fallback on `<base>` (five `TestBaseRemote` tests); `cairn_impact._base_commit` tries `<base>/HEAD` first. The rulebook's detection recipe names `<base>` and the `<base-repo>`/`<fork-owner>` slug recipe (`gh repo view … --json nameWithOwner`); its Collaboration-mode section states the base remote and the never-merge handoff, and the transition line admits `blocked → done` for a merged guest handoff. `/milestone-implement` and `/hotfix` cut from `<base>/<default-branch>` with a fetch-only sync (rebase + `--force-with-lease` to the fork for milestone branches); `/milestone-review` and `/hotfix` push to the fork, open a draft PR with `gh pr create --repo <base-repo> --head <fork-owner>:<branch> --draft`, put `--repo <base-repo>` on every `gh pr` read, and end with a handoff gate in the merge gate's shape (hand off via `gh pr ready`, or decline; no merge option), review setting `blocked` with the line `blocked: PR #<N> awaits the maintainers of <base-repo>` and its hygiene running on disk with no commit. `/milestone` §2 routes a `blocked` milestone with a PR by `gh pr view --json state,reviewDecision` (MERGED → review hygiene; CLOSED → chip; `CHANGES_REQUESTED` → implement; OPEN → stay blocked) and skips the inbox enumeration in guest mode. `r-package.md`'s consistency gate justifies the `cairn` directory NOTE by the mode. CHANGELOG's guest-mode entry extended.

**Decisions:** none milestone-local; `blocked` reused for the handed-off state, never merging via cairn, and the justified NOTE are plan-gate choices logged in the work log under D-137.

**Review:** three-lens fan-out. Blame-history and prior-review lenses clean. Diff-bug lens: 11 findings — 7 fixed at the gate (`/hotfix` guest `--repo` on every `gh pr` read, merged-PR re-entry sync from `<base>`, guest hotfix PR opened as a draft; review step-9 issue-close passage pointed at the guest arm; `/milestone` inbox skipped in guest mode; two CHANGELOG clauses; `cairn_impact` base ref), 4 rejected with reasons (cairn-init bullet order; `default_branch` profile read on the push hot path; ff-only onto a diverged local default branch; `hotfix-` prefix under the vocabulary rule). No defect returns. Hygiene retired the candidate row "`/hotfix` guest arm" (M184 review finding 1), which this milestone shipped.
