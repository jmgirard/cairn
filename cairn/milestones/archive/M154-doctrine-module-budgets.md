# M154: The maturation exit's doctrine modules gain budgets

**Status:** done (2026-08-22, PR #155 https://github.com/jmgirard/cairn/pull/155)

**Goal:** A lesson family graduating through the maturation exit lands in a
module with a stated size budget the repo's hygiene passes read — closing the
gap circumplex M104 measured (user-facing tier).

**Outcome:** The maturation exit mandates the graduating milestone write the
module's line/byte budget into its header, hand-read `wc -l -c` at hygiene
passes, no validator; Weight caps gained the module-budget bullet with the
compress-or-retire remedy; both hygiene sites read each of the repo's modules,
and /milestone-review step 9's graduation mints the header. All three plugin
modules retrofitted (records-hygiene 44/2,575 < 55/4,000; validation-doctrine
99/6,480 < 115/8,000; migration-protocol 185/11,924 < 200/14,000). Rulebook
pins re-seeded to 418/38,127. D-122 (annotates D-055; D-108 walk per D-098).

**Decisions:** D-122; gate choices with falsifiers in the work log (door
walked over parked; retrofit over forward-only; budget-only over pointer-check).

**Review:** Full fan-out. [O] 15 findings: 10 gate-fixed (ordering, headerless
modules, scoping, remedy home, D-122 pointer, comment, rewrap, DESIGN list),
3 rejected with reasons, F11 accepted-with-deviation (AC3 base figures).
[S] lenses: plan-time §1 prune self-corrected on main (f911896) — the mistake
M153 recorded, repeated; LESSONS line captured. Candidate row graduated here.
