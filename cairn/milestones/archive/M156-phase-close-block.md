# M156: Phase ends close with a standard block; gate chips stand alone

**Status:** done (2026-08-22, PR #157 https://github.com/jmgirard/cairn/pull/157)

**Goal:** Every skill ends with one recognizable close — recap, status table, copyable next command — and every decision chip is posed in the same turn as its presentation and readable on its own, so the user always knows at a glance what to do next.

**Outcome:** The phase-close rule in tracking-rules.md (section retitled "Question gates and phase closes"): every phase or skill end that hands off a next command closes with the block — outcome recap, status table or line, fenced next command(s) with plain labels, adjust-or-`/clear` safety line — never a chip; mid-skill continue/stop or acceptance decisions stay gates. The three chip-placement bullets went same-turn self-sufficient (compact decision substance in the chip's question/option text, full text verbatim best-effort above, file path cited where it lives on disk); the merge-approval gate follows the same form. Accessible-language identifier ban extended to option descriptions with a no-record-identifier-filler standard. All nine skills, migration-protocol, README (5 passages), and DESIGN.md converted; TestRoutingChipMandate retired, TestPhaseCloseBlock added (whitespace-normalized no-reintroduction sweep over all skills markdown), 8 mutation entries added/re-pointed; rulebook-mass baseline 443 lines / 40,949 chars, tagged M156, in its three sites.

**Decisions:** D-124 — routing chips retire into the phase-close block; supersedes D-003's routing-chip clause and D-022's one-click mechanism, absorbs D-019 as the norm, narrows D-123's turn-break prong; D-037's verbatim bar stands; re-open condition: a harness guarantee that pre-tool-call text renders reopens one-click routing.

**Review:** user-facing tier, three-lens fan-out: 23 findings (prior-PR 2, blame-history 1, diff-bug 20; one shared). 17 fixed at the gate — most notably a line-wrapped "routing chip" that evaded the line-based sweep and raw-substring guard (guard hardened to whitespace-normalized matching), the review skill's merge gate still mandating the two-exchange flow the milestone retired, README/DESIGN still documenting chip routing, and 7 chip-conclusion directives left pointing at an unsatisfiable placement. 4 rejected with logged reasons. No returns. At hygiene: one lesson added (wrapped-token sweeps); M151's done row pruned for terminal retention.
