# M173: A whole-list triage pass over candidates and Known issues

**Status:** done (2026-09-03, PR #177 https://github.com/jmgirard/cairn/pull/177)

**Goal:** Give cairn an on-demand `/cairn-triage` skill that reads every candidate row and every Known issues entry, proposes one disposition per item at one gate, applies the accepted ones in one docs-only commit, and records principled drops in a D-entry, so the lists are pruned when the operator asks instead of growing until a per-row hygiene chip nags.

**Outcome:** New `skills/cairn-triage/SKILL.md` (256 lines): preconditions (clean tree, default branch, synced), enumerate → assess → propose-and-gate → apply → record → validate-stamp-commit → close. Seven-word vocabulary (keep, compress, merge, split, drop, promote, route); five evidence classes in check order (staleness, overlap, overgrowth, misfiling, readiness), records-hygiene §7 rows taking §7's options first; a ~300-byte soft aim for `compress`, stated only in the skill. One table above one three-option chip (accept / amend / apply-nothing); nothing under `cairn/` written before the answer, only Accept applies, every unaccepted item byte-identical. A D-027-shaped decision entry only for principled drops and trigger-losing merges; refuted-premise and already-shipped drops named in the commit and stamp. Steps 4–6 skipped when nothing changes; one `triage:` docs-only commit otherwise; close block fences `/milestone-plan` per promote, else `/milestone`. Pointers from `/milestone` §2, `/milestone-review` step 9, README, DESIGN.md (skills × 10); CHANGELOG Unreleased entry; `cairn_cost.py` PHASES maps the skill; hand-run skill-set pins widened to ten.

**Decisions:** none milestone-local; plan-gate choices (standalone skill, both lists, ~300-byte aim, principled-drop D-entries only) recorded in the work log with their falsifiers.

**Review:** full fan-out. [S] blame-history and [S] prior-review-record: 0 findings each. [O] diff-bug: 19; AC5's two fresh dry runs (live repo: 21 + 5 items, 26 proposals, apply-nothing left the tree clean; scratch copy with both lists empty: empty table, chip posed, no failure) added 10 skill-text observations. 23 fixed as skill wording before the gate (§7 precedence over class order, validate before the stamp, a second Amend re-poses the chip, rewritten rows previewed, empty/all-keep skips the commit, fenced next command without a promote, `route` keeps provenance, D-entry template and numbering, colon-less subjects, preconditions moved first, citations corrected); 6 rejected with reasons. No follow-up rows, nothing graduated or retired.
