# M139: The narrowing repair for a defeated promise is reachable at a review return

**Status:** done (2026-08-14, PR #139 https://github.com/jmgirard/cairn/pull/139)

**Goal:** A review return whose only available repair widens an author-recalled
enumeration is classified and repaired at the return, without a full re-cut.

**Outcome:** `/milestone-review` step 5 ships the widening test — an
inside-the-domain failure whose only repair widens a recall-fixed enumeration
is an amendment return on D-097's track, never the defect count — carved out
of three colliding limbs (return floor ×2, amendment return);
`/milestone-implement` step 6 ships the repair direction (narrow per plan
step 4; wider inadmissible); 18 single-assert guards, registered one-to-one.
AC4's probe-coverage promise, defeated thrice, moved to a child milestone
(RR12 BC1–BC7, top candidate row; recorded exposure per D-103).

**Decisions:** D-101 (classification + counting), D-102 (three limbs; the IP4
violation record), D-103 (equality instrument + split), D-104 (draws D-103's
annotation); RB12/RR12 escalation decided the instrument question.

**Review:** four passes; three defect returns, each a new mechanism of one
shape (guard anchor reach vs rule extent); thrash (a)+(b) → RB12 → split.
Pass 4: 30 findings, 4 actioned (two comment fixes, D-104, one rejected with
reason), no return. Nothing graduated or retired.
