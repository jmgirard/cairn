# M91: Reference re-verification — the three partial extractions get read against their sources (done 2026-07-19)

**Goal:** re-read every claim in the three partially-verified references pages against its current source, so each extraction status states what was actually checked.

**Outcome:** the `references staleness` advisory goes WARN (3) → OK (0), earned by reading four external repos rather than by rewording statuses — the follow-through M89's archive named ("clearing them needs a re-read of three external clones"). Applied M83's `task-master.md` precedent. spec-kit 0.13.1.dev0 (`57cc518`): every claim held, all three citations still line-exact, re-anchored from bare filenames to full paths. BMAD-METHOD v6.10.0 (`c23f234`): two wrong — "Quick Flow" was removed (`removals.txt`; personas consolidated into the Developer agent), and `bmad-help` is user-invoked, not auto-run at every workflow end (1 of 26 non-agent skills calls it at close). Backlog.md v1.48.0 (`babd1d2`): three wrong, all lifecycle — the three staged guides are now one `agent-guidelines.md` with Creation/Implementation/Wrap-up phases, the agent checks its own ACs and sets Done rather than being forbidden to, and follow-up tasks are offered rather than gated on approval. Meridian (`d9b8775`, dormant since 2026-03-10): every claim exact. `competitive-landscape.md` claimed its eight inputs had "none re-read since 2026-07-11" — falsified by M83 the same day it was dated; corrected, with all five differentiators walked and dispositions recorded. Two corrections propagated into steal lists per the plan gate's break-a-steal rule.

**Key decisions:** AC3 amended via the step-6 gate — its premise ("the cited files moved since ingestion") was mine and false; `git log --follow` puts them at those paths since 2025-08-22, the page having cited bare filenames. Two guards fired correctly and were updated with justifications rather than loosened: `TestShippedPageStateLedger` (per its own deliberate-update protocol) and `TestUnlistedShippedFormsSatisfyTheShapeRule`, whose `partly verified at ingestion` entry M91 retired from the corpus.

**Review:** 6/6 criteria verified. Blame-history and prior-PR lenses clean — the latter confirming no prior-PR evidence exists at all (11 merged PRs, zero comments). Diff-bug lens: 6 findings (88/85/80/78/62/45). F4/88 — re-dating the derived synthesis note restarted its 180-day clock though half its inputs were never re-read, structurally M89's false-green shape; now ages from its oldest un-re-read input (`('ok', 2026-07-11)`). F3/85 — the FORMS docstring still said "four forms" over three tuples. F2/80 — the reconciliation justified differentiator 1 with a claim false under both readings. F1/78 fixed against its score: "now a separate installable repo" asserted drift never verified, the very failure the milestone exists to fix. F5/62 and F6/45 rejected with reasons.

**PR:** https://github.com/jmgirard/cairn/pull/90
