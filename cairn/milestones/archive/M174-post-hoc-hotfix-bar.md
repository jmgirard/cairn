# M174: A merged hotfix or adopted PR is verified to the hotfix bar post-hoc

**Status:** done (2026-09-03, PR #178 https://github.com/jmgirard/cairn/pull/178)

**Goal:** A hotfix or adopted PR merged outside the session re-enters `/hotfix` through a post-hoc verification of the merged diff — regression test, gate-lite, changelog — so the hotfix bar holds on every path to the default branch, not only the one the session drove.

**Outcome:** `/hotfix` step 1 "Merged-PR re-entry (M172, M174)": a PR-reference argument whose `gh pr view <N> --json state,headRefName` reports `MERGED` with a head not matching `m<nnn>-*` states in chat that the bar never ran before the merge, then runs seven moves before step 7 — tier check (an over-the-bar diff takes the existing disposition with no test demanded, step 7's close-out still runs, a `Fixes #N` close chip-gated first); baseline = `gh pr view <N> --json baseRefOid`, cross-checked against `git rev-parse <oid>^` of `--json mergeCommit --jq .mergeCommit.oid`, the current default branch never the baseline; two-way regression test on the checked-out up-to-date default branch and a `git worktree add --detach /tmp/<repo>-verify <baseRefOid>` outside the repo, a missing or passes-on-both test authored on a `hotfix-<slug>` branch, the worktree removed either way; the `verify` slot on the default branch (a red run the diff introduced is a third owed item); the `changelog` slot; owed items land only through step 5's authoring variant (a new PR; the merged head branch is gone) and step 6's chip, which also carries the merged PR's issue close; nothing owed → one `AskUserQuestion` acceptance chip folding the `Fixes #N` close, step 7 only on acceptance, a decline closing with a candidate row. Step 6's cross-reference updated; CHANGELOG Unreleased entry. `TestHotfixMergedPrReentry` (hand-run, D-109) pins the six moves whole with whitespace collapsed; six mutation entries. Rehearsed on merged hotfix PR #176 (`baseRefOid` 13be808f = `0a1b5b5c^`).

**Decisions:** D-131 (annotates D-130: the re-entry gains the bar against `baseRefOid`; D-108's trigger read as satisfied; rejected routing through `/milestone-review` route (b), revert-and-redo, accept-with-candidate-row, and a merge-method case split, each with its reopening evidence class).

**Review:** one pass. AC1–AC7 verified at 0b9d1ab, re-verified after the gate fixes; three-lens fan-out (blame-history and prior-review lenses clean; GitHub probe empty). Diff-bug lens: 8 findings. Fixed on-branch at the gate: F1 over-the-bar path reached step 7's unconditional issue close with no chip, F2 a red `verify` matched no branch, F3 chip misattributed to step 7, F5 `mergeCommit` is an object not an oid, F6 no checkout of the default branch, F7 no chat statement that the bar never ran. Rejected: F4 (step 3's own worktree command, unmodified), F8 (shared `/tmp` path, existing conduct). No return. Hygiene: the "Post-hoc hotfix bar" candidate row graduated; the M172 lesson extended; nothing retired.
