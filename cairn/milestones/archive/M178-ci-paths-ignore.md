# M178: cairn-init names the CI runs tracking-only commits start and offers the `paths-ignore`

**Status:** done (2026-09-05, PR #185 https://github.com/jmgirard/cairn/pull/185)

**Goal:** An adopter learns at `/cairn-init` that cairn's tracking-only commits start the repo's push-triggered workflows, can have `cairn/**` added to the `push` trigger's `paths-ignore` under a chip, and finds the fact and its merge-gate interaction stated in the rulebook's git model.

**Outcome:** `skills/cairn-init/SKILL.md` §0 gains the bullet "CI runs on tracking-only pushes": when `.github/workflows/` exists it runs `scripts/cairn_ci_paths.py --report` and, when a file reports a `push`/`pull_request` trigger or `unrecognized`, states (a) tracking-only commits reach the remote on every branch push and start the push-triggered workflows whose `branches` filter admits the branch, (b) a `pull_request` filter reads the whole PR diff so the ignore helps `push` triggers only, (c) a required check under branch protection stays pending on a path-skipped run, and (d) the by-hand item `- 'cairn/**'` under `paths-ignore` for each `push` trigger lacking both `cairn/**` and `paths` (a `paths` trigger left to the operator); silent with no directory or when every line reads `no push or pull_request trigger`; no chip. `scripts/cairn_ci_paths.py` (stdlib; `[ROOT] --report`, the only mode — any other flag exits 2; exit 2 outside a git repo since §0 runs before `cairn/` exists) prints one verdict per `.yml`/`.yaml` file: triggers with per-trigger `branches`/`branches-ignore`/`paths`/`paths-ignore`/`cairn/**` presence, `no push or pull_request trigger`, or `unrecognized` (no single `on:`, unplaceable, unreadable); writes nothing. `scripts/tests/test_ci_paths.py` (14 tests) over 28 `report/` fixtures with a recorded verdict each, byte-identity per run, and a PyYAML agreement oracle (skips with a message when absent). Rulebook git model: "A branch push starts CI, tracking-only commits included" (push-only reach of the ignore, the no-checks case, the branch-protection carve-out, mergeability deferred to the wait clause), the no-checks clause naming it as one source. `skills/tests/test_ci_paths_note.py` pins (harness-registered, tokens by concatenation). DESIGN `scripts/` line; CHANGELOG entry; D-133. The plan-gate choice of a chip-applied `--apply` edit was descoped 2026-09-05 after three defect returns on the line reader, each a legal YAML form missed (comment in the `on:` block, flush-left `paths-ignore` items, a column-0 comment truncating the block); it stands as the candidate row "Chip-applied `cairn/**` `paths-ignore` edit", needing a real YAML parse.

**Decisions:** D-133 (annotates D-128: the no-checks case gains a named source; suggest-only, the chip-applied edit dropped). Milestone-local: none; plan-gate choices and their falsifiers in the work log via git.

**Review:** four rounds, three-lens fan-out each (user-facing tier). Rounds 1–3 (2026-09-03): [O] 11, 5, 3 findings; returns 1–3 each an AC2 line-reader miss (thrash rule (a) and (b) fired at round 3; user chose park over descope, then resumed 2026-09-05 as suggest-only under a gated amendment of Scope, AC1–AC3, Coverage, T11). Round 4 (2026-09-05): [O] 6 — fixed at the gate: CHANGELOG and D-133 narrowed to the no-`paths` arm, DESIGN's `cairn_common` reuse sentence scoped to the tracking-file readers; rejected: quoted filter key and quoted-`#` flow-line verdicts (outside AC3's fixture set, Scope Out), `unrecognized` firing the bullet (the user's accepted gate widening), D-133's heading date (creation date). [S] blame-history and prior-review: none in every round. PR #185 conversation read: empty. Hygiene: lesson added (a hand-rolled reader of a structured format is held to a real parser oracle before anything writes over it); the 2026-07-18 references re-dating lesson pruned as stalest for the cap; M173 row pruned to retention.
