# M130: Review returns are reserved for breaches of bounded promises

**Status:** done (2026-08-02, PR #130 https://github.com/jmgirard/cairn/pull/130)

**Goal:** Review converges by construction: criteria promise only what a
named procedure can check; returns need a real breach (intraclass M100).

**Outcome:** `/milestone-plan` ships the bounded-promise rule (a universal
criterion names its enumerating procedure; a hand-list is not one) as the
criteria audit's third question, asked at `/milestone-brief` ingestion too.
`/milestone-review` ships the return floor (defect returns: in-domain AC
breach ≥80 or functional shipped defect ≥90; sub-floor findings triage, no
status change) and the amendment return — a wrong promise reworded on its
own count, second-same-AC stop, fixed work-log shape wired into
`/milestone-implement` step 6. §6 gains delete-first for branch-added
false prose. 24 rule sentences pinned and inversion-proven.

**Decisions:** D-097 (amendment returns on their own track, narrows D-064).
Plan gate: two-band floor over any-80+; live-on-merge over advisory;
delete-first scoped to unshipped prose (D-045 stands).

**Review:** 3 lenses → 26 scored; actioned D12/92 (wrong recorded counts),
D6/82 (shape unwired), D3/80 (procedure-less criteria missed) — all fixed
inline, zero returns. M118 lesson trimmed: list-half graduated into the rule.
