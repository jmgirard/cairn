# M182: Claim audit at implement time for user-facing prose; release-walk changelog claim-read

**Status:** done (2026-09-10, PR #189 https://github.com/jmgirard/cairn/pull/189)

**Goal:** A user-facing milestone whose branch adds behavior claims to prose has those claims read against the code before review, so review confirms a recorded audit instead of being the first claim-versus-code pass and the return floor's only exit.

**Outcome:** `/milestone-implement` step 7 **Claim audit** (Blocked and Completion renumbered 8 and 9): owed when `Surface tier:` reads `user-facing` and `git diff <default-branch>...HEAD -- . ':!cairn/'` adds lines; a fresh-context [O] reader that authored none of the lines reads every added line, reports each claim about what an artifact does, and reads it against that artifact in the same session; a refused spawn stops the step with a close block (freshness-spawns clause); one pass, a corrected claim re-read once by the same reader; fixed work-log line `claim audit: <N> claims read, <K> corrected — <files>` or `claim audit: not owed — <reason>` (first that applies of `internal tier`, `no added lines outside cairn/`), an absent line meaning the reader did not run. `/cairn-release` step 2 **Claim-read**: each behavior claim in the consolidated release section read against its artifact before the release-prep commit, a false one corrected in that commit, skipped with the step on a "none" changelog. D-136 passes D-090/D-108's door on the retained trigger, citing tidymedia M119/M120 for the measurement, with a falsifier per rule; the diff-scoped second review holds as a candidate row. The step ran on its own branch: 12 claims read, 1 corrected.

**Decisions:** D-136. Plan gate: fresh [O] reader over the author's re-read; candidate row over shipping the diff-scoped second review.

**Review:** user-facing tier, three-lens fan-out, one pass. [O] 10 findings: the stale `7. **Blocked?**` end anchor in `skills/tests/test_positional_labels.py` (the `section()` helper ran to EOF silently, suite green) fixed at the gate; three edge cases (same-reader re-read without agent continuation, an unfilled `Surface tier:` slot fitting neither not-owed reason, "release-prep commit" named only by the generic release-walk) filed as one candidate row; "twice" in D-136 kept at the maintainer's call as recurrence, not a count; five rejected. [S] blame-history 2 findings (D-136's out-of-repo citations), rejected as AC3-mandated; [S] prior-review: no regression, PR-comment probe empty. Defect-return count 0, amendment-return count 0. Nothing graduated; the M148 fixture-anchor lesson extended.
