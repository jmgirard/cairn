# M181: cairn-init applies the `cairn/**` `paths-ignore` edit under a chip, parsed by PyYAML

**Status:** done (2026-09-06, PR #188 https://github.com/jmgirard/cairn/pull/188)

**Goal:** An adopter whose Python has PyYAML gets the `cairn/**` `paths-ignore` item added to each editable `push` trigger by `/cairn-init` under one approve/decline chip, the edit verified against PyYAML's own reading before it is written and left uncommitted for the operator.

**Outcome:** `scripts/cairn_ci_paths.py --apply [--dry-run]`: PyYAML imported inside the apply path only (any raise at the import → exit 3 naming PyYAML, nothing written); placement by `yaml.compose` node marks, lines inserted only (`- 'cairn/**'` after the last item, or a created `paths-ignore:` key after `push`'s last child; a final line lacking its ending gains one); `_anchor_reach` refuses any anchor, alias, or plain `<<` in the `on` span; a `safe_load` post-edit equality check before the write; twelve closed refusal reasons in check order, every refusal byte-identical, exit 0 whatever the verdicts; any raise while composing refuses as unparseable and any later raise as post-edit (the whole-plan guard). `--report` unchanged, stdlib. Fixtures: 12 `apply/pairs`, 10 `apply/refused`, 4+6 `apply/returns`; `TestStdlibOnly` admits `yaml` for this script alone. `/cairn-init` §0 clause (d): dry run, one chip naming the `would apply` files (a decline writes nothing, the by-hand suggestion standing), apply + re-report, the edited file never staged by the §1/§3 commits and named in both close blocks; a scalar or flow-list `on:` is refused and the by-hand suggestion says to rewrite it in block form first. D-135 annotates D-133 (`writes nothing` binds `--report`; PyYAML an optional dependency of `--apply` alone). CHANGELOG's unreleased M178 entry amended in place; DESIGN `scripts/` layer names the one writing mode and the one non-stdlib import; tracking-rules git-model bullet updated.

**Decisions:** D-135. Plan gate: PyYAML optional over hard or vendored; scalar/flow-list `on:` refused over rewritten; the edited file left uncommitted.

**Review:** user-facing tier, three-lens fan-out, three passes. Pass 1: 12 findings, defect return 1 (F1 quoted-`on` crash, exit 1; F2/F3 anchors and merge keys reaching `pull_request` or `paths`) — T6–T8; the anchor refusal took a gated AC2/AC3 amendment. Pass 2: 12 findings, defect return 2 (G1 `RecursionError` past a `YAMLError`-only guard) — thrash trigger (b) fired, closed by the structural whole-plan guard (T9); G2/G3/G5 gated AC2 amendments; G7/G8 filed as the `--report`/`--apply` disagreement candidate row. Pass 3: 8 findings; H1 (an import raising past `ImportError` escaping as exit 1) fixed at the gate with H4/H6 prose, H2/H3 filed as the safe-over-refusal candidate row, H5/H7/H8 rejected. Blame-history and prior-review lenses: zero findings each pass. Defect-return count 2, amendment-return count 0. Graduated the `[high]` "Chip-applied `cairn/**` `paths-ignore` edit" candidate row; the M178 parser-oracle lesson extended, nothing retired.
