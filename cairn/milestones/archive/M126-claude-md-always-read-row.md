# M126: CLAUDE.md joins the always-read governance frame

**Status:** done (2026-07-31, PR #126 https://github.com/jmgirard/cairn/pull/126)

**Goal:** The frame's worked table covers every surface this repo loads at
session start, `CLAUDE.md` included, and says why cairn's governance of it
stops at the cairn section.

**Outcome:** A sixth row in `tracking-rules.md`'s "Always-read governance" table
for `CLAUDE.md`'s `## Project tracking` section — inflow D-009's routing-only
rule, outflow the trim-to-template weight-caps remedy, signal the <30-line
section cap — appended below the fifth. A statement beneath it gives the
governed unit as the section, not the file (D-018), against the milestone file's
cap-exempt sections, still governed by a read-bound (D-063); uniqueness is
claimed for neither. Guards: the statement pinned entire under two-sided
whitespace normalization; the table by unique header plus membership, order and
per-row column integrity; a section-bounded position check.

**Decisions:** D-094 (the sixth surface, its section-scoped unit, three exclusions).

**Review:** Three rounds; R1 and R2 returned. R2's six >=80 included the §8
structural remedy failing to close its class, firing §8's stop-rule falsifier
(one occurrence spent). RB11/RR11 (Fable) reversed the guard approach
additive->subtractive; BC1/BC3 ingested verbatim, BC2 amended, BC4-BC6 deferred.
R3 fixed F2 (92, decoy table), F6, F8 at the gate.
