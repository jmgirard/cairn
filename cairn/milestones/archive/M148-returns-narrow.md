# M148: Review returns narrow the promises, never widen them

**Status:** done (2026-08-17, PR #149 https://github.com/jmgirard/cairn/pull/149)

**Goal:** After a defect return the amendment surface recommends narrowing or
holding — widening is the user's explicit, never-recommended decision — and the
plan gate's full audit flags promises binding the verification instrument.

**Outcome:** `/milestone-implement` step 6 gained the return-adjacent direction
rule (narrowing-or-holding the one recommended option; a user-selected widening
logged per criterion; the motivating finding offered a candidate row or split
milestone; widening-test-reclassified amendments carved out to D-101) and a
re-entry sentence naming every assigned-mode question, full-mode instrument
included. `/milestone-plan` step 3's full audit gained the instrument-binding
question (deliverable property vs a verifying instrument's; disposal: move to
tasks/gate procedure or narrow), tiebroken to never relax the probe question;
the reduced audit stays at two questions. Restatement sweep dispositioned every
hit; "Stakes-tier follow-through" row trimmed to its tier-recording half.

**Decisions:** none milestone-local; D-118 promoted (annotates D-101; narrowly
supersedes D-111's full-mode question enumeration; hosted per D-098).

**Review:** three-lens fan-out (user-facing tier); blame-history and prior-PR
lenses zero findings; [O] diff-bug ranked 10 — 3 fixed at the gate, 7 rejected
with logged reasons; no floor return. Suites 308/103/513 OK, validate clean.
