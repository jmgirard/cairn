# M187: Guest-mode DESIGN.md — principles are the operator's provisional model of the maintainers' constraints

**Status:** done (2026-09-10, PR #194 https://github.com/jmgirard/cairn/pull/194)

**Goal:** In guest mode, DESIGN.md's Conventions and IP/GP principles are stated to be the operator's provisional model of the maintainers' constraints — inferred from the upstream's public norms, corrected by maintainer feedback — and the seed and the interview say so.

**Outcome:** tracking-rules "Collaboration mode" gains one bullet: Conventions and IP/GP principles are the operator's provisional model of the maintainers' constraints, sourced from CONTRIBUTING, style docs, and review feedback; one maintainer feedback contradicts is corrected in place, marked `corrected <PR ref>`, no D-entry. The IP/GP definition and the "Correcting a record proven false" principle exception each carry a clause naming that bullet as the guest reading. `/cairn-init` "Guest mode" item 4: the DESIGN seed reads `CONTRIBUTING*` and `.github/*.md` (listed under `bash -c`, since zsh aborts an unmatched glob with an error the redirect cannot hide) plus any relative in-repo path they name, cites each Conventions line's source, states an empty listing; the close block is item 5 and the opener's runs-as-above list no longer names the seed. `/design-interview`: a Session-start paragraph entered on the PROFILE mode line (the interview elicits the operator's understanding of the maintainers' design), the opening line's guest parenthetical, and `(provisional — guest mode)` on every principle Phase 2 writes.

**Decisions:** D-139 (annotates D-137; in-place correction with no D-entry; rejected: the owner-mode D-entry rule for guest corrections, and a no-IP guest interview).

**Review:** Implement-time claim audit read 17 claims, corrected 1 (the zsh glob abort; re-read in a fresh reader, this session having no tool to continue the first). Three-lens fan-out: blame-history and prior-PR-comments clean; diff-bug 8 findings — F1 (seed "nothing it infers" overreach) and F3 (interview opening line) fixed with F8's rewrap; F2 (Conventions unmarked), F4 (pointer phrase), F5 (shared skeleton comment), F6 (AC1's "History integrity" naming slip, same passage), F7 (`bash -c` exit code, already audited) rejected with reasons. No returns. Nothing graduated or retired at hygiene; the zsh gotcha lives in the cairn-init prose (ownership exit), so no LESSONS line.
