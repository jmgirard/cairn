# M132: A criterion's enumerating procedure covers the domain its promise quantifies over

**Status:** done (2026-08-02, PR #132 https://github.com/jmgirard/cairn/pull/132)

**Goal:** The bounded-promise rule rejects a criterion whose named procedure enumerates
a proxy for its domain rather than the domain itself, and names the repair.

**Outcome:** `/milestone-plan` step 4's bounded-promise rule gains the domain-match test
— the named procedure must enumerate the domain the criterion's own universal quantifies
over. The failing form is identified by its property (membership fixed by author recall,
not decided by a procedure over the domain), examples marked non-exhaustive so a
family-enumeration cannot escape the list; both remedy halves stated — a counterexample
is not answered by a wider enumeration, the repair is to narrow the promise until a
stated procedure settles it. The audit's third question carries the test at
`/milestone-plan` step 3 and `/milestone-brief` ingestion in one byte-identical sentence,
enforced by a cross-surface guard nothing previously covered. 8 blocks blank-RED.

**Decisions:** D-098 (a downstream repo's surfacing of a defect in cairn's shipped rules
is hosted by a cairn milestone; annotates D-090). Plan gate: positive repair beside the
refusal; second case judged by a fresh reader; section-level inversion.

**Review:** 3 lenses → 21 scored, 4 actioned — F1/PR1 (95, a partial pin let part of a
rule sentence delete green; both lenses converged), F8 (90, wrong registry-comment
count), F5 (82, insertion broke a pronoun's antecedent), F2 (85, AC5's own procedure
enumerated lines while promising over sentences → lesson). Zero returns.
