# M143: The thrash threshold recommends descoping before re-planning

**Status:** done (2026-08-15, PR #142 https://github.com/jmgirard/cairn/pull/142)

**Goal:** At the thrash threshold `/milestone-review` recommends shrinking or
parking the milestone rather than re-cutting the same objective.

**Outcome:** Step 4's thrash block recomposed whole: trigger (a)'s remedy is
descope-or-park (descope via the gated amendment protocol, remainder to
candidates or a split, re-review; or park), a same-objective re-cut present
but never recommended and gone once spent; the composition paragraph composes
the chip from (a)'s menu, carries (b)'s diagnosis/escalation in, and names
where (b)'s remedy lives (D-106). Whole-slice THRASH_FIXTURE added beside
re-anchored pins; lineages pinned (intraclass `1e95baf`: 4 defect + 1
amendment returns; circumplex `b343054f`: 15-milestone norms arc).

**Decisions:** D-105 (narrowly supersedes D-064's trigger-(a) remedy clause);
D-106 (narrows D-105's at-every-firing clause; adds the (b)-remedy sentence).

**Review:** Three lenses, 17 findings, 0 scored ≥80; maintainer directed
F1/F2 (both 60) fixed at the gate — D-106 plus the (b)-remedy sentence —
rather than accepted as logged; F5 (78, circumplex lineage is a milestone
count, not a work-log return count) surfaced verbatim at the gate and
accepted. One lesson captured (probe reds under a whole-slice fixture are
non-localizing).
