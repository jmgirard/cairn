# M56: LLM Wiki investigation — references/ + linking fit assessment (done 2026-07-16)

**Goal:** Assess Karpathy's LLM Wiki formalism (+ ecosystem) against cairn's
references/, cross-file linking, and agent-memory model — investigation only.

**Outcome:** `references/llm-wiki.md` — primary-source read of the gist,
ecosystem scan (openwiki/nvk/SamurAIGPT/DeepWiki), three fit assessments,
disposition ledger. Verdict: cairn already IS a governed LLM Wiki for project
state; the ecosystem independently re-derived cairn-style governance
(human-owned schema, mechanized lint). Two real deltas + one advisory banked
as the grouped "references/ + linking hardening" candidate (synthesis-note
page type, INDEX↔disk references lint, dangling M/D-token ADVISORY with
M57/M99 FP tolerance); governed-LLM-Wiki positioning absorbed into the
release-prep candidate.

**Rejected with reasons in the note:** `[[wikilink]]` syntax (bare ID tokens
are cairn's link syntax — 480 M / 249 D / 96 IP-GP cites, zero dangling),
committed raw sources, a references log.md, a formal query op, graph tooling,
free-form-page structural steals.

**Review:** all 6 ACs fresh-evidenced; validate 14/14; three lenses + scorer —
1 finding (F1/88, missing DeepWiki/AutoWiki anchors) fixed; prior-PR lens
no-op (52 PRs, zero inline comments).

**PR:** https://github.com/jmgirard/cairn/pull/54
